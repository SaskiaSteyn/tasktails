# IMY761 — Feature List

> Implementation-level breakdown of all features to be developed.
> Status values: `To Do` · `In Progress` · `Done`
> Cross-reference: [Requirements.md](Requirements.md)
> Last updated: 2026-08-10

---

## Module 0 — Infrastructure & Database

| # | Feature | Type | Status |
|---|---------|------|--------|
| INF-01 | Prisma schema — `User` model (id, email, password hash, username, displayName, avatarUrl, role, A/B group, createdAt) | Database | Done — migrated; Postgres in `docker-compose.yml` |
| INF-02 | Prisma schema — `Task` model (id, userId, title, complexity tier, dueDate, completedAt, createdAt) | Database | Done |
| INF-03 | Prisma schema — `Subtask` model (id, taskId, title, completedAt) | Database | Done |
| INF-04 | Prisma schema — `StoreItem` model (id, name, category, levelRequired, coinPrice, imageUrl) | Database | Done |
| INF-05 | Prisma schema — `InventoryItem` model (id, userId, storeItemId, equippedToPetId, quantity) | Database | Done |
| INF-06 | Prisma schema — `CartItem` model (id, userId, storeItemId, quantity) | Database | Done |
| INF-07 | Prisma schema — `Transaction` model (id, userId, storeItemId, coinSpent, purchasedAt) | Database | Done |
| INF-08 | Prisma schema — `Pet` model (id, userId, storeItemId, happiness, hunger, lastInteractedAt) | Database | Done |
| INF-09 | Prisma schema — `TelemetryEvent` model (id, userId, eventType, payload JSON, createdAt) | Database | Done |
| INF-10 | Prisma schema — `UserEconomy` model (id, userId, coins, xp, level, streak, lastStreakDate, dailyCoinsEarned, dailyXpEarned, dailyCapResetAt) | Database | Done — migrated; AUTH-04 creates the row with the user |
| INF-11 | NextAuth configuration (credential + Google providers, JWT strategy, session callbacks) | Backend | Done |
| INF-12 | Global layout component — header with persistent coin balance, XP bar, and level indicator | Frontend | Done — `AppShell` + `AppHeader`/`PersistentHeader` in `src/components/layout/`, reads through `src/lib/economy.ts`. Two densities, both from the handoff: greeting + XP/streak row (dashboard) and title + coins (store, sanctuary). Profile and Settings now use the shell. Three AA gaps ship as designed — level disc 3.79:1, streak pill 2.43:1, streak numeral 2.69:1 — recorded in `globals.css` for INF-14 to accept or fix. (Note: this row previously cited 3.84/2.52/2.94, an earlier measurement pass superseded by `globals.css`'s "re-measured 2026-07-28" figures; corrected here during the dev-merge audit, 2026-07-29 — no code changed, only this note). **Updated 2026-08-12, at the user's request** (two header screenshots compared, "Leaderboard" vs "Sell items"): every sub-screen's back-chevron header — 12 of them, one per screen, all hand-duplicated `<header>` JSX with no shared component to reuse — now uses one exact padding: `py-[14px] px-[18px]`, matching `TaskTails Screens.dc.html`'s back-chevron frames (`Edit task`, `Your cart`, `Purchase history`, `Customize Mochi`, `Settings`) verbatim — all five use the identical inline `padding:8px 18px 14px`, confirming one true spec exists and every header should match it. Before this: 4 headers used `p-3` (flat 12/12, no safe-area handling at all — `PRO-10`/`PRO-11`/`TASK-03`/`LEAD-09`), 2 used flat `pt-[14px] pb-[14px]` (`PET-01`/`PET-05`), and 6 already had safe-area-aware top padding but at the wrong base offset, `6px` instead of the mock's `8px` (`GATC-12`/`GATC-15`/`GATC-17`/`STOR-06`/`STOR-09`/`PRO-18`) — that `6px` figure turned out to be copied from `AppHeader`'s own spec (a *different* header variant, the persistent greeting/title header, whose mock frame uses `padding:6px 18px 14px`, 6px specifically because it's a different screen role) rather than from the back-chevron mock frames these screens are actually built from. **Found in passing**: `AppHeader` itself (this ticket's own component) had silently regressed from `pt-[calc(6px+env(safe-area-inset-top))]` to flat `pt-[14px]` in an unrelated 2026-08-01 commit about pet visuals — the doc comment above it still described the safe-area add-on behavior the code no longer had. Restored to match its own comment; unrelated to the 12 back-chevron headers (different component, different mock spec, correctly still 6px) but found while auditing this exact class of bug. Verified: `tsc` and `eslint` clean across all 13 touched files; the generated CSS was confirmed correct by direct inspection of the dev server's compiled stylesheet (`padding-top: calc(8px + env(safe-area-inset-top))` present for the new selector) — the session's own browser preview tool served a stale cached copy of that CSS chunk that a cache-busting fetch couldn't clear, so a live pixel-diff screenshot wasn't possible this pass; the fix is confirmed correct by source, `tsc`/`eslint`, and the live-server CSS response, not by a rendered screenshot |
| INF-13 | Responsive layout system — mobile and desktop breakpoints using Tailwind | Frontend | Done — `--breakpoint-frame` (480px) and `--container-app` in `globals.css`; the phone frame now switches to a centred card just past the widest phone rather than at `sm` (640px), where a 300px design was being stretched across tablet widths. `AppShell` is the single frame — the three hand-copied versions (auth, onboarding, app) are gone. Safe-area insets wired through `viewportFit: "cover"`. Documented at `/style-guide#s07`. **Fixed 2026-07-30**: `BottomNav` wasn't pinned — a screen with more content than fit the viewport grew `body` past it (`min-h-full`) and pushed `nav` down with the page, since nothing in the flex chain from `body` to `main` was height-bounded (a flex item's automatic minimum size is its content size unless told otherwise). `body` is now `h-full overflow-hidden` (`src/app/layout.tsx`) and every level of `AppShell`'s chain got `min-h-0`; the `frame:` breakpoint's card changed from `min-h-[640px]` (growable) to `h-[640px]` (bounded) for the same reason. `main` now scrolls its own content (`overflow-y-auto`) rather than the page doing it. Verified live with 13 seeded tasks: mobile 375px and the desktop 640px card both keep `nav` at the exact bottom edge (`document.body.scrollHeight` no longer exceeds `innerHeight`) while the task list scrolls internally |
| INF-14 | WCAG AA compliance pass — contrast ratios, keyboard navigation, focus states, colour-independent signals | Frontend | Done — contrast is measured with **BPCA** (bridge for WCAG 2 contrast using APCA), not plain WCAG 2: `src/lib/contrast.ts`, ported from `bridge-pca` 0.1.6 and checked by `scripts/verify-bpca.ts`. **Done:** focus ring changed from terracotta (2.40–2.69, failed everywhere) to ink via an inheritable `--focus-ring`; skip link (2.4.1); "Skip for now" target raised to 32px (2.5.8); `Modal` ids generated + `aria-describedby`; field errors are `role="alert"` and focus moves to the first invalid field. Keyboard nav, headings, labels, alt text and colour-independent signals audited clean. **Deferred to alpha by decision:** the four text tokens at ~3.6–3.7 on dark surfaces, the INF-12 header trio, and eight non-text-contrast items (input/checkbox boundaries, progress fills, step dots, secondary + destructive borders) — all listed in the `globals.css` audit block |
| INF-15 | Docker container configuration for EC2 deployment (Dockerfile + docker-compose) | Infrastructure | Done — multi-stage `Dockerfile` on Next's `output: "standalone"` (316MB, runs as non-root `node`, healthcheck), plus `migrate` and `app` services behind a compose `app` profile so plain `docker compose up -d db` stays build-free. Built and run end-to-end: register → sign in → authenticated `/profile`, with the `UserEconomy` row written through the container. Two bugs found by running it — the Prisma client was constructed at import, making `DATABASE_URL` a *build*-time requirement (now lazy, `src/lib/prisma.ts`), and the AUTH-06 sign-in redirect was pinned to `localhost:3000` regardless of `Host`/`X-Forwarded-Host`, which would have broken every redirect behind the EC2 proxy (now derived from the request, `src/proxy.ts`) |
| INF-16 | Environment variable configuration (`.env.example` for Vercel + AWS connection strings) | Infrastructure | Done — `.env.example` rewritten as the reference: every variable, which file it belongs in (`.env.local` vs Compose's `.env` vs the instance environment), and connection-string recipes for RDS, Neon and Vercel. Cross-checked against `process.env` usage — nothing undocumented, nothing stale. Two traps documented after confirming both: copying the template to a Compose `.env` overrides `DATABASE_URL` to `localhost`, which inside the container is the container; and `sslmode=require` means *verify-full* under the pg adapter (verified — `sslmode` is parsed and enforced), so RDS needs `sslrootcert` or it fails cert validation |
| INF-17 | A/B group assignment middleware — enforces Group A/B isolation on all store API routes server-side | Backend | Done — no store API routes exist yet to attach to (STOR-10..18, URG-08 all still To Do), so this ships the enforcement primitive they'll call: `groupGatedData()` in `src/lib/study-group.ts`, returning `null` for Group A/signed-out and the computed value for Group B. Unit tested in `src/lib/study-group.test.ts` against a mocked Prisma client. No test runner existed in this repo — added `vitest` + `vitest-mock-extended` (`vitest.config.ts`, `src/test/`) to support it |
| INF-18 | Prisma schema — `UserSettings` model (id, userId, dailyReminder, streakAlert, soundEffects, reduceMotion) | Database | Done — migrated (`inf18_user_settings`), with a backfill so every existing account got a row. Created with the account like `UserEconomy`, so PRO-15 can update rather than upsert. Defaults live in the schema and match the Settings frame: notifications on, preferences off |
| INF-19 | Prisma schema — `Achievement` + `UserAchievement` models (id, key, name, description, criteria, unlockedAt) | Database | Done — migrated (`inf19_achievements`). `criteria` is JSON with a documented discriminated shape; `@@unique([userId, achievementId])` is what makes PRO-09 idempotent, and absence of a row is what "locked" means. **Catalogue deliberately not seeded** — the design draws six badge tiles but names none, so PRO-09 owns what the badges are |
| INF-20 | `StoreItem` catalogue seed script — populate the store with the `design_handoff` catalogue and animal artwork. Nothing else in the store module writes `StoreItem` rows | Database | Done — `prisma/seed.ts`, wired through `prisma.config.ts`'s `migrations.seed`. Deliberately small: only the 5 items the design mock actually names/prices (Sunflower seeds, Red collar, Treat box, Cosy den, Fox kit) plus 2 more animal unlocks it doesn't invent catalogue data for the rest of the 4-category × 5-level grid, which nothing in `design_handoff` or `economy_system.md` grounds. **3 animal types only** (Koala Lvl 1 starter, Fox Lvl 7, Penguin Lvl 10) — economy_system.md frames the Lvl 7/10 unlocks as a deliberately scarce "2nd/3rd animal type," not a full 22-animal roster; the other ~19 `design_handoff` SVGs are unused. Fox's level comes from the Store mock's own explicit "Fox kit · 2nd animal · Unlocks at Lvl 7" label, taken over the README's looser "starter pet" aside. Non-animal items have no real artwork in the handoff (the mock renders them as flat colour swatches) — `imageUrl` holds a `lucide-react` icon name for those, and a `public/animals/*.svg` path for the 3 animals. Idempotent (matches by `name`, not a DB constraint — `name` isn't unique in the schema). Verified end-to-end against a scratch Postgres container: applied migrations, ran the seed twice, confirmed exactly 7 rows both times with no duplicates |
| INF-21 | Level threshold table — shared XP-per-level constant consumed by ECO-05 and the header XP bar | Backend | Done — `src/lib/levels.ts`. Deviates from the mock: the XP bar fills *within* the current level and empties on level-up, where the mock fills it against the cumulative threshold. INF-12's header label must therefore read `xpIntoLevel / xpLevelSpan` ("7 / 20 XP"), not the mock's "42 / 55 XP" |
| INF-22 | **Desktop adaptation pass** *(low priority)* — adapt the mobile-first app screens to wide viewports: max-width centred column, bottom nav → side rail, two-column dashboard, wider store grid. Distinct from INF-13, which builds the breakpoint system; this is the per-screen pass that uses it. Satisfies the desktop half of NFR-GEN-2. **Deprioritised:** `design_handoff` only specifies phone frames (300×640) for the app — the desktop layouts would be invented, and participants use the study on mobile. The marketing site (MKT-01/02) and admin cards (ADM-01/02) are already desktop-first and are *not* in scope here | Frontend | To Do |
| INF-23 | CI/CD — [GitHub #165](https://github.com/SaskiaSteyn/tasktails/issues/165), branch `165-cicd-automated-testing`. Automated testing via GitHub Actions on every push to `dev`/`main` and every pull request | Infrastructure | Done — `.github/workflows/ci.yml`, four parallel jobs (`lint`, `typecheck`, `test`, `build`), each a fresh `npm ci` on Node 22 (matching the Dockerfile). `DATABASE_URL` is set to the same throwaway value the Dockerfile's deps stage uses, since `prisma.config.ts`'s `env()` throws without it even for `generate` — nothing in CI ever dials it; `next build` itself needs no database (`src/lib/prisma.ts` is lazy) and no `AUTH_SECRET`. **Found and fixed two real, previously-undetected bugs while getting the suite to a state CI could trust**: `pets.test.ts` was failing at import time on every run (`next/server` unresolvable from `next-auth/lib/env.js`, a Vite-resolver quirk against this repo's `exports`-less `next` build — see AGENTS.md) and had been doing so silently since before this ticket, `npm test`'s non-zero exit swallowed into "239 passed" summaries that never mentioned the one file erroring out with zero tests collected; fixed the same way `economy.test.ts`/`achievements.test.ts` already do it, `vi.mock("@/auth", ...)`, since `pets.ts` pulls `next-auth` transitively through `economy.ts`. That unmasked a second, older bug the crash had been hiding: `recordPetInteraction`'s own `describe` block never mocked `prismaMock.$transaction` (unlike its `recordFeedInteraction`/`createPetForTransaction` siblings in the same file), so its assertions were silently checking a `vi.fn()` that had never run the callback; and two of its exact-match `toHaveBeenCalledWith` expectations still lacked `timesPetted: { increment: 1 }`, added to `recordPetInteraction`'s update by PRO-18 after these tests were last touched. All three fixed in `pets.test.ts` — no application code changed, only the tests now actually exercising it. Verified by simulating a real CI run rather than trusting the YAML: built a detached `git worktree` from a clean commit with the staged diff applied, confirmed no `.env.local`/`node_modules` present, then ran `npm ci` (dummy `DATABASE_URL`) → `lint` → `typecheck` → `test` → `build` in sequence exactly as the workflow does — all four green, 12 test files / 184 tests passing (the local dev tree's own `npm test` shows 15/258 because a stray gitignored `.claude/worktrees/` leftover triples three files; irrelevant to CI, which only ever sees a real checkout) |

---

## Module 1 — Authentication

| # | Feature | Type | Status |
|---|---------|------|--------|
| AUTH-01 | Register page — form with email and password fields | Frontend | Done — shares `TextField` (`src/components/ui/text-field.tsx`) with the login page; see AUTH-02's row for the 2026-08-12 iOS-zoom fix, which fixed both at once |
| AUTH-02 | Login page — form with email and password fields | Frontend | Done — **Updated 2026-08-12, UX/UI pass**: the shared `TextField` primitive's 14px input text bumped to 16px — iOS Safari/Chrome zooms the page in on focus below that size, reported live on the New Task sheet (see TASK-02's row for the full note) and found to affect every text input in the app, including this one and register's (AUTH-01), since both are built on this one component. Verified live: computed `font-size` on the email field is `16px`. `tsc` and `eslint` green |
| AUTH-03 | Logout button and action in global header | Frontend | Done — on Settings (`/settings`), where the designs put it, reached from a gear in the Profile banner. Now asks for confirmation via SHR-03's modal before signing out (added alongside SHR-03, not originally) |
| AUTH-04 | `POST /api/auth/register` — create user, hash password, assign A/B group randomly, initialise `UserEconomy` record | Backend | Done — backed by Prisma + Postgres |
| AUTH-05 | NextAuth sign-in handler (`POST /api/auth/[...nextauth]`) — validate credentials, issue JWT | Backend | Done — the A/B group is deliberately **not** on the session; read it with `currentStudyGroup()` |
| AUTH-06 | Protected route middleware — redirect unauthenticated users to login | Backend | Done — `src/proxy.ts` (Next 16 renamed middleware to proxy); everything is protected unless allowlisted |
| AUTH-07 | Change username from the profile — inline editor with the same live availability check as the onboarding step | Frontend | Done — **Updated 2026-08-12, UX/UI pass**: the username field's 14px input text (and its "@" prefix, so the two stay matched in size) bumped to 16px — iOS Safari/Chrome zooms the page in on focus below that size. See TASK-02's row for the full note. `tsc` and `eslint` green. **Updated 2026-08-12, at the user's request**: some buttons in the app carried an icon beside their label and some didn't, read as inconsistent — dropped every such icon (kept text-only) across the four real cases found: this row's "Edit" (`Pencil`), PET-04/PET-05's "Go to store" (`ShoppingCart`, each), and PWA-07's "Install app" (`Download`). Left alone: icon-only buttons (nav tabs, back chevrons, checkboxes, the FAB) — removing those would leave nothing to tap; the bespoke `Coin` token beside a price (`CartPanel`'s checkout, `LuckyBoxHome`'s "Open") — load-bearing currency marker, not a decorative icon; and the pet-rename trigger's name+pencil (`PetCustomizer`) — no background fill, an inline edit affordance rather than a labelled action button like the others. Verified live: registered a fresh account, confirmed "Edit" renders as plain text with no pencil. `tsc` and `eslint` green. **Follow-up the same day**: missed on the first pass — a literal `"+ "` prefix on three button labels reads as the same icon-like inconsistency even though it's plain text, not a Lucide component. Dropped from all three: SHR-04's "New task" (was "+ New task"), and both subtask "Add" triggers, on the create sheet (TASK-02) and the edit screen (SUB-02) — see those rows for their own short notes. The confirm-add square button that appears once one of those is tapped (a `Plus` icon, no text) is unrelated and correctly untouched — that one actually is icon-only. `tsc` and `eslint` green |
| AUTH-08 | "Continue with Google" button on Register and Login — full-width OAuth button, hidden when the OAuth env vars are absent | Frontend | Done |
| AUTH-09 | Google OAuth provider — NextAuth Google provider; upserts the account on first sign-in and assigns the permanent study group | Backend | Done |

> **Out of scope:** the Login design shows a "Forgot password?" link. Password
> reset is deliberately not ticketed — participants are onboarded directly and
> the study has no mail infrastructure. Remove the link when building AUTH-02.

---

## Module 2 — TODO App

### Task Management

| # | Feature | Type | Status |
|---|---------|------|--------|
| TASK-01 | Task list view — displays all user tasks with title, due date, complexity badge, and completion state | Frontend | Done — `src/app/tasks/page.tsx` at `/tasks`, now the login flow's post-auth landing spot (was a placeholder pointing at `/onboarding`). Reuses `<PersistentHeader />`'s greeting variant for the stats bar (TASK-06, already built by INF-12) rather than rebuilding it. `src/lib/tasks.ts` is the new storage-access module (mirrors `users.ts`/`economy.ts`); `src/lib/task-tiers.ts` is the tier label/color/base-coin table from `economy_system.md`, shared for later reuse by ECO-01. `TaskRow`/`TaskList` in `src/components/tasks/` match the Dashboard frame's row and the "Empty tasks" state exactly, including the README's "13% alpha bg" tier tag styling. Two deliberate deviations from the mock, both noted inline: the "TODAY" section label is dropped (this view shows *all* tasks per TASK-7, not just today's — the label would misrepresent a task due next week), and the empty state's "+ New task" pill is inert, not a link (TASK-02 doesn't exist yet). Read-only — creating/editing/deleting/completing are TASK-02..05. Verified against a scratch Postgres: registered a real account, confirmed the empty state, then seeded 4 tasks (varied tiers, due/undue, one completed) and confirmed ordering (incomplete-by-due-date first, done last), tier badges/coins, and the completed row's strikethrough + sage check. Along the way found and fixed a real repo bug: the committed generated Prisma client was stale (missing `username`/`displayName`/`settings` from INF-18/19), breaking registration outright — `npx prisma generate` fixes it locally, but it's gitignored so this doesn't show in the diff; worth a `postinstall`/CI check if it recurs. **Extended 2026-07-30** at the user's request: subtasks now render and complete inline, indented under their parent task, instead of only being reachable from the edit screen's `SubtaskList`. `tasksForUser()` now includes subtasks (`src/lib/tasks.ts`); `TaskRow` (`src/components/tasks/task-row.tsx`) was generalised to serve both — `dueDate`/`complexityTier` are now optional, `coins`/`href` are explicit props rather than derived internally, and its root changed from `<li>` to `<div>` so `TaskList` can wrap a task's row and its nested `<ul>` of subtask rows in valid list markup (a subtask row nested `<li>`-inside-`<li>` without an intervening `<ul>` would have been invalid HTML). A subtask row links to its *parent's* edit page, not its own (subtasks have none), and completes for real through SUB-05's existing endpoint — `router.refresh()` afterward is what picks up SUB-4's auto-complete of the parent, the same mechanism already used for a level-up or coin change. Verified live: created a task with 2 subtasks via the create-sheet extension below, completed one from the dashboard (coins/XP/streak updated, row struck through), completed the second and watched SUB-4 auto-complete the parent (its checkbox now disabled, no double-grant) — matches on both mobile and the desktop card. **Extended again 2026-07-30**: completed tasks now collapse into a "COMPLETED (`N`)" section at the bottom, closed by default, at the user's request — the flat list got more crowded the longer an account was used. **Deliberately a collapse, not a delete**: a completed task backs `completedTaskCount()` (ONB-01's third quest, and PRO-04/PRO-09's still-To-Do lifetime stats and achievement criteria), so discarding it by default would silently break history other features already depend on. TASK-04's delete-from-the-edit-screen still exists unchanged for anyone who wants a task actually gone, not just out of the way. When there's nothing open but history exists, `EmptyTasksState` ("All clear!") shows above the collapsed section rather than an empty screen. Verified live: with 12 open + 2 completed (one itself carrying 2 completed subtasks), the section renders collapsed, expands on tap to show all of them correctly nested, and the chevron flips |
| TASK-02 | Create task form — fields: title (required), due date (optional), complexity tier (required, 5-option select) | Frontend | Done — `src/components/tasks/create-task-sheet.tsx`, a native-`<dialog>` bottom sheet (same platform-focus-trap/Escape pattern as `Modal`/SHR-03, bottom-anchored instead of centred). Reachable from `BottomNav`'s "+" (SHR-01) and from the empty state's "+ New task" (SHR-04, via TASK-01). **No `POST /api/tasks` yet** — TASK-08 is a separate, unbuilt ticket, by explicit scope decision. Real client-side validation runs (title required, tier required, errors clear as you fix them — matching `register-form.tsx`'s convention), but a fully valid submit stops on a visible "not connected yet (TASK-08)" notice rather than pretending to save. Tier selector is `src/components/tasks/tier-select.tsx`, built standalone for TASK-03 to reuse. Verified live: empty-submit shows both required-field errors; valid submit shows the stub notice and leaves the DB untouched (checked directly); Escape verified via the dialog's native `close()` event (same mechanism as `Modal`) since this environment's synthetic Escape keypress doesn't trigger native dialog cancel; scrim-tap-to-close verified working. **Extended 2026-07-30** at the user's request: an optional SUBTASKS section, matching `SubtaskList`'s row styling. Titles are held as local state — the task has no id to attach a real subtask to until `POST /api/tasks` returns one — and each is sent through SUB-04's existing `/api/tasks/[id]/subtasks` right after, in parallel (`Promise.allSettled`, so one subtask failing doesn't block the task itself from saving or read as the whole submit having failed). A title still sitting in the draft field when "Add task" is pressed is included rather than dropped. Verified live: created a Small task with two subtasks, one added via the row's own "+" and one left uncommitted in the draft field at submit time — both landed on the real task in the DB, correctly splitting its 15-coin reward 7/7 (`calculateReward`'s existing per-subtask share) on the edit screen. **Updated 2026-08-12, at the user's request** (reported live on an iPhone): focusing this sheet's title field zoomed the whole app in and left the layout overflowing — the classic iOS Safari/Chrome bug where a focused input under 16px font-size triggers an automatic zoom with no way to opt out short of `user-scalable=no`, which the project's accessibility bar (NFR-GEN-1) rules out. The design draws this field at 14px and the subtask draft field at 13px; both bumped to 16px, the minimum that keeps iOS from zooming at all. **Same root cause affects every other text input in the app**, all fixed in this pass: `EditTaskForm`'s title (TASK-03), `SubtaskList`'s add-subtask field (SUB-02), the shared `TextField` primitive behind login/register (AUTH-01/02), the username field (AUTH-07), and the store's search box (STOR-02) — see each row for its own note. Verified live at 375px: the New Task sheet's title and subtask fields both compute to `font-size: 16px` and the sheet no longer has anything to zoom into on focus. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: the subtask section's "+ Add" trigger dropped its literal "+" prefix, now just "Add" — see AUTH-07's row for the full icon-consistency note |
| TASK-03 | Task detail / edit screen — pre-filled title, due date, complexity chips, subtask list, reward footer, and delete. The designs use a full screen here, not a modal | Frontend | Done — `src/app/tasks/[id]/page.tsx` + `src/components/tasks/edit-task-form.tsx`. `taskForUser()` scopes the lookup to the signed-in owner; a task id that's someone else's reads identically to one that doesn't exist (redirects to `/tasks`), rather than confirming the id was valid. Reachable — `TaskRow` (TASK-01) now links each row to its edit screen instead of doing nothing. **Subtasks omitted entirely** by explicit scope decision — SUB-01/02/03 are separate, unbuilt tickets, and a partial read-only list was judged worse than none. **Save changes** validates for real (title required) but stops on a visible "not connected yet (TASK-09)" notice rather than persisting — same stub pattern as TASK-02, no `PATCH /api/tasks/[id]` yet. **Delete** wires the *real* SHR-03 confirm modal (its first non-onboarding consumer), but the confirm itself stops on a "not connected yet (TASK-10)" notice — no `DELETE /api/tasks/[id]` yet. Verified live end-to-end: pre-fill correct for all 3 fields, reward footer tracks the selected tier live, delete confirm → stub notice → task still in DB (checked directly), save with edited title → stub notice → title unchanged in DB (checked directly). **Updated 2026-07-29:** the footer's "Reward on completion · N coins · N XP" line was **removed on the user's call** — it looked wrong in place, and the figure already appears on the task row and under each subtask, so it was the third copy. The tier chips still drive `taskTier()`, which `SubtaskList` uses for the per-subtask share, so nothing downstream changed. Fixed at the same time: **"Save changes" was the wrong width** — it was passed `fullWidth={false}` (which emits `flex-none`) *and* `className="flex-1"`, and `cn` is a plain join with no conflict resolution, so the two raced on stylesheet order. The button is now stretched by a `flex-1` wrapper instead. Measured after the fix at 375px: delete 48px at x=16, save 287px from x=72 to x=359, both 46px tall — the row fills the width exactly. **Found and fixed 2026-07-31**: the `<Link>` this ticket added to `TaskRow` (making the row reachable) had no explicit text color class, so it inherited `globals.css`'s base `a { color: var(--color-terracotta) }` rule — every task/subtask title on `/tasks` rendered terracotta instead of ink, undetected by this ticket's or TASK-01's live verification since neither checked the row's computed text color. Fixed by adding `text-ink` to the Link's className; confirmed live (`rgb(46, 42, 38)`, the ink token) with a real account and task. **Updated 2026-08-12, UX/UI pass**: the title field's 14px text bumped to 16px — iOS Safari/Chrome zooms the page in on focus below that size. See TASK-02's row for the full note (same bug, found on that screen first, fixed everywhere the same day). `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: header padding standardized to the mock's `8px 18px 14px` (was flat `p-3`) — see INF-12's row for the full note |
| TASK-04 | Delete task action — confirmation prompt before deletion | Frontend | Done — built as part of TASK-03 (the trash-icon button + SHR-03's confirm modal on the edit screen), noticed and flipped here rather than left stale. Confirmed via `design_handoff` and Requirements.md that this is the only delete affordance anywhere in the designs — nothing else to add. The confirm step is real; the deletion itself is stubbed pending TASK-10, same as TASK-03's note |
| TASK-05 | Complete task action — checkbox or button; triggers reward animation and coin/XP update | Frontend | Done — **fully wired to TASK-11 for real** (2026-07-29, after Saskia's ECO branch merged). `src/components/tasks/task-row.tsx` + `task-list.tsx`. **Forward-only, by design**: the checkbox completes but does not un-complete — reversing a grant correctly means undoing a streak day, daily-cap counters and potentially a level, which neither TASK-05 nor TASK-11's ticket text asks for, so a completed row's checkbox is disabled rather than toggling back. **Tap targets stay split** (checkbox completes, rest of the row still links to TASK-03's edit screen) since the mock's single whole-row tap can't serve both. The reward pop shows the *real* granted amount from TASK-11's response (efficiency/streak/anti-spam/cap-adjusted), not the tier's base figure — `TaskList` owns the fetch, `TaskRow` is purely presentational about it. `router.refresh()` on success updates the header (coins/level/XP/streak) from the server. A level-up crossing goes straight to ECO-07's `useLevelUp().celebrate()`. Verified live end-to-end: completed a Medium task with no due date → granted exactly the base 35 coins/45 XP, level correctly jumped 1→4 (45 XP crosses 3 thresholds) with the celebration screen showing the right "climbed from Level 1" copy, header updated correctly (Lv4, 10/20 XP into level, 1 day streak); completed a second, differently-titled task same day → 5 coins/8 XP granted (no anti-spam reduction, different title), streak correctly stayed at 1 (`"already-counted"`, same calendar day); re-completing the first task returned 409 and did not double-grant (checked coins directly). **Updated 2026-08-12, at the user's request**: the checkbox no longer fires the complete request on tap — since there's still no un-complete (the user chose this over building reversal, given coins can already be spent elsewhere by the time anyone would want to undo it), an accidental tap used to be permanent with no recourse. It now opens the shared `Modal` (`src/components/tasks/task-list.tsx`, same pattern as `EditTaskForm`'s "Delete task"), naming the task, and only "Complete" calls the real endpoint — "Not yet" or a scrim tap backs out with nothing sent. Terracotta, not destructive red, since completing is the *good* outcome, just an unconfirmed one. Same gate applies to subtask rows (SUB-05) sharing this list's `TaskRow`. Verified live: created a Small task, "Not yet" left it incomplete with 0 coins granted; confirming via "Complete" ran the full pipeline unchanged (15 coins, 20 XP, streak day 1, quest advanced, row moved to Completed). `tsc` and `eslint` green |
| TASK-06 | Task dashboard stats bar — current level, XP progress bar, streak counter, coin balance | Frontend | Done — built by INF-12 (`PersistentHeader`'s greeting variant: level disc, XP bar, streak card, coin pill) and already in use on `/tasks` since TASK-01; noticed and flipped here rather than left stale. No new code — `Requirements.md` TASK-8/9 don't ask for anything beyond what's already rendered |
| TASK-07 | `GET /api/tasks` — list all tasks for the authenticated user | Backend | Done — `src/app/api/tasks/route.ts`. Not consumed by anything yet (TASK-01's dashboard reads `tasksForUser()` directly, no network hop needed) — this is the read half of the resource TASK-08..11 write to. Auth checked explicitly in the handler, same pattern as `api/user/username/route.ts`, on top of `proxy.ts`'s blanket `/api/*` check. Verified live: empty list for a fresh account, correct shape once a task exists, 401 with no session |
| TASK-08 | `POST /api/tasks` — create a new task | Backend | Done — `POST` handler in `src/app/api/tasks/route.ts`, `createTask()` in `src/lib/tasks.ts`, validated by the new `src/lib/validation/tasks.ts` (`createTaskSchema`, same messages the form already showed client-side — server re-validates rather than trusting the client). **TASK-02's create-task sheet wired to this for real** (your call) — no more stub notice; a successful create closes the sheet and calls `router.refresh()`, which also required fixing `TaskList` (TASK-01/05) to actually re-sync its local state from the refreshed `tasks` prop, since its `useState` was previously seeded once and never updated. Verified live: created a real task through the UI, confirmed it in the DB, confirmed it appears in the list without a manual reload, confirmed server-side validation rejects bad input (empty title + out-of-range tier) independent of the client |
| TASK-09 | `PATCH /api/tasks/[id]` — edit task title, due date, or complexity | Backend | Done — `src/app/api/tasks/[id]/route.ts`, `updateTask()` in `src/lib/tasks.ts` (`updateMany` scoped to `{ id, userId }` rather than `update`, so an id belonging to someone else fails as "0 rows" → 404, not a thrown error — same "can't tell the difference" reasoning as `taskForUser()`). Reuses TASK-08's `createTaskSchema` rather than a near-duplicate schema. **TASK-03's "Save changes" wired to this for real** (same pattern as TASK-08/TASK-02) — success now navigates back to `/tasks` instead of showing a stub notice; delete is still stubbed pending TASK-10. Verified live: edited a real task through the UI, confirmed both fields persisted in the DB, confirmed the updated task shows in the list post-navigation, confirmed server-side validation and 404-on-unowned-id independently of the client |
| TASK-10 | `DELETE /api/tasks/[id]` — delete a task and its subtasks | Backend | Done — `deleteTask()` in `src/lib/tasks.ts` (`deleteMany` scoped to `{ id, userId }`, same "0 rows → 404" shape as `updateTask()`). Subtasks need no explicit handling — `Subtask.task` is already `onDelete: Cascade` in the schema. **TASK-03's delete confirm wired to this for real** (same pattern as TASK-08/09) — confirming now actually deletes and returns to `/tasks`; the confirm button shows "Deleting…" while in flight, and a failure closes the dialog and shows the error in the footer rather than leaving it stuck behind an open modal. This closes out every stub TASK-01..05 left behind except TASK-05's completion toggle, which is blocked on TASK-11 + Saskia's ECO branch, not on Johan. Verified live: seeded a task with a subtask, deleted it through the real UI, confirmed both rows gone from the DB, confirmed 401 unauthenticated and 404 for a nonexistent/unowned id |
| TASK-11 | `POST /api/tasks/[id]/complete` — mark task complete; invoke economy service to calculate and apply reward | Backend | Done — `src/app/api/tasks/[id]/complete/route.ts`, plus `markTaskComplete()` in `src/lib/tasks.ts`. Runs the full ECO-01..05 pipeline in the order `economy.ts` itself documents: mark complete (atomic `completedAt: null` guard — this, not the pre-check, is what actually prevents a double-grant from two racing requests) → `recordStreakDay()` *before* pricing → `antiSpamCheck()` against the task's own pre-completion title/createdAt → `calculateReward()` priced without `earnedToday` → `grantEarnings()` applies the real, lock-read daily cap. **Forward-only, no un-complete** — reversing a grant means undoing a streak day, daily-cap counters and potentially a level, which isn't asked for by this ticket or TASK-05's; a completed task simply stays completed. Returns the granted reward, streak update and level-up event (if any) in one response so TASK-05 doesn't need a second round trip. Merged from Saskia's `38-eco-01-to-07` branch: the merge itself touched `src/lib/tasks.ts` (her title-normalisation work for ECO-02 combined with my TASK-07..10 read/write functions), `globals.css` (both sides' keyframes, purely additive) and `Features.md`/`.csv` (kept the richer `Done` rows over her placeholder `To Do` ones) — verified with `npm test` (74 tests, hers + mine), typecheck and lint clean post-merge before building anything new. Verified live end-to-end: completed a Medium/no-due-date task → exactly 35 coins/45 XP granted, level jumped 1→4 correctly with the right level-up copy, header updated via `router.refresh()`; completed a second, different-titled task same day → 5/8 granted with no reduction, streak correctly held at 1 (`"already-counted"`); re-completing the first task returned 409 with no double-grant (checked coins directly in the DB); 401 with no session. **Updated 2026-08-12, UX/UI pass**: still forward-only (this endpoint is unchanged), but the checkbox no longer calls it on tap — a confirm step went in front instead. See TASK-05's row for the full note |

### Economy Service (shared business logic)

| # | Feature | Type | Status |
|---|---------|------|--------|
| ECO-01 | Reward calculation service — base coins/XP from complexity tier, apply efficiency modifier (±% for early/late), apply streak bonus, apply anti-spam reduction, apply daily cap check | Backend | Done — `src/lib/rewards.ts`. Pure arithmetic; the two DB-backed inputs (last same-title completion, today's earnings) are parameters supplied by ECO-02/ECO-03 |
| ECO-02 | Anti-spam check — query last completion of same task title within 72 h; apply tiered reward reduction (50% / 25% / 10%) | Backend | Done — `antiSpamCheck()` / `reduceForRepeats()` in `src/lib/economy.ts`, over the new `src/lib/tasks.ts` (the module that owns `prisma.task`). Matched on title, not task id, so delete-and-recreate farming is caught. **Two deviations from NFR-TASK-1 as written, both recorded in Requirements.md**: matching is on a new normalised `Task.titleKey` column (migration `eco02_task_title_key`, backfilled) rather than on `title`, and duplicates that predate the last completion of their title are exempt up to 3 per day, so batch-planning a week of identical tasks is not graded as farming. Verified against the docker Postgres, not just the mock |
| ECO-03 | Daily cap enforcement — reject coin/XP earnings above 300 coins and 500 XP per calendar day | Backend | Done — `dailyAllowanceOf()` / `dailyAllowanceFor()` / `grantEarnings()` in `src/lib/economy.ts`. **Partial grant, not rejection**: at 290/300 coins a 150-coin task still banks the last 10 and reports the withheld 140, so the completion toast can explain the shortfall. Reset is lazy (counters from a past day read as zero) — no midnight sweep to keep alive on a $0 budget. The read is `SELECT … FOR UPDATE` in a transaction: verified that four simultaneous 100-coin grants against the 300 cap bank exactly 300, which a plain read-then-write does not |
| ECO-04 | Streak service — detect if today is a streak day; update streak counter; calculate and apply streak coin bonus (10% / 20% / 35%) | Backend | Done — `nextStreak()` (pure) and `recordStreakDay()` in `src/lib/economy.ts`; the bonus table itself is ECO-01's `streakBonusFor`. Safe to call on every completion — only the first of a calendar day moves the counter, and it must be called *before* pricing so the task that reaches day 3 earns the 10% itself (verified: 35 → 39 coins). Locked with `SELECT … FOR UPDATE` like ECO-03; verified that four simultaneous completions add exactly one day, and that a missed day breaks back to 1 |
| ECO-05 | Level-up service — compare cumulative XP against level threshold table; update level in `UserEconomy`; return level-up event if threshold crossed | Backend | Done — `levelUpBetween()` (pure) and `syncLevel()` (repair) in `src/lib/economy.ts`; the level column is now written by `grantEarnings()` in the *same* update as the XP, not a follow-up write, so the two can never be seen disagreeing. The event carries `levelsGained` as a list because the hockey-stick curve routinely crosses several thresholds on one completion — verified: one Epic from zero gains levels 2–6. Levels off *banked* XP, so a cap-trimmed grant cannot fire a threshold the participant never crossed. **Found a spec error doing this**: Requirements §3.6's first-session example claimed 48 XP reached Level 5; it reaches Level 4 (55 needed). Corrected there. Every `xp` write now goes through one `xpWrite()` helper that pairs the increment with the derived level, so a future write path cannot leave the column stale — the failure would otherwise be silent, since the header reads `xp` and only the store gate reads `level`. **Rebalanced 2026-07-30** at the user's request: 3 tasks in one sitting reached level 6, which read as overwhelming. `LEVEL_THRESHOLDS` levels 2–6 were raised (40/110/190/280/380, from 8/20/35/55/200); levels 7–10 (550/900/1400/2000) are untouched, since those were already correctly paced against Requirements §3.6's ~150 XP/day assumption and raising them would push level 10 past the study's two weeks. `economy_system.md` and Requirements §3.6 updated to match. Kept intact: level 6 stays under the 500 XP/day cap and level 7 stays over it, so the level-7 gate (second animal type, day-3–4 exposure schedule) still can't be reached on day one. 9 tests in `economy.test.ts` (`levelUpBetween`, `grantEarnings`, `syncLevel`, `xpWrite`, `buyXp`) hardcoded the old thresholds and were updated to the new crossings; `npm test` (74) and `tsc` green |
| ECO-06 | `POST /api/economy/buy-xp` — deduct 100 coins, add 40 XP, invoke level-up service | Backend | Done — `buyXp()` in `src/lib/economy.ts` behind `src/app/api/economy/buy-xp/route.ts`. Price is server-side only (never from the body) and the account comes from the session. Adds a `GET` returning `{cost, gain}` so PRO-06's card need not hard-code them. **The daily XP cap deliberately does not apply** — those coins were already capped when earned, and charging the ceiling twice would make a conversion after a busy day silently do nothing. Verified over real HTTP with a signed-in session: 401 unauthenticated, 409 with a shortfall body at 0 coins, three purchases walking level 1→5, and four simultaneous requests against a 100-coin balance yielding exactly one 200 and three 409s |
| ECO-07 | Level-up notification — UI toast or modal shown when the user crosses a level threshold | Frontend | Done — full-screen celebration per `design_handoff/ADDENDUM-levelup.md`, not a toast: `LevelUpScreen` + `LevelUpProvider` in `src/components/economy/`, mounted once in `AppShell` so any screen can raise it with `useLevelUp()`. Queued, so two crossings in quick succession don't overwrite each other, and one screen covers a multi-level climb rather than firing five. Adds `Button` `variant="on-brand"` / `size="hero"` (the landing screen's "Get started" is the same treatment) and `--color-brand-soft`. **Deviation:** with no reward tiles the two actions collapse to one "Continue" — Requirements §3 defines no level-up payout, so "Claim rewards" would promise something that doesn't exist. Confetti falls the full height and exits the bottom (one pass, ~2.4–3.6s staggered; a loop would owe WCAG 2.2.2 a pause control). Distance is a `--confetti-fall` custom property so it works for both shapes the screen takes — measured 941px travel in the 812px full-bleed and 758px in the 640px frame. Verified in the browser at 375×812 full-bleed and 400×640 framed, all states plus reward tiles, via `/style-guide#s08`; console clean (no hydration warning — the confetti is deterministic). **Escape-to-close is inherited from `<dialog>` but could not be verified**: the automation harness's synthetic Escape doesn't drive the UA close-watcher, confirmed with a bare native dialog as a control |

### Subtasks

| # | Feature | Type | Status |
|---|---------|------|--------|
| SUB-01 | Subtask list within a task card — expandable, shows each subtask with completion checkbox | Frontend | Done — `src/components/tasks/subtask-list.tsx`, added to `src/app/tasks/[id]/page.tsx`'s edit screen (TASK-03) under the COMPLEXITY block, matching the "Task detail / edit" mock's SUBTASKS block exactly (row style, coin figure, strikethrough on completed). `taskForUser()` in `src/lib/tasks.ts` now includes `subtasks` (ordered by id — cuid is k-sortable, so this reads as creation order without a separate `createdAt` column); its return type is `TaskWithSubtasks`. **Read-only, by design**: the mock has no expand/collapse affordance for this block — "expandable" is read as the list growing with `subtasks.length`, not an accordion. Checkboxes just reflect `completedAt` — completing one is SUB-03/05, still unbuilt. ("+ Add" was an inert label at the time this ticket shipped; SUB-02 made it functional the same day, see that row.) The coin figure per row is a client-side preview of the eventual proportional split (`parentCoins / subtasks.length`, floored, tracking the selected tier live) — not authoritative, since SUB-05 computes the real grant server-side. Verified live: seeded a Large task (75 coins) with 3 subtasks (1 done, 2 not) — rendered exactly as the mock (25 coins each, done row strikethrough + sage check), and a task with zero subtasks shows "No subtasks yet." rather than an empty block |
| SUB-02 | Add subtask inline input within a task card | Frontend | Done — folded into `src/components/tasks/subtask-list.tsx` (SUB-01's file) rather than a new file, since "+ Add" was already that component's affordance. Clicking it swaps the label for an inline title input + a terracotta add button, styled to the app's input spec; Enter or the button submits. Real validation runs (empty title blocked with an inline error). (At the time this ticket shipped, SUB-04 didn't exist yet and a valid submit stopped on a "not connected yet (SUB-04)" notice; SUB-04 wired it for real the same day — see that row.) **Found and fixed a real bug while building this**: the input was first wired inside its own `<form>`, nested inside `EditTaskForm`'s outer `<form>` (TASK-03's save form) — nested `<form>` is invalid HTML, and the browser routed the inner submit to the *outer* form instead, silently saving the task and discarding the typed subtask title with no error shown. Fixed by dropping the inner `<form>` for a plain `div` with hand-wired `onKeyDown`/`onClick` submission (noted inline in the component). Verified live: empty submit shows "Give the subtask a title."; a valid submit shows the stub notice, input keeps its text, and the DB gained no row (checked directly). **Updated 2026-08-12, UX/UI pass**: the add-subtask field's 12.5px text bumped to 16px — iOS Safari/Chrome zooms the page in on focus below that size. See TASK-02's row for the full note. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: "+ Add" dropped its literal "+" prefix, now just "Add" — see AUTH-07's row for the full icon-consistency note |
| SUB-03 | Complete subtask action — triggers proportional reward | Frontend | Done — folded into `src/components/tasks/subtask-list.tsx` (SUB-01/02's file). Each incomplete row's checkbox is now a real button; a done row's is disabled rather than toggling back — same forward-only rule TASK-05/11 use, for the same reason (undoing a grant correctly means unwinding a streak day/cap/level, out of scope here). (At the time this ticket shipped, SUB-05 didn't exist yet and a click just surfaced a "not connected yet (SUB-05)" notice with no fetch attempted; SUB-05 wired it for real the same day, including a reward pop and level-up handling — see that row.) Verified live at the time: clicking an incomplete row's checkbox showed the notice and left the DB untouched; a done row's checkbox did nothing (disabled) |
| SUB-04 | `POST /api/tasks/[id]/subtasks` — add a subtask to a task | Backend | Done — `src/app/api/tasks/[id]/subtasks/route.ts`, `createSubtask()` in `src/lib/tasks.ts`, `createSubtaskSchema` in `src/lib/validation/tasks.ts`. 404s (not 403) when the task id belongs to someone else or doesn't exist, same "can't confirm the id was ever real" reasoning as `/api/tasks/[id]`. `Subtask` has no `userId` of its own, so ownership is checked against the parent `Task` before the insert rather than folded into one scoped write the way `updateTask()`/`deleteTask()` manage. No reward math here — nothing is earned until SUB-05's completion endpoint exists. **Wired SUB-02's stub for real the same day**, matching the TASK-02→TASK-08 convention: `subtask-list.tsx`'s add form now `POST`s here and closes on success (`router.refresh()` picks up the new row via `taskForUser()`) instead of showing the "not connected yet (SUB-04)" notice. SUB-01's coin-share preview and SUB-03's complete-stub are untouched. Verified live: added a real subtask, confirmed the row in the DB directly and that it rendered immediately after refresh; empty-title submit still blocked client-side; unauthenticated `POST` returns 401 |
| SUB-05 | `POST /api/tasks/[id]/subtasks/[subId]/complete` — mark subtask complete; calculate proportional share of parent task's reward; auto-complete parent when all subtasks done | Backend | Done — `src/app/api/tasks/[id]/subtasks/[subId]/complete/route.ts`, `markSubtaskComplete()` in `src/lib/tasks.ts`. Runs the same pipeline TASK-11 does — `recordStreakDay()` before pricing, `calculateReward()` (with `share: 1 / task.subtasks.length`, the field ECO-01 already built for this — Requirements §3.5's worked example, 35-coin/45-XP task ÷ 3, was already a passing unit test before this ticket existed), `grantEarnings()` against the real daily cap. **No anti-spam reduction** — NFR-TASK-1/ECO-02 grades a *task's* repeat history by title, and `Subtask` has neither a title key nor completion history to grade against; out of scope by clean requirements reading, not an oversight. **Whether a subtask completion should advance the streak was genuinely ambiguous** (`economy_system.md` defines a streak day as "at least 1 **task** completed", and `Subtask` is a separate model) — asked the user directly rather than guess; decided it should, full pipeline, same as a full task. **Double-grant guard**: rejects with 409 if the *parent task* is already complete before touching the subtask — a task completed directly (TASK-11, which doesn't check for open subtasks) has already paid out its full reward, so a subtask grant on top of that would double-pay it. SUB-4's auto-complete calls `markTaskComplete()` directly (bypassing the reward pipeline) once every subtask is done, so the parent gets no reward of its own. **Wired SUB-03's stub for real the same day**: `subtask-list.tsx`'s checkbox now `POST`s here, shows the real granted amount as a transient reward pop (`TaskRow`'s pattern) instead of the row's coin-share preview, routes level-ups through `useLevelUp().celebrate()`, and `router.refresh()`s to pick up the row's new `completedAt` and — when SUB-4 fires — the parent's. Verified live end-to-end against a scratch Postgres: completed a Medium task's (35c/45xp) 3 subtasks one at a time — each granted 12c/15xp (36/45 total, the 1-coin overage from independent per-subtask rounding is `calculateReward`'s existing, tested behaviour, not new), the third auto-completed the parent with no extra grant; a second task (Small, 2 subtasks, 1 pre-seeded done) granted 8c/10xp on its second subtask and correctly crossed a level threshold (level-up event returned); confirmed via raw `fetch` from an authenticated session that re-completing an already-done subtask 409s, and that completing a subtask of an already-complete task 409s before granting anything; unauthenticated `POST` returns 401. `npm test` (74 tests) and `tsc` stay green. **Updated 2026-08-12, UX/UI pass**: still forward-only (this endpoint is unchanged), but a subtask row's checkbox now opens the same confirm dialog as a task row's before calling it. See TASK-05's row for the full note |

### Onboarding

| # | Feature | Type | Status |
|---|---------|------|--------|
| ONB-01 | Onboarding checklist widget — displays three goals ("Add a task", "Complete a task", "Buy a pet") with live completion status | Frontend | Done — `src/components/onboarding/quest-checklist.tsx` on `/onboarding`, replacing that page's placeholder, plus `src/components/onboarding/next-quest-card.tsx` on `/tasks`. Counts come from a new `src/lib/onboarding.ts` (`onboardingStatus()`), server-rendered so they're right on first paint; the two task counts are `taskCount()`/`completedTaskCount()` in `src/lib/tasks.ts`, since nothing outside that module touches `prisma.task`. **The goals were simplified on 2026-07-29** at the user's request, from "Complete 3 tasks / Buy 1 animal / Feed 3 animals today" — Requirements ONB-1 was rewritten to match. The old set asked for seven actions across three subsystems before it cleared; the new one is three one-shot steps that walk a participant into the economy. Two consequences worth noting: the old feed goal's caveat (it could only read `Pet.lastInteractedAt`, which PET-07's *petting* also writes, so it would have ticked without anyone feeding anything) is **gone with the goal**, and with every target now 1 the per-goal progress bars were replaced by `TaskRow`'s 22px check circle — a bar that can only read 0% or 100% is a worse answer than a checkbox to the same question. **"Buy a pet" counts `Transaction`s with an `ANIMALS` store item, not `Pet` rows** — the quest is about the purchase, and counting pets would make it depend on PET-11, which hasn't shipped; it is also **not yet reachable in the app**, since the store (STOR-01+) is still To Do. **Koala kit was repriced 200 → 5 coins** (`prisma/seed.ts`) so ONB-1's new "one sitting" clause holds: the smallest completion in the economy is a Trivial task at 5 coins, and at 200 no first completion could reach it. That is below `claude-memory/economy_system.md`'s 30–80 band for the level-1 gate — the user's call; the level-7 and level-10 animals are untouched, so the gate table still governs everything the 14-day earnings model was calibrated against. The dashboard card exists because "Let's go" otherwise left the quests unreachable; it shows one quest at a time (the header already carries coins, level, XP and streak, and a three-row block under it pushes the task list below the fold on a 640px frame), links through to `/onboarding`, sits *above* `TaskList` so it survives the empty state, and returns null once all three are met. Also added: `buttonClasses()` split out of `Button` so "Let's go" can be a `<Link>` without nesting an anchor in a button, and `ProgressBar` gained a `goal` tone and `sm` size (still used by nothing after the check-circle switch — harmless, and the tone is the obvious one for a future multi-step quest). The ring's figure is computed rather than the mock's unexplained 17%. **Verified live end-to-end against the Docker Postgres**: from a zeroed account (0 coins, no tasks) the card read "QUEST 1 OF 3 · Add a task"; adding a Trivial task ticked quest 1 and advanced the card to quest 2; completing it granted exactly 5 coins (confirmed in `UserEconomy`) and advanced to "QUEST 3 OF 3 · Buy a pet" — and 5 coins ≥ the koala's 5, so the sequence clears as ONB-1 now requires. Also checked: the checklist at 1/3 renders 33% with one check circle filled; the card renders above `EmptyTasksState` with zero tasks, disappears with no leftover gap once all three are met, and taps through to `/onboarding`; desktop renders as a centred 400px card. `npm test` (74), `tsc` and `eslint` green |
| ONB-02 | Goal completion celebration — visual feedback when each onboarding goal is met | Frontend | Done — `src/components/tasks/task-list.tsx`. No frame in `design_handoff` draws this (only the level-up celebration, ADDENDUM-levelup.md, is drawn), so the treatment is new: a small sage banner ("Quest complete: `label`!", `PartyPopper` icon, `motion-safe:animate-medallion-pop` reusing ECO-07's existing keyframe) above the task list, auto-dismissing after 3.2s. **The three goals are each satisfied on a different screen** — creating a task (`CreateTaskSheet`), completing one (this component), eventually buying a pet (STOR, still To Do) — so there's no single action handler that "witnesses" a goal being met the way `TaskRow`'s reward pop witnesses a completion. What every path shares is `router.refresh()`, which re-runs `/tasks`'s server component and hands `TaskList` a fresh `onboarding` prop (now passed down from `src/app/tasks/page.tsx` alongside `NextQuestCard`'s); the component diffs that prop against what it was on the previous render (same "adjust state during render" pattern already used for the `tasks`/`syncedFrom` pair) and celebrates whichever goal(s) flipped from incomplete to complete. This is also why "Add a task" fires correctly even though the request that satisfies it happens inside a different component — that component's own `router.refresh()` is what re-renders this one with new props. **Same limitation as `LevelUpProvider`'s level-ups**: only fires for a goal met *during the current session*; a goal already met when the page first loads is correctly silent rather than replaying stale news, since there is no "missed celebration" queue. Verified live: from a zeroed account, creating the first task flipped "Add a task" and showed "Quest complete: Add a task!", with `NextQuestCard` simultaneously advancing to "Complete a task" — confirming both consumers read the same fresh `onboarding` value from the one `router.refresh()`. `npm test` (74), `tsc`, `eslint` green |
| ONB-03 | `GET /api/onboarding` — return current completion status of the three onboarding goals for the authenticated user | Backend | Done — `src/app/api/onboarding/route.ts`, a thin wrapper over ONB-01's `onboardingStatus()`, same shape/auth convention as `/api/tasks`'s `GET` (`{ error }` + 401 when signed out, `{ onboarding }` on success). Neither `/onboarding`'s checklist nor `/tasks`'s `NextQuestCard` call it — both are server components that already read `onboardingStatus()` directly, same "no network hop needed" reasoning `/api/tasks`'s own GET documents; this route exists for whichever client-side consumer needs to re-fetch without a full navigation, and there isn't one yet. `onboardingStatus()` itself was already exercised end-to-end by ONB-01/ONB-02's live testing, so this row only needed to verify the route wiring: `fetch('/api/onboarding')` from an authenticated session returned 200 with the correct three-goal shape (`{goals, percent, completed, complete}`), and a stripped-cookie request returned 401 with `{error: "Not signed in."}`. `npm test` (74), `tsc`, `eslint` green |
| ONB-04 | Username step — step dots, live availability check, suggestion chips, and a skip that assigns an auto-generated handle | Frontend | Done — found during the dev-merge audit (2026-07-29) that this row had shipped with **no step dots rendered** despite `src/components/ui/step-dots.tsx` (`StepDots`) existing for exactly this screen and the handoff addendum specifying "3 pips, first active" — a built-but-never-wired component, and this row had no verification note at all, unlike every sibling row. `StepDots` was wired up then (`total={3} current={1}`), and **removed again on 2026-07-29 at the user's request** — they judged the indicator to serve no function on a single-screen step, which it doesn't: nothing in the flow advances it, and steps 2 and 3 were notional. `src/components/ui/step-dots.tsx` is left in place but is now unused by anything. The remaining scope — live availability check, suggestion chips, skip-to-auto-handle — was already real and correctly wired (`username-step.tsx`, `src/components/user/username-field.tsx`). Verified live after the removal: `/onboarding/username` renders avatar → heading → field → suggestions with no pip row; `tsc`/`eslint` clean |
| ONB-05 | `POST /api/user/username` (availability) and `PUT` (claim) — session-scoped, enforces uniqueness | Backend | Done — Prisma-backed; uniqueness enforced by the `User.username` index |

---

## Module 3 — Store

### Core Store

| # | Feature | Type | Status |
|---|---------|------|--------|
| STOR-01 | Store listing page — grid of item cards showing name, image, coin price, level requirement, and availability | Frontend | Done — `src/app/store/page.tsx` + `src/components/store/store-item-card.tsx`, replacing the `ComingSoon` placeholder `BottomNav` had pointed at (now-dead `coming-soon.tsx` deleted). Reads `storeItemsForUser()` (STOR-10) directly, same server-component pattern `TasksPage`/`ZooPage` use. **Locked items show only a plain "Lvl N" text label in place of the buy button** — the mock's actual desaturated/lock-icon "locked appearance" is STOR-04's own separate ticket, confirmed with the user rather than guessed, so it isn't built here. **Header, search bar, category chips, and the per-card "+" button all render matching the mock exactly but are inert** (no filtering, no cart, no history nav) — STOR-02/03/05/09 are separate, unbuilt tickets; same "render the control, wire it up later" pattern TASK-01 used for its own then-unbuilt "+ New task" pill. `PersistentHeader`'s title variant (STOR-08's coin balance) was already shipped by INF-12 — done by reuse, same relationship TASK-06 had. Category subtitle text ("Food"/"Accessory"/"Decoration"/"Animal") is a plain per-category label, not the mock's specific per-item copy ("Basic food", "2nd animal") — that copy isn't backed by any schema field, and inventing it risked fabricating data. Animal items render real SVG artwork (`next/image`, same as `pets.ts`'s cards); food/accessories/decorations render `lucide-react/dynamic`'s `DynamicIcon` off `StoreItem.imageUrl`, same cast-with-comment pattern `feed-sheet.tsx` established. Verified live end-to-end: registered a real account, bumped it to Level 5 with 500 coins directly in Postgres, confirmed all 7 seeded items render in the correct order with correct name/category/price, Koala kit (Lvl 1) through Cosy den (Lvl 5) show the "+" button, Fox kit (Lvl 7) and Penguin kit (Lvl 10) correctly show "Lvl 7"/"Lvl 10" instead; header shows "Store" + 500 coins; bottom nav's Store tab shows `aria-current="page"`; zero console errors; all three lucide icon chunks (wheat/shirt/package/house) and all three animal SVGs (200 OK) resolved correctly. Test account deleted after. `npm test` (99), `tsc` and `eslint` green |
| STOR-02 | Item search bar — filters visible items by name in real time | Frontend | Done — `src/components/store/store-browser.tsx`, a new `"use client"` component `src/app/store/page.tsx` now renders instead of the inert search markup STOR-01 left in place. Client-side filter over the one server-fetched `items` array (`useMemo`, case-insensitive substring match on `name` only, per the ticket's own wording — not description/category, which is STOR-03's). Category chips moved into this same component (still inert, STOR-03 not built) so the whole toolbar lives in one place. Added a small "No items match…" empty state — not specified anywhere, but an empty grid with zero feedback read as broken; kept minimal. **Found and fixed a real build bug while wiring this up**: `StoreItemCard` (STOR-01) imported the *runtime* `StoreItemCategory` enum from `@/generated/prisma/client` for its category `Record` lookups; once it became reachable from this client component, that pulled Prisma's Node-only runtime into the browser bundle and broke compilation entirely ("chunking context does not support external modules (request: node:module)"). Fixed by importing `StoreItemCategory` as a type only and switching every runtime reference to the plain string literals ("FOOD"/"ANIMALS"/etc.) — same values, no behaviour change, verified by re-testing STOR-01's own scenarios afterward. Verified live: registered a real account (had to work around the register page not submitting via the browser-automation tool in this session — created the account directly through `createUser()` instead, then logged in normally, which worked immediately), bumped to Level 5, confirmed all 7 items show by default; typing "kit" filtered to exactly Koala/Fox/Penguin kit in real time; typing "SUNFLOWER" (mixed case) correctly matched "Sunflower seeds"; a no-match query showed the new empty state; zero console errors throughout. Test account deleted after. `npm test` (99), `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: the category-chip row's own `overflow-x-auto` no longer draws a native scrollbar under it — new `.no-scrollbar` utility (`src/app/globals.css`, `scrollbar-width: none` + `::-webkit-scrollbar{display:none}`) on the chip row itself; same treatment applied to PRO-18's achievements-screen tabs below, the only other row with this exact pattern. Content still scrolls (touch/trackpad/keyboard) — only the bar is hidden. Verified live at 340px: confirmed no bar renders under either row while "Decorations"/"Items" still scroll into view. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: the search box's 13px input text bumped to 16px — iOS Safari/Chrome zooms the page in on focus below that size. See TASK-02's row for the full note. `tsc` and `eslint` green |
| STOR-03 | Category filter — filters visible items by category (food, accessories, animals, decorations) | Frontend | Done — `src/components/store/store-browser.tsx`, wiring up the category chips STOR-01/02 left inert. Chips are now real `role="radio"` buttons (`radiogroup` with `aria-label="Category"`, `aria-checked` on the active one), values are `StoreItemCategory`'s plain string literals per the same type-only-import rule STOR-02's fix established (no runtime Prisma import in this `"use client"` file). Combines with STOR-02's search as AND, not OR — a query narrows within the selected category rather than replacing it — and the empty-state message adapts to name both the query and the category when both are active ("No items match "collar" in Animals."). **Two real bugs found and fixed after this row first shipped, both confirmed live with the user, not just assumed**:

1. **Hydration mismatch** — `coin.tsx`'s `CoinPill`, `app-header.tsx`'s XP display, and this row's own price used `toLocaleString()` with no explicit locale, which formats large numbers differently depending on the server process's locale vs. the browser's (observed directly: the same Penguin kit price rendered as "1,200" in one load and "1 200" in another). Fixed by pinning every call site to `.toLocaleString("en-US")`.
2. **Chip row collapsing to 0 height in the user's real browser** — the category row is a flex child of `main` (`flex-col`, `overflow-y-auto`), and per the flexbox spec, giving a flex item any non-`visible` overflow (this row's own `overflow-x-auto`, needed for long category lists to scroll sideways) makes its *automatic minimum size* resolve to 0 instead of its content size. Nothing else on the page has that overflow + flex-child combination, so this was the one element the browser was willing to shrink to nothing when `main` was short on vertical space — invisible and unclickable, matching exactly what the user reported and screenshotted. Fixed by adding `flex-none` to the row. This also explains an earlier false lead: my own live-testing session had seen the chip buttons not respond to synthetic clicks and I'd attributed that to a browser-automation quirk — it was actually this same bug (a zero-area element can't be hit-tested), confirmed once the fix made a real click register correctly on the first try.

Re-verified everything live after both fixes: chip row now measures a real height (~28px) instead of 0; clicking "Animals" narrows the grid to exactly the 3 seeded animal items; clicking "All" resets it; searching "fox" while Animals stays selected narrows to just Fox kit; searching "collar" (an Accessories-only item) while Animals is selected correctly shows the empty state rather than Red collar; a 2,000-coin/level-10 account (crossing the exact thousands threshold that triggers the locale bug) shows no hydration error and consistent "2,000" formatting. Zero console errors throughout. `npm test` (99), `tsc` and `eslint` green |
| STOR-04 | Item card — level-gated state (locked appearance + "Requires Level X" label for items above user level) | Frontend | Done — `src/components/store/store-item-card.tsx`, replacing STOR-01's plain "Lvl N" text label with the mock's full locked treatment: desaturated card fill, desaturated icon well with a `lucide-react` `Lock` icon (a real icon rather than the mock's hand-drawn CSS shape — same call already made for the unlocked wells), muted name text, and an "Unlocks at Lvl N" pill in place of the price/buy row. `#F2EEE7`/`#E9E3D9` have no `@theme` token (nothing else uses this muted neutral) so they're arbitrary values, same precedent as `zoo-gallery-card.tsx`'s own raw-hex accent. Verified live: unlocked cards (Koala kit…Cosy den) still show `#FBF6EF` fill and the price/`+` row unchanged; locked cards (Fox kit, Penguin kit) show exactly `#F2EEE7` card fill, `#E9E3D9` well fill with a real lock SVG present, `#B8AFA4` (ink-disabled) name text, and "Unlocks at Lvl 7"/"Unlocks at Lvl 10" — all measured via computed styles, not eyeballed. Zero console errors. `npm test` (99), `tsc` and `eslint` green. **Found and fixed after STOR-06 shipped**: the well (shared with STOR-01) rendered as a fixed 54×54 square at the card's left edge instead of spanning the card's full inner width at a fixed 54px height with the icon centred *within that width*, per the mock — read as the icon stuck to the left, and the animal well's fill visibly narrower than the template. `ItemWell` (`item-visual.tsx`) gained a `fullWidth` option for this call site; the cart row (STOR-06) is the one place the mock genuinely draws a fixed square, so that call site is unaffected. Verified live: well width now matches the card's full inner width (148.5px in a 172px card), the icon's centre X matches the well's centre X to a fraction of a pixel for both an animal and a goods card, and the cart row's 44×44 square is unchanged |
| STOR-05 | Add to cart button on item card | Frontend | Done — `src/components/store/store-item-card.tsx`, wiring the "+" to STOR-12's real `POST /api/store/cart`. Now a client component (`"use client"`) with a small `idle → pending → added/error → idle` state machine: the button itself swaps colour/icon (terracotta "+" → sage check on success, urgency-red × on failure, auto-reverting after 1.2s via a tracked timer so a rapid second click's own feedback window can't be cut short by the first click's stale timeout), and a paired `sr-only` `role="status"` live region announces the outcome for screen readers, since the icon swap alone is silent to them. No design spec existed for this micro-interaction (the mock has no feedback state for add-to-cart), so this was designed from scratch rather than guessed from an ambiguous reference — kept deliberately minimal. No `router.refresh()` after success — nothing on `/store` reads cart state yet (STOR-06's cart panel is a separate, unbuilt screen), so there's nothing on this page a refresh would change. Only ever rendered for unlocked items (STOR-04's locked cards show the "Unlocks at Lvl N" pill in this slot instead), so the 403 the route can return for a locked item is a state this button can never actually trigger. Verified live against the real Docker Postgres: clicking "Add Sunflower seeds to cart" created a real `CartItem` row (confirmed by direct query, not just UI); a second click on the same item incremented it to quantity 2 rather than duplicating the row (STOR-12's own increment behaviour, exercised end-to-end from the UI for the first time); caught the transient "added" state mid-flight showing `bg-sage` on the button and "Added Treat box to cart" in the live region at the correct moment; zero console errors across three separate item clicks. `npm test` (99), `tsc` and `eslint` green |
| STOR-06 | Cart panel — lists cart items, quantities, subtotal; supports quantity edit and item removal | Frontend | Done — `src/app/store/cart/page.tsx` + `src/components/store/cart-panel.tsx`, per the mock's "Cart / checkout" frame. Real quantity edit/removal against STOR-14's `PATCH`/STOR-15's `DELETE`, local state updated from each mutation's own response (no `router.refresh()`). The "−" button removes the line once quantity would drop to 0 rather than a separate trash icon. Empty-cart state matches the mock (`ShoppingBag` icon, "Cart's empty", "Go to store"). **Cart entry point was a real design gap** (the handoff never shows how a user reaches this screen) resolved by explicit user direction: `/store`'s header icon swapped from history to a cart icon (item-count badge), history relocated to this screen's own header. Factored `CATEGORY_LABEL`/the icon well out of `StoreItemCard` into new `src/components/store/item-visual.tsx` (`ItemWell`) so cart rows and the store grid share one mapping. The "Check out" footer button is inert — STOR-07 owns wiring it. Verified live against the real Docker Postgres: added 2 items, confirmed correct quantities/subtotal (105); incrementing recalculated live (145) with no reload; removing deleted the row live, confirmed gone from the database directly; decrementing 2→1→0 correctly stepped down before removing, then the empty state rendered. **Three issues found by the user after this row first shipped, all fixed and re-verified**: (1) cart badge didn't update live — the badge (header) and add-to-cart buttons (grid) are siblings with no prop path between them; fixed with new `src/components/store/cart-count-context.tsx` (`CartCountProvider`/`useCartCount()`) wrapping both, `StoreItemCard`'s success path calls `increment()`, badge itself moved to new `cart-link.tsx`; verified live watching the badge go empty → "1" → "2" with no navigation. (2) Empty-cart "Go to store" button had no horizontal padding — `buttonClasses({ fullWidth: false })` bakes in none (same gap `not-found.tsx`'s own button had already hit); fixed with explicit `px-5` (mock: `padding:0 20px`), verified via computed style at exactly 20px both sides. (3) The badge itself rendered as an oval with off-centre text, not a circle — `min-w-[16px]` with no fixed height and horizontal-only padding meant its height came from the 9px text's line box, not 16px; fixed with `h-4 min-w-4` (fixed height, growable width) plus `leading-none`; verified live at exactly 16×16px with `rounded-full`'s computed radius. Zero console errors throughout, including tracking down what first looked like a data bug (two unlocked items showing as locked) back to stale client-side state in one heavily-reused browser tab, not an app bug. `npm test` (99), `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: "Go see your zoo" on the purchase-complete screen no longer gates on `adoptedCount > 0` — it now shows for every purchase, not just one that included an animal, since food and accessories are spent in the zoo too. `Confirmation.adoptedCount` (state that had no other reader) was removed along with the check. Verified live: bought only a food item (Hay, no animal in the cart) and confirmed both "Go see your zoo" and "Back to store" render on the confirmation screen. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |
| STOR-07 | Checkout flow — confirms purchase, deducts coins, shows confirmation | Frontend | Done — `src/components/store/cart-panel.tsx`, wiring the "Check out" button STOR-06 left inert to the real STOR-16 `POST /api/store/checkout`. On success, replaces the cart list with a confirmation view (design has no spec for this state — designed from scratch, matching the "Cart's empty" state's own visual language for consistency) showing what was bought, coins spent, and the new balance; a "Go see your zoo" link appears when `pets.length > 0` (an animal was among the purchases). On failure (chiefly `insufficient-coins`, since STOR-12 already blocks locked items and an empty cart hides the button entirely) shows the server's own error message inline above the button without clearing the cart, so the user can adjust and retry. Verified against the real Docker Postgres: attempting checkout on a 280-coin cart with 100 coins showed "Not enough coins to check out." and left the cart untouched; a 45-coin mixed cart (Koala kit + Sunflower seeds) against 100 coins produced the confirmation screen with "Koala kit, Sunflower seeds" / "45 spent · 55 left" and the zoo link, and direct DB queries confirmed coins at 55, cart empty, a real `Pet` row for Koala kit at 100/0, and both `Transaction` rows — not just the UI claiming success. Also confirmed the store header's cart badge (STOR-06) correctly reset to no-count after the cart emptied. Zero console errors throughout. `npm test` (99), `tsc` and `eslint` green |
| STOR-08 | Coin balance display in store header (persistent) | Frontend | Done — by reuse, no new code. `PersistentHeader`'s title variant (INF-12) already renders the coin pill on every titled screen, and `/store/page.tsx` has used it (`<PersistentHeader title="Store" .../>`) since STOR-01 shipped — same "done by reuse" relationship TASK-06 had to INF-12's greeting variant. Verified live rather than just flipped on the strength of that reasoning: a fresh account set to a distinctive 777-coin balance showed exactly "777" in the store header on page load, with the pill's own `role="img"` accessible label reading "777 coins"; zero console errors |
| STOR-09 | Purchase history / transaction log page — lists past purchases with item name, coins spent, and date | Frontend | Done — `src/app/store/history/page.tsx`, per the mock's "Purchase history" frame, reading STOR-17's `transactionsForUser()` directly (pure server component, no client boundary at all — nothing here is interactive, so there's no hydration risk even with locale-sensitive date formatting, though it's pinned to `"en-US"` anyway for consistency). Entries grouped by day (Today/Yesterday/`Mon D` for anything older), footer shows a rolling 7-day "Spent this week" total. `/store/cart`'s history icon (added inert under STOR-06) now links here for real. **Two deliberate deviations from the mock**, both because of what `Transaction` actually stores (STOR-16): no "Bought 40 XP" row — ECO-06's coin→XP purchases only touch `UserEconomy`, never write a `Transaction`, so showing one would invent data that was never recorded; and no `×N` quantity suffix on a line — `Transaction` stores one row per cart line with `coinSpent` already totalled, not a quantity column, so a suffix could only be a derived guess (`coinSpent / storeItem.coinPrice`) that reads wrong the moment a price changes (this app has already repriced an item once, per `prisma/seed.ts`'s own note on the Koala kit) — not worth the risk for a cosmetic label. New empty state ("No purchases yet") designed from scratch, no mock spec for it either. Verified live against the real Docker Postgres: seeded 4 transactions across today/yesterday/10-days-ago, confirmed correct grouping ("Today", "Yesterday", "Jul 22"), correct per-row timestamps (time-only for today, "Jul 20 · HH:MM" for older), correct negative amounts in `text-terracotta` (not the reserved `urgency` token — confirmed via computed style, `rgb(226, 122, 84)`), and "Spent this week" correctly totalling the 3 purchases inside 7 days (425) while excluding the 10-day-old one; confirmed 38×38 fixed icon wells with correct per-category tint; confirmed the empty state on a fresh zero-transaction account; confirmed the cart page's history link navigates here. Zero console errors throughout. `npm test` (99), `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |
| STOR-10 | `GET /api/store/items` — list all store items; include per-item `locked` flag based on user's current level | Backend | Done |
| STOR-11 | `GET /api/store/items/[id]` — single item detail | Backend | Done |
| STOR-12 | `POST /api/store/cart` — add item to cart | Backend | Done — 403s on a locked item (STOR-04's card design); STOR-16 keeps its own independent level-gate check at checkout |
| STOR-13 | `GET /api/store/cart` — retrieve user's current cart | Backend | Done |
| STOR-14 | `PATCH /api/store/cart/[id]` — update quantity of a cart item | Backend | Done |
| STOR-15 | `DELETE /api/store/cart/[id]` — remove item from cart | Backend | Done |
| STOR-16 | `POST /api/store/checkout` — validate cart, check coin balance, check level gates, deduct coins, create `Transaction` and `InventoryItem` records, log telemetry | Backend | Done — one `Transaction`/`ITEM_PURCHASED` event per cart line (not per unit). `ANIMALS` lines skip `InventoryItem` and call PET-11's `createPetForTransaction()` once per unit instead (quantity 3 adopts 3 distinct `Pet` rows, since `Pet` has no quantity column) — wired in after merging `79-pet-sanctuary` into this branch, once `createPetForTransaction()` existed to call. Verified live against real seeded data: a mixed cart (2 Koala kits + 1 Treat box) produced 2 `Pet` rows (100/0 stats), 1 `InventoryItem` row (Treat box only), 2 `Transaction` rows (one per line, not per unit — 3 units, 2 lines), correct coin deduction, and 2 telemetry events; a solo animal purchase and a non-animal-only cart were checked separately; empty-cart/locked/insufficient-coins branches re-verified unaffected. `npm test` (99), `tsc` and `eslint` green |
| STOR-17 | `GET /api/store/history` — list the authenticated user's purchase transactions | Backend | Done |
| STOR-18 | Telemetry logging — log `STORE_VISIT`, `ITEM_VIEWED`, and `ITEM_PURCHASED` events to `TelemetryEvent` on relevant actions | Backend | Done — new `src/lib/telemetry.ts` (`logTelemetryEvent()`) is now `TelemetryEvent`'s sole owner; STOR-16's `ITEM_PURCHASED` write was refactored to go through it too. `STORE_VISIT` logs on `GET /api/store/items` (STOR-10), `ITEM_VIEWED` on `GET /api/store/items/[id]` (STOR-11, only when the item exists — a 404 logs nothing) — the backend proxies for those page-view actions until STOR-01's frontend exists to trigger them directly |

### False Urgency — Group B Only

All urgency data is fabricated. No real stock, social, or deadline data exists.

> **Reading the group (NFR-TASK-3).** The A/B assignment is not on the session
> and must never be sent to the browser — `/api/auth/session` is public JSON, and
> a participant who can read their arm is a compromised participant. Use
> `currentStudyGroup()` (`src/lib/study-group.ts`), which reads the database, and
> branch on the server so the losing variant never reaches the client bundle.

| # | Feature | Type | Status |
|---|---------|------|--------|
| URG-01 | **Countdown timer (flash sale)** — MM:SS countdown shown on item cards; resets on page load to a random value within a configured window | Frontend | Done — one banner above the grid (`src/components/store/flash-sale-banner.tsx`), not per item card: `design_handoff/TaskTails Screens.dc.html`'s Group B store frame (pin 1) draws exactly one "Flash sale · everything 20% off" banner, and design_handoff is the authoritative visual source per AGENTS.md — confirmed with the user over the ticket's literal wording rather than inventing a per-card spec that doesn't exist anywhere. The "configured window" (5–15 min) and expiry behaviour (loops to a fresh random value rather than freezing or hiding) were likewise unspecified in Requirements.md/claude-memory — confirmed with the user, not guessed. Gated server-side in `src/app/store/page.tsx` via `groupGatedData()` (INF-17): only a pre-decided `<FlashSaleBanner />` element or `null` crosses into the client `StoreBrowser`, never the group value itself, per `study-group.ts`'s NFR-TASK-3 isolation rule. Renders nothing until mounted to avoid a hydration mismatch from picking the random start second on both server and client (the same bug class STOR-02/03 hit with locale formatting). Verified live against the real Docker Postgres, first with two scratch accounts forced to Group A/Group B directly in the database (Group B showed the banner ticking down, 9:38 → 9:30 over a few seconds, with a real `role="timer"` and `aria-label="Sale ends in …"` accessible name; Group A's page had no trace of the banner at all, not just hidden; both deleted after), then re-confirmed against `johan.testb@email.com` — a standing Group B test account created on request and kept, not deleted — which showed the same ticking banner (12:29 → 12:21) after a full computer restart and Docker/dev-server restart. Zero console errors throughout. `npm test` (99), `tsc` and `eslint` green |
| URG-02 | **Stock depletion indicator** — "Only X left!" badge on item cards; X is a seeded random value per item per session | Frontend | Done — `src/components/store/stock-badge.tsx`, wired through `store-item-card.tsx`/`store-browser.tsx`/`src/app/store/page.tsx` the same slot pattern URG-01 established. Not a client component — unlike `FlashSaleBanner` this has no ticking state, so nothing needs `"use client"` and zero extra JS ships for it. `StorePage` calls URG-08's `urgencyDataForItems()` directly (not the `/api/store/urgency-data` route — this is already a server component, so hitting its own API route would be a needless network hop back to itself) inside `groupGatedData()`, builds one `<StockBadge>` per **unlocked** item (a locked item shows "Unlocks at Lvl N" instead of a buy row, so advertising urgency on something you can't yet buy would read as nonsensical — same call STOR-05's add-to-cart button already made), and hands the whole `Record<itemId, ReactNode>` down through `StoreBrowser` to `StoreItemCard`, which only ever renders whatever slot it's given. Badge matches the mock exactly (measured, not eyeballed): `#DB4C3F` fill, white text, 20px pill radius, 9px/800 type, positioned ~8px from the card's top-right corner. Verified live against the real Docker Postgres: `johan.testb@email.com` (Group B) showed "Only 2/2/4 left!" on the three unlocked items and no badge on any of the four locked ones, identical values across a hard reload (seeded, not random-per-render) even as URG-01's flash-sale timer correctly reset to a fresh value; a scratch Group A account showed no badges and no flash-sale banner at all. Zero console errors on either account. Test account deleted after. `npm test` (103, unchanged — no new lib logic, URG-08 already covers `urgencyDataForItems()`), `tsc` and `eslint` green. **Revised by URG-03**: `StockBadge` is no longer self-positioned (`position: absolute` moved to `StoreItemCard`'s wrapper) so it can stack with `CartActivityBadge` — visible position on screen is unchanged, only which element owns the CSS moved. **Revised again after all 7 frontend URG tickets shipped**: at the user's request, made mutually exclusive with `CartActivityBadge` (URG-03) instead of "both allowed" — see URG-03's row for the detail, since that's where the shared `badgeSelection` seed lives |
| URG-03 | **Cart activity indicator** — "N people have this in their cart" label on item cards; N is a seeded random value | Frontend | Done — `src/components/store/cart-activity-badge.tsx`. Copy follows `design_handoff/TaskTails Screens.dc.html`'s own badge exactly ("N in carts", amber `#E5A93C`) over this row's own looser "N people have this in their cart" and Requirements.md §4's "5 people have this in their cart" — design_handoff is the authoritative visual source per AGENTS.md. **Badge-stacking design decision, confirmed with the user rather than guessed** (also recorded in the assistant's own project memory, not `claude-memory/` — that folder is read-only during ticket implementation per `CLAUDE.behavior.md`): the mock's Group B frame only ever shows one urgency badge per example card, but nothing stops an item having both stock and cart-activity data now that both tickets exist, so `urgencyDataForItems()` (URG-08) originally gained a seeded `showStockBadge`/`showCartActivityBadge` selection allowing both. `StoreItemCard` owns a single `position: absolute` top-right wrapper (`flex flex-col`) that stacks whatever it's handed, rather than each badge positioning itself — see URG-02's row above for that revision. Unit tested (`src/lib/urgency.test.ts`): 2–9 range, at least one badge always selected, both/stock-only/cart-only outcomes all occur, selection stable across repeated calls. Verified live against the real Docker Postgres: `johan.testb@email.com` (Group B) showed all three combinations across its 5 unlocked items (stock-only, cart-only, and one item with both stacked with a clean 4px gap and no overlap, measured via computed styles — `Only 5 left!` at top 9–26px, `7 in carts` at top 30–48px within the card), correct `#DB4C3F`/`#E5A93C` fills; a scratch Group A account showed neither badge type nor the flash-sale banner. Zero console errors on either account. Test account deleted after. `npm test` (106), `tsc` and `eslint` green. **Revised after all 7 frontend URG tickets shipped, at the user's explicit request**: made mutually exclusive instead — `badgeSelection` narrowed from a 3-way pick (stock/cart/both) to a 2-way pick (stock/cart), so exactly one corner badge always shows, matching the mutual-exclusivity convention every note-slot ticket (URG-04..07) already used. `urgency.test.ts` updated accordingly (exactly-one assertion replacing the old "at least one, sometimes both" one). Verified live in a fresh browser tab against the real Docker Postgres: `johan.testb@email.com` (Group B) showed exactly one corner-badge element per unlocked item (confirmed via DOM inspection, not just visual scan) across all 5 unlocked items, no card with two; a scratch Group A account still showed nothing. Zero console errors. Test account deleted after. `npm test` (112, unchanged count — tests replaced, not added), `tsc` and `eslint` green. **Copy revised to "In N carts"** (2026-08-18) per `design_handoff/ADDENDUM-store-zoo-art.md`, which supersedes the older Screens frame's "N in carts" for this badge; the addendum's Group-B card art names it verbatim, and Sunflower seeds' curated card in `StorePage` is the one place it stacks under `StockBadge` again |
| URG-04 | **Recent purchases indicator** — "X sold in the last hour" label on item cards; X is a seeded random value | Frontend | Done — `src/components/store/recent-purchases-badge.tsx`, wired through a new `note` slot on `StoreItemCard` (separate from URG-02/URG-03's `badge` corner-slot — this renders inline below the category label, in normal flow, not absolutely positioned). **Two decisions confirmed with the user over the mock's own layout**: (1) the mock's Group B demo card has no category line at all when this indicator shows — it draws "X sold in the last hour" directly where the category label sits — but dropping the already-shipped, real category text for Group B read as worse than diverging from the mock, so this renders as an *additional* line below it instead; (2) range is 3–7, narrower than URG-02/URG-03's ranges, because the study only has 20 participants and the mock's own "8 sold" example reads as implausible against that population. Independent seeded coin flip (`showRecentPurchases`) — confirmed with the user — not tied to `badgeSelection`: this indicator can be absent from an item entirely, unlike the two corner badges which always show at least one. **Design decision recorded in the assistant's own project memory** (not `claude-memory/`, which is read-only during ticket implementation per `CLAUDE.behavior.md`). Unit tested (`src/lib/urgency.test.ts`): 3–7 range, both on/off outcomes occur, stable across repeated calls, independence from the corner-badge seed. Verified live against the real Docker Postgres: `johan.testb@email.com` (Group B) showed the line on 4 of its 5 unlocked items with values in range, category label still present on every card, coexisting correctly with corner badges (e.g. Koala kit had both a stock badge and a purchases line; Treat box had corner badges but no purchases line — confirming independence); computed styles matched the mock exactly (`#DB4C3F`, 9.5px/800, flame icon). A scratch Group A account showed none of the four urgency features. Zero console errors on either account. Test account deleted after. `npm test` (109), `tsc` and `eslint` green. **Revised by URG-05**: `showRecentPurchases`'s seed is no longer its own independent coin flip — it's now derived from a shared `noteSelection` seed (mutually exclusive with URG-05's note), so the same user+item can show a different `showRecentPurchases` value than before this revision. No live data was affected — this is pre-study dev/testing, not the actual 2-week collection window |
| URG-05 | **Urgency language overlay** — "Last chance!", "Don't miss out!" text on item cards or item detail | Frontend | Done — `src/components/store/urgency-language-note.tsx`, fixed copy ("Last chance — don't miss out!") matching `design_handoff/TaskTails Screens.dc.html`'s own single example verbatim rather than rotating Requirements.md §4.2's wider phrase taxonomy — confirmed with the user; no item-detail page exists yet (only `/store`, `/store/cart`, `/store/history`), so this ticket is card-only. **Mutual exclusivity with URG-04, confirmed with the user rather than guessed**: the mock draws this text in the exact same card slot URG-04's "X sold in the last hour" occupies, and stacking two full sentences read as too crowded for a 172px card (unlike the two corner badges, which the user chose to allow stacking). `urgencyDataForItems()` (URG-08) replaced URG-04's independent `showRecentPurchases` coin flip with a single seeded `noteSelection` (neither / recent-purchases / urgency-language) so at most one is ever true — "neither" stays possible, preserving URG-04's own already-confirmed "can be absent" behaviour. **Design decision recorded in the assistant's own project memory** (not `claude-memory/`, read-only during ticket implementation per `CLAUDE.behavior.md`). No icon, unlike `RecentPurchasesBadge` — matches the ticket's own "text" wording and the mock's plain-text rendering. Unit tested (`src/lib/urgency.test.ts`): never both true on the same item, all three outcomes (recent-purchases/urgency-language/neither) occur, stable across repeated calls. Verified live against the real Docker Postgres: `johan.testb@email.com` (Group B) showed "Last chance — don't miss out!" on 2 of its 5 unlocked items, "X sold in the last hour" on the other 3, never both on one card; computed styles matched the mock exactly (`#DB4C3F`, 9.5px/800, no icon). A scratch Group A account showed none of the five urgency features shipped so far. Zero console errors on either account. Test account deleted after. `npm test` (112), `tsc` and `eslint` green |
| URG-06 | **Bundle timer** — "Buy 2 get 1 free — offer ends in MM:SS" banner; timer resets on page load | Frontend | Done — `src/components/store/bundle-timer-badge.tsx`. Lands in the same `note` slot as URG-04/URG-05 (confirmed by the user before implementation started, matching the design mock's own layout), so `urgencyDataForItems()`'s `noteSelection` seed (URG-08) grew from a 3-way to a 4-way pick (neither/recent-purchases/urgency-language/bundle-timer) rather than adding a separate rule — still mutually exclusive, still "neither" possible. A client component — unlike `RecentPurchasesBadge`/`UrgencyLanguageNote`, this one ticks, so it follows `FlashSaleBanner`'s (URG-01) pattern exactly: renders nothing until mounted (hydration-mismatch avoidance), deferred `setTimeout` for the initial pick (`react-hooks/set-state-in-effect`), loops to a fresh random value on hitting 0:00 rather than freezing (all urgency data is fabricated, no real deadline — §4). **Window is 3–8 minutes, narrower than URG-01's 5–15 — confirmed with the user**: the mock's own static demo values differ between the two timers (~14:32 vs ~4:59), read as intentionally different in scale. "Buy 2 get 1 free" is decorative copy only, like every other Group B stimulus — checkout applies no real bundle discount. Unit tested (`src/lib/urgency.test.ts`): `noteSelection` extended to assert at most one of the three note flags is ever true (not just pairwise), all four outcomes (three notes + neither) occur, stability. **Design decision recorded in the assistant's own project memory** (not `claude-memory/`, read-only during ticket implementation per `CLAUDE.behavior.md`). Verified live against the real Docker Postgres (after restarting a dev server that had degraded to 60s+ response times from an hour of accumulated Turbopack recompiles — confirmed unrelated to this ticket's code by testing in a fresh browser tab against the restarted server): `johan.testb@email.com` (Group B) showed "Buy 2 get 1 · M:SS" on 2 of its 5 unlocked items, ticking down correctly (7:17→7:07, 4:40→4:30 over ~10s) with both values in the 3–8 min range, `role="timer"` with a correct `aria-label`, computed styles matching the mock exactly (`#8478C4`, white, 7px radius, 9px/800, inline-block); never appeared alongside the other two note types on the same card. A scratch Group A account showed none of the six urgency features shipped so far. Zero console errors in a fresh tab. Test account deleted after. `npm test` (112), `tsc` and `eslint` green |
| URG-07 | **Currency-based urgency indicator** — "Double XP for purchases this hour only" or "Bonus coins if bought before midnight" banner | Frontend | Done — `src/components/store/currency-urgency-badge.tsx`, fixed copy ("Double XP this hour only") matching the mock's own badge verbatim over this row's two looser examples — same design_handoff-is-authoritative precedent URG-05 already established. Purely decorative, like every other Group B stimulus (§4) — no checkout/economy logic actually doubles XP. Lands in the same `note` slot as URG-04/05/06 (visible directly in the mock — all four render in the identical card position), so `urgencyDataForItems()`'s `noteSelection` seed (URG-08) grew from a 4-way to a 5-way pick (neither/recent-purchases/urgency-language/bundle-timer/currency-urgency) — same extension pattern URG-06 used, no new rule needed. Not a client component — static text, no ticking state, unlike `BundleTimerBadge`. Unit tested (`src/lib/urgency.test.ts`): mutual exclusivity extended to all four note flags, all five outcomes (four notes + neither) occur, stability. **Design decision recorded in the assistant's own project memory** (not `claude-memory/`, read-only during ticket implementation per `CLAUDE.behavior.md`). Verified live against the real Docker Postgres in a fresh browser tab (learned from URG-06's dev-server-degradation false alarm): `johan.testb@email.com` (Group B) showed "Double XP this hour only" on 1 of its 5 unlocked items, computed styles matching the mock exactly (`#DB4C3F`, white, 7px radius, 9px/800, inline-block), never alongside the other three note types on the same card. **Revised 2026-08-18** for `ADDENDUM-store-zoo-art.md`: copy gained the addendum's exclamation mark ("Double XP this hour only!"), and an `overlay` variant renders it as a `rounded-pill` corner badge over the card's art region for the addendum's curated Hearts card — the addendum draws that one top-*centred*, but the user asked for it on the same right-hand edge as every other badge (a lone centred pill read as misaligned beside its neighbours), so `StoreItemCard`'s centred slot was dropped and it goes through the shared `badge` wrapper. The inline `note`-slot form (still the one URG-08's random seed picks) is unchanged apart from the copy. A scratch Group A account showed none of the seven urgency features shipped so far. Zero console errors on either account. Test account deleted after. `npm test` (112), `tsc` and `eslint` green. **All 8 URG tickets (URG-01 through URG-08) are now Done.** |
| URG-08 | `GET /api/store/urgency-data` — return seeded fabricated urgency values (stock counts, viewer counts, sale timers) for Group B users only; returns empty/null for Group A | Backend | Done — `src/app/api/store/urgency-data/route.ts` + `src/lib/urgency.ts`. Scoped to `stock` counts only (URG-02), not the full "stock counts, viewer counts, sale timers" this row's own summary lists: URG-01 and URG-06 (the two timers) are self-contained per their own "resets on page load" wording — confirmed with the user for URG-01 — so neither needs data from here, and URG-03/URG-04's counts need this module too but their numeric ranges haven't been confirmed the way URG-02's 1–5 has, so inventing ranges for tickets nobody has taken up yet was avoided. `urgencyDataForItems()` seeds each value from a SHA-256 hash of `(userId, itemId, kind)` rather than `Math.random()` — same user always sees the same "Only X left!" for the same item, satisfying "seeded ... per item per session" with no database row or cache needed. Route follows STOR-10's auth pattern (401 signed out) and reuses `storeItemsForUser()` for item ids rather than a new Prisma query, since `store.ts` is the only module allowed to touch `prisma.storeItem`. Group isolation goes through `groupGatedData()` (INF-17) — a Group A user gets `{ items: [] }`, not an error. Unit tested (`src/lib/urgency.test.ts`): stability across repeated calls, 1–5 range, variation across items/users, stable ordering. Verified live against the real Docker Postgres: a Group B account (`johan.testb@email.com`) got 7 rows with `stock` 1–5, identical on a second request; a scratch Group A account got `{"items":[]}`; a signed-out request got 401. Zero console errors. `npm test` (103), `tsc` and `eslint` green |

---

## Module 4 — Petting Zoo

| # | Feature | Type | Status |
|---|---------|------|--------|
| PET-01 | Pet sanctuary view — displays all of the user's owned animals with their current visual state | Frontend | Done — **rebuilt 2026-07-30** to match `design_handoff/ADDENDUM-zoo-gallery.md`, added after the first pass shipped as a single page stacking every animal's full Sanctuary stage. The addendum specifies an actual two-screen flow instead: `Zoo nav button → Zoo gallery → (tap a pet) → Sanctuary`. `/zoo` (`src/app/zoo/page.tsx`) is now the gallery — a bespoke header ("Your zoo" / "`{n}` friends · tap to visit", no coin pill, since `AppHeader`'s title variant has no way to omit one), a 2-column grid of `ZooGalleryCard`s (new, `src/components/pets/zoo-gallery-card.tsx`), and a trailing dashed "Adopt another" tile linking to `/store`. The grid renders even at zero pets — the addendum's own instruction ("should surface the adopt slot prominently"), not a separate empty-state screen. `/zoo/[id]` (new) is the drill-in Sanctuary — `petForUser()` (new, scoped lookup in `src/lib/pets.ts`, same "0 rows → redirect" pattern `taskForUser()`/`EditTaskPage` use) renders one `AnimalCard` (PET-02) full-screen, under a header that's the addendum's other change: the pet's own name instead of "Sanctuary", no coin pill, no background fill, a back chevron to `/zoo` — built the same bespoke way `EditTaskPage`'s back-to-list header is, for the same reason (neither is `PersistentHeader`'s title variant, which always carries a coin pill). **Found and fixed a real nav bug while wiring this up**: `BottomNav`'s active-tab check was `pathname === tab.href`, an exact match — visiting `/zoo/[id]` left the Zoo tab unlit. Fixed to also match anything nested under a tab's route (`isActiveTab()`), which only this ticket's nested route ever exercised. Verified live: registered a fresh account and seeded 4 `Pet` rows directly in Postgres spanning all three species and all four moods; gallery shows all 4 cards + the adopt slot with correct per-species gradients, mood pills and needs-attention borders (any non-good stat, not just critical, per the addendum); tapping a card opens `/zoo/[id]` with the correct header and stage; back returns to the gallery; Zoo tab stays lit on both screens; checked at 375px and the 640px desktop card. **Also completes PET-02** — see that row. `npm test` (74), `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: the Sanctuary header's padding standardized to the mock's `8px 18px 14px` (was flat `pt-[14px] pb-[14px]`) — see INF-12's row for the full note |
| PET-02 | Animal card — shows animal name, image, and current happiness/hunger state indicators (happy / neutral / hungry / unhappy) | Frontend | Done — `src/components/pets/animal-card.tsx` (the Sanctuary stage, `/zoo/[id]`) and the new `src/components/pets/zoo-gallery-card.tsx` (the gallery tile, `/zoo`) — one component per screen the addendum draws, sharing one mood/colour model so the two "never disagree" per its own instruction. `moodFor()` (`src/lib/pet-mood.ts`) derives the four states from the stored `happiness`/`hunger` ints directly — hunger ≥70 → Hungry (checked first, an actionable need beats a not-yet-happy read), else happiness ≤30 → Unhappy, ≥70 → Happy, otherwise Neutral. No time-based decay in this function — recomputing `happiness`/`hunger` from `lastInteractedAt` is PET-10's job, upstream of this. **Updated 2026-07-30 for the addendum's colour rule** ("the icon stroke ALWAYS matches its own bar fill... healthy = green, caution = amber, critical = terracotta") at the user's explicit request: each bar's icon, numeral and fill now share one value-driven colour (`stateTone()`, same 70/30 split as `moodFor()`, sage/amber/terracotta — see `ProgressBar`'s new `good`/`caution`/`critical` tones, replacing the old fixed `happiness`/`hunger` ones). Terracotta, not literal red: `--color-urgency` stays reserved for Group B per AGENTS.md. Hunger is inverted before it reaches `stateTone()` (`stateTone(100 - pet.hunger)`) — low hunger is the good end, the opposite direction from happiness, and `stateTone()` expects "how good is this stat"; the *displayed* number and bar width are still the raw, un-inverted `pet.hunger` (matches Requirements.md's "hunger increases over time" — only the colour is inverted, not what's shown), which is a deliberate deviation from the addendum's own sample data (its "Hunger 76% green" reads as a fullness score, not a hunger level, which conflicts with the requirements text and would mean labelling a well-fed animal's hunger figure with a *high* number). Terracotta-as-text is a known, already-accepted contrast exception in this palette (globals.css's audit, streak card numeral), not a new one invented here. The gallery's mood pill is a simpler good/needs-attention binary (sage-on-sage-tint / terracotta-hover-on-brand-soft, both exact existing tokens) — needs-attention fires on *either* stat landing in the caution or critical band, not just critical, per the addendum. **Found and fixed a real bundling bug while building this**: `AnimalCard` needed to become a client component for PET-03/04's buttons, and it was importing `moodFor()`/`MOOD_COPY` from `src/lib/pets.ts` — that file's Prisma import pulled `pg`'s Node built-ins (`dns`/`fs`/`net`/`tls`) into the browser bundle and broke the page outright. Fixed by splitting the pure mood logic into `src/lib/pet-mood.ts` (no Prisma import), the same split `task-tiers.ts` already uses for the same reason. Verified live: all four moods and all three traffic-light bands rendered with correct label/colour on both the gallery and Sanctuary cards. **Updated 2026-07-30 again**, at the user's request against a reference screenshot: the Sanctuary card now fills the screen top to bottom instead of sizing to its own content — `flex-1` on the card and its stage, `mt-auto` on the bars wrapper to pin them to the stage's bottom (mirroring the mock's own big gap between the animal and the bars), `min-h-fit` alongside so a short viewport can't trigger the same `overflow-hidden`-zeroes-the-minimum shrink bug this ticket already fixed once. Both classes are inert in the style guide's grid, whose card wrapper isn't a flex container, so that demo is unchanged — checked live, still sized to content. `npm test` (74), `tsc` and `eslint` green. **Updated 2026-07-31**, from live user testing against PET-07: two bar changes on `AnimalCard`/`ZooGalleryCard`. (1) The visible `{value}%` numeral next to each bar's label is gone — a real tester found the number and the bar disagreeing (below) confusing enough on its own without also cross-checking a digit; `aria-valuetext` still carries the real percentage for assistive tech, just nothing rendered. (2) The HUNGER bar's *fill* is now `100 − pet.hunger` (fullness) instead of raw `pet.hunger` — previously the bar grew *and* reddened as a pet got hungrier, fighting the "bigger bar = better" convention every other meter in the app uses (happiness, XP, quest progress), which is what the same tester flagged as "backwards" after petting a fox and watching its hunger bar grow. `hungerTone`'s colour direction was already correct (green at low hunger) and is unchanged; only the length flipped, so colour and length now agree with the label's actual meaning at a glance. This also restores what `design_handoff/ADDENDUM-zoo-gallery.md`'s own sample data implied ("Hunger 76% green" only makes sense as a fullness reading) — PET-02's original build deliberately took the literal-hunger reading over the mock's own sample instead, on the reasoning documented above; live testing showed that literal reading is what actually confused a real person, so this reverses it. `npm test` (160), `tsc` and `eslint` green |
| PET-03 | Pet interaction button — "Pet" action on animal card | Frontend | Done — `src/components/pets/animal-card.tsx`, the mock's action row below the stat bars (`bg-surface`, `flex gap-2` wrapper deliberately, so PET-04's "Feed" and PET-05's customize icon can join as siblings without restructuring). Uses the shared `Button` (`variant="primary" size="inline"`) rather than hand-rolled classes — its `shadow-btn`/hover/press states already match the mock's Pet button exactly. **Updated 2026-07-31**: wired to PET-07's real `POST /api/pets/[id]/pet` the day that ticket shipped, replacing the stub notice described below — see PET-07's row for the fetch/refresh wiring. **Not forward-only** — unlike a completion checkbox, petting has no local "done" state to flip (stubbed or real), so the button stays enabled and can be clicked again; each card's notice is independent local state. This forced `AnimalCard` to become a client component, which surfaced a real bug: it was importing `moodFor`/`MOOD_COPY` from `src/lib/pets.ts`, and that file's Prisma import pulled `pg`'s Node built-ins (`dns`/`fs`/`net`/`tls`) into the client bundle, breaking the page outright (Turbopack `Module not found` errors, blank screen). **Fixed by splitting the pure mood logic into a new `src/lib/pet-mood.ts`** (`PetMood`, `moodFor()`, `MOOD_COPY`) — same "pure calc, no Prisma" split `task-tiers.ts` already uses so a client component can import it safely; `src/lib/pets.ts` keeps only `petsForUser()` and the Prisma-derived `PetWithItem` type, which `AnimalCard` now imports `type`-only (erased at compile time, so it can't reintroduce the same bundling problem). Also hit and fixed **TASK-03's exact "Save changes" bug** a second time: passing `Button` both `fullWidth={false}` (emits `flex-none`) and a `flex-1` className raced on stylesheet order and lost, collapsing the button to its label width — fixed the same way, wrapping `Button` in a `flex-1` div instead. Verified live: clicked "Pet" on multiple cards, each showed its own correctly-named notice independently, button stayed enabled and re-clickable, full-width terracotta button matches the mock at both 375px and the 640px desktop card. `npm test` (74), `tsc` and `eslint` green. **Updated 2026-07-30**: `AnimalCard` now lives at `/zoo/[id]` (PET-01's Sanctuary drill-in) rather than stacked on `/zoo` — no change to this ticket's own behaviour. **Updated 2026-07-31, at the user's request**: a successful pet now spawns a small burst of 4 terracotta hearts (Lucide `Heart`, filled) over the stage, floating up and fading via a new `pet-heart-float` keyframe in `globals.css` (same "centering baked into the keyframe" shape `task-reward-float` already uses for TASK-05's coin pop). Purely decorative — fired from `handlePet()` on a successful response, not derived from `pet.happiness`, so an unrelated page refresh can't accidentally replay it. Each heart's horizontal position and stagger (`110ms` apart) are randomised/offset client-side so a burst reads as one "poof" rather than four identical hearts; no `motion-safe:` gate, same as `task-reward-float`, since the global `prefers-reduced-motion` block in `globals.css` already collapses every animation's duration regardless. Verified live: a JS-level DOM query immediately after clicking "Pet" confirmed exactly 4 heart elements with the expected staggered `animation-delay`s (0/110/220/330ms) and spread horizontal positions, and confirmed zero remained ~1.3s later once the animation/cleanup window passed. `npm test` (173), `tsc` and `eslint` green |
| PET-04 | Feed interaction — select a food item from the user's inventory, apply to animal | Frontend | Done — `src/components/pets/feed-sheet.tsx`, opened by `AnimalCard`'s new "Feed" button (sage `positive` variant, beside "Pet" in the row PET-03 already reserved). Built on the same native-`<dialog>` bottom sheet as `CreateTaskSheet` (TASK-02) — platform focus-trap, Escape, scrim tap, centred `frame:` dialog above 480px. **No dedicated read ticket exists for inventory** (INF-05 is schema only), so this ticket adds `src/lib/inventory.ts` itself — `foodInventoryForUser()`, the same "nothing else touches this model" rule `pets.ts`/`tasks.ts` use — mirroring how PET-01 added `petsForUser()` for the same reason. Fetched once by `/zoo` and passed to every `AnimalCard`, since inventory is per-user, not per-animal, and a per-card query would repeat the same read N times. Items render with `lucide-react/dynamic`'s `DynamicIcon` keyed off `StoreItem.imageUrl` (`seed.ts`'s food rows store a lucide icon name, e.g. `"wheat"`, not an image path) — new to the codebase, and it lives at the `lucide-react/dynamic` subpath, not the package root, which cost a `tsc` failure before it was found. Selection is a `role="radiogroup"` of `role="radio"` rows, same a11y pattern `TierSelect` already uses for "pick exactly one of several". **Updated 2026-07-31**: wired to PET-08's real `POST /api/pets/[id]/feed` the day that ticket shipped, replacing the stub notice described below — see PET-08's row for the fetch/close/refresh wiring. Empty-inventory state ("You don't have any food yet. Buy some in the store first.") covers the fact that, same as PET-01's animals, there is currently no in-app way to own any food either — the store isn't built. Also added the style guide's `SAMPLE_FOOD_ITEMS` so PET-02's "09 · Petting zoo" section can demo the sheet, including its empty state on the fourth (Unhappy) sample card. Verified live: seeded 2 `InventoryItem` FOOD rows for the `pet01test` account directly in Postgres (Sunflower seeds ×3, Treat box ×1) — both render with correct icon/name/quantity, selecting a row highlights it and enables "Feed" (disabled until a selection exists), confirming shows the correctly-named stub notice, "Cancel" and the scrim both close and reset the sheet for its next open; checked at 375px and the 640px desktop dialog. `npm test` (74), `tsc` and `eslint` green. **Updated 2026-07-30**: the "Feed" button/sheet now lives on `/zoo/[id]` (PET-01's Sanctuary drill-in) rather than stacked on `/zoo` — no change to this ticket's own behaviour. **Updated 2026-08-03, at the user's request**: a successful feed now spawns PET-03's same floating-heart burst over the stage, not just petting. `spawnHearts()` stays owned by `AnimalCard` (it needs the animal image's own ref to position the burst), so `FeedSheet` gained one new optional prop, `onFed`, called right after the sheet is told to close in `handleFeed()`'s success path — `AnimalCard` wires `onFed={spawnHearts}`. No new animation code; this is the existing `pet-heart-float` keyframe and `FloatingHeart` state firing from a second call site. Verified live: bought Sunflower seeds through the real store/cart/checkout flow, fed a koala through the real sheet, and confirmed via the browser console the whole round trip completes (`POST /api/pets/[id]/feed` succeeds, sheet closes, happiness/hunger bars update) — which only happens if `onFed()` ran without throwing, since it sits before `celebrateAchievements`/`router.refresh()` in the same try block. Didn't manage to catch the 1.2s burst itself in a screenshot (the shared local dev server this session attached to was mid-edit from another session, and headless timer throttling made frame-accurate polling unreliable), but the call site is identical to PET-03's already-verified `handlePet()` path, so no new risk there. `tsc` green. **Updated 2026-08-12, UX/UI pass**: the empty-food notice ("You don't have any food yet…") now pairs with a "Go to store" button linking to `/store` — `buttonClasses({ variant: "secondary", fullWidth: false, size: "inline" })`, the same treatment STOR-06's empty-cart CTA already uses, rather than leaving the dead end as text only. Verified live: seeded a pet with an empty inventory directly in Postgres, confirmed the button renders with `href="/store"` on the real `/zoo/[id]` Feed sheet, and on the style guide's Koala kit "Unhappy" sample card. `tsc` and `eslint` green. **Superseded 2026-08-12, at the user's request** (mockup supplied): the empty-food state is no longer inline text in the picker — it now replaces the whole sheet body with a dedicated prompt card (icon well, "Out of food" heading, "`{petName}`'s hungry — grab a snack from the store to feed them.", a filled "Go to store" `Link` with a cart icon, and a "Not now" close action), since there was nothing left to pick anyway. Verified live against a pet named "Kobi" seeded with zero food, confirming the copy is genuinely per-pet (not the mockup's literal name) and that both "Go to store" (`href="/store"`) and "Not now" (closes the sheet) work. `tsc` and `eslint` green. **Updated 2026-08-12, at the user's request**: dropped the `ShoppingCart` icon from "Go to store" — the app's buttons mixed icon+text and text-only inconsistently, so this pass strips every button icon it found (see AUTH-07's row for the full list). The icon well above the heading (`Utensils`, illustrative, not inside the button) is untouched. `tsc` and `eslint` green |
| PET-05 | Customize interaction — select an accessory from inventory, equip to animal | Frontend | Done — `src/components/pets/customize-sheet.tsx`, opened by `AnimalCard`'s new sparkle icon button — the fixed 52×44 square the mock draws beside the two flex-1 Pet/Feed buttons, not a third flex-1 sibling. `accessoryInventoryForUser()` (new, `src/lib/inventory.ts`) mirrors PET-04's `foodInventoryForUser()` but with no `quantity: { gt: 0 }` filter — equipping doesn't consume a copy the way feeding does, per PET-09's own spec (it only ever writes `equippedToPetId`, never touches `quantity`), so an owned-but-currently-equipped-elsewhere accessory still has to be pickable. Unlike PET-03/04's stat bars and inventory lists, **equip state is real, already-queried data, not part of the stub** — `equippedToPetId` is a genuine column PET-11/store-checkout will eventually write for real, so the sheet opens pre-selected on whichever accessory is already equipped to *this* pet, and every row is labelled with its real state ("Equipped" / "Equipped elsewhere" / "×N owned") rather than only a quantity. **Updated 2026-07-31**: wired to PET-09's real `POST /api/pets/[id]/customize` the day that ticket shipped, replacing the stub notice described below — see PET-09's row for the fetch/close/refresh wiring. Verified live: seeded one `InventoryItem` (Red collar) equipped to one of two test pets directly in Postgres — the equipped pet's sheet opens pre-selected showing "Equipped" (sage), the other pet's sheet shows the same row unselected and labelled "Equipped elsewhere", confirming shows the correctly-named stub notice; checked at 375px and the 640px desktop dialog. Also added to the style guide's "09 · Petting zoo" section (`SAMPLE_ACCESSORIES`), including a pre-equipped sample so that state is demoed without a database. `npm test` (74), `tsc` and `eslint` green. **Updated 2026-08-01**: reworked into a full-page screen at `/zoo/[id]/customize` (`src/app/zoo/[id]/customize/page.tsx`, `src/components/pets/pet-customizer.tsx`) at the user's request, replacing the bottom sheet above (`customize-sheet.tsx`, deleted) — the pet stays fixed in a stage panel at the top while the owned-accessories grid scrolls independently underneath it, and tapping a tile equips it immediately (no separate "Equip" button), showing as a badge over the pet's art so the pet visibly "wears" it as a live preview. `AnimalCard`'s sparkle button now links to this route instead of opening the sheet; the style guide's `SAMPLE_ACCESSORIES` sample was removed along with it since nothing there demos this screen. Also added an inline rename control on this screen — outside this ticket's original spec, but delivered alongside it since both touch the same screen: a new nullable `Pet.name` column (migration `20260801120008_pet_name`), `renamePet()` in `src/lib/pets.ts`, `POST /api/pets/[id]/rename`, and `petDisplayName()` in `src/lib/pet-mood.ts` (falls back to the shared `storeItem.name` until renamed) used everywhere a pet's name is shown. Verified live: registered a fresh account, seeded a Koala pet plus two accessories (Red collar, Party hat) directly in Postgres, confirmed tapping each tile re-equips instantly and persists (`equippedToPetId` moved in Postgres after each tap), renamed the pet to "Mochi" and confirmed the customize header, back-link, `/zoo` gallery card and Sanctuary header all pick it up, and confirmed only the accessory grid scrolls (its container is the only descendant with `scrollHeight > clientHeight`) while the stage panel stays put. `tsc` and `eslint` green. **Superseded 2026-08-12, at the user's request** (same mockup as PET-04's row): the empty-accessories state is now the matching prompt card — icon well (`Sparkles`, violet-tinted to match the accessory colour used elsewhere on this screen), "No accessories" heading, "`{name}` has nothing to wear yet — grab an accessory from the store.", and a filled "Go to store" `Link` with a cart icon. No "Not now" here (unlike PET-04's sheet) — this is a full page with its own back chevron already, not a dialog to dismiss. Verified live against a pet named "Kobi" seeded with zero accessories, confirming the per-pet copy and `href="/store"`. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: dropped the `ShoppingCart` icon from "Go to store", same as PET-04's row — see AUTH-07's row for the full note. `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: this screen's own header padding standardized to the mock's `8px 18px 14px` (was flat `pt-[14px] pb-[14px]`) — see INF-12's row for the full note |
| PET-06 | `GET /api/pets` — list user's animals; compute current happiness and hunger state from `lastInteractedAt` and time elapsed | Backend | Done — `src/app/api/pets/route.ts`, same auth-then-list shape as `src/app/api/tasks/route.ts` (`auth()` checked explicitly, 401 if signed out). The decay math itself is new: `src/lib/pet-decay.ts`'s `decayedStateFor()` (pure, no Prisma, injectable `now` — same shape as `economy.ts`'s `dailyAllowanceOf()`) applies a flat rate — hunger +5/hour, happiness −4/hour since `lastInteractedAt`, each clamped to 0–100 — a deliberate choice since neither Requirements.md nor the design handoff specify a rate ("decay over time implied" is the design doc's own phrase); chosen so an untouched pet crosses PET-02's 70/30 mood thresholds in well under a day, giving a once-daily participant something to notice without a background job. Decay is **read-only** — the stored `happiness`/`hunger` columns are never written back to, only recomputed on each read, matching PET-10's own scope ("calculated on `GET /api/pets`, no background job required"). New `decayedPetsForUser()` in `src/lib/pets.ts` wraps the existing `petsForUser()` with decay, kept as a separate function rather than folded into `petsForUser()`/`petForUser()` themselves — those two are read directly by PET-01/02's already-shipped `/zoo` and `/zoo/[id]` server components against static seed data, and applying decay there as a side effect of this ticket would have silently crushed those already-verified screens (the seed pets are now >24h stale, past the point where +5/hr hunger clamps at 100 and −4/hr happiness clamps at 0) without anyone asking this ticket to touch frontend behaviour. Wiring the Sanctuary/gallery pages up to decay is a reasonable next step but a Frontend-scoped one. **Completes PET-10** — its decay-computation scope is exactly what `decayedStateFor()`/`decayedPetsForUser()` implement; see that row. Verified live: registered a fresh account, confirmed `GET /api/pets` 401s signed-out and returns `{"pets":[]}` for a pet-less account, seeded one `Pet` row directly in Postgres (`happiness: 60, hunger: 40, lastInteractedAt: now - 3h`) and confirmed the route returned the exact expected decayed values (`happiness: 48, hunger: 55` — 3h × 4/hr and 3h × 5/hr) while the underlying DB row stayed at its original 60/40; cleaned up the test account and row after. `npm test` (154), `tsc` and `eslint` green. **Updated 2026-07-31**: this row's original "kept `petsForUser()`/`petForUser()` undecayed" decision was reversed after live testing of PET-07 surfaced a real bug it caused — see PET-07's row. `decayedPetsForUser()` is gone; decay is folded directly into `petsForUser()`/`petForUser()` in `src/lib/pets.ts` now, so `GET /api/pets` calls `petsForUser()` like everything else does |
| PET-07 | `POST /api/pets/[id]/pet` — record pet interaction; update happiness in `Pet` record | Backend | Done — `src/app/api/pets/[id]/pet/route.ts`, same auth-then-act shape as `tasks/[id]/complete/route.ts` (401 signed-out, 404 for a pet that doesn't exist or belongs to someone else — `petForUser()`'s "can't tell the difference" scoping). The write itself is `recordPetInteraction()` in `src/lib/pets.ts`: it runs the pet's stored stats through PET-06's `decayedStateFor()` first, *then* adds the mock's `+7` happiness boost on top of the caught-up value and persists both stats plus a reset `lastInteractedAt` — not the raw stored happiness plus 7, which would silently discard whatever had already decayed away since the last write and (worse) reset the decay clock without saving the caught-up hunger, losing that decay permanently. `hunger` itself is written back at its decayed value unchanged — petting has no hunger term in the design mock's "Pet +7 happiness". Happiness clamps at 100. **Not forward-only** (PET-03's own note) — no `updateMany` completion guard, since petting has no "already done" state and can run any number of times. **Wires up PET-03's stub for real** — `AnimalCard`'s "Pet" button now `POST`s this route and `router.refresh()`s on success, same "wired the same day" convention `SubtaskList` used for SUB-04→SUB-05, replacing the "Not connected yet" notice; the notice paragraph is repurposed for real fetch failures (`role="alert"`/`text-urgency-text`, matching `SubtaskList`'s own error styling — genuine error state, not the false-urgency accent AGENTS.md reserves for Group B). Verified live: `src/lib/pets.test.ts` (4 tests: boost application, decay-then-boost ordering, 100 clamp, cross-owner 404) plus live HTTP against the dev server — registered a throwaway account, confirmed 401 signed-out and 404 for both a nonexistent pet and another user's pet, seeded a `Pet` row 3h stale (`happiness: 60, hunger: 40`) and confirmed the route returned the exact expected caught-up-then-boosted values (`happiness: 55, hunger: 55`), confirmed the DB row was actually written (not just the response) and that repeated calls clamp happiness at 100. Also drove the real `/zoo/[id]` UI in the browser: seeded a 2h-stale pet, clicked "Pet", watched the bars update live from 60/40 to the correct 59/50 with no reload; cleaned up every test account/row after. `npm test` (158), `tsc` and `eslint` green. **Updated 2026-07-31, found via live user testing of this exact button**: `/zoo/[id]` displayed a pet's *raw, undecayed* stats (PET-06's original scope decision), so on a pet stale enough that decay outweighs the +7 boost, clicking "Pet" made the displayed happiness *drop* and hunger *rise* — indistinguishable from the button doing the opposite of its job. Root cause was the display, not this endpoint's math (verified correct above): fixed by folding decay into `petsForUser()`/`petForUser()` themselves (see PET-06's row) so the number on screen is always already current, and an interaction's effect is the only thing that visibly changes. Re-verified live against a fresh 3h-stale pet: before clicking, the Sanctuary already showed the true decayed 48%/55%; after, happiness rose to 55% (the boost) and hunger stayed exactly at 55% (unaffected by petting), confirmed via each bar's `aria-valuenow`. `npm test` (160) |
| PET-08 | `POST /api/pets/[id]/feed` — validate user owns the food item; update hunger and happiness; decrement item quantity in `InventoryItem` | Backend | Done — `src/app/api/pets/[id]/feed/route.ts`, same auth→validate→delegate shape as `tasks/route.ts`'s `POST`. Body is `{ inventoryItemId }`, validated by a new `feedPetSchema` (`src/lib/validation/pets.ts`). The real work is `recordFeedInteraction()` in `src/lib/pets.ts`: applies the mock's `hunger −18 (min 0), happiness +4 (max 100)` on top of the pet's *decayed* state (same "raw row in, `decayedStateFor()` exactly once" shape PET-07's `recordPetInteraction()` uses, applied here from the start rather than needing PET-07's live-testing bug to be found twice), inside one `prisma.$transaction` alongside the inventory decrement — a feed that consumed the item without crediting the pet (or vice versa) would be a real inconsistency, not just a display glitch. **Crosses a module boundary deliberately**: `pets.ts` owns the transaction (the pet is the primary resource) but never touches `prisma.inventoryItem` directly — the decrement is a new `consumeFoodItem(tx, userId, itemId)` in `src/lib/inventory.ts`, taking the transaction's `tx` client as a parameter so it can participate in `pets.ts`'s transaction while `inventory.ts` keeps sole ownership of `prisma.inventoryItem`, the same "the caller owns the transaction" shape `grantEarnings()` uses for `UserEconomy`. The decrement itself is guarded by `quantity: { gt: 0 }` inside `updateMany` — the same atomic single-statement guard `markTaskComplete()` uses, race-safe against two concurrent feeds fighting over the last unit of a stack (verified live: fed a 2-quantity stack down to 0, then confirmed a third feed 404s as "out of stock" rather than going negative, and the row survives at `quantity: 0` rather than being deleted, matching PET-04's "decrement, don't delete" note). Returns a `{ ok: false, reason }` result (`"pet-not-found"` or `"item-not-found"`) rather than a single null — "wrong pet" and "wrong/depleted item" need different 404 messages, unlike PET-07's single failure mode. **Wires up PET-04's stub for real**: `FeedSheet`'s "Feed" button now `POST`s this route (needed a new `petId` prop it didn't have before — the stub never needed to know which pet it was feeding); unlike PET-07's "Pet" button, a successful feed *closes* the sheet (nothing left to do, same as `SubtaskList`'s "add" flow) and `router.refresh()` picks up both the pet's new bars and the food list's `×N owned` count from the server in one round trip. Verified live: `src/lib/pets.test.ts` (6 new tests: deltas with no elapsed time, decay-then-deltas ordering, both clamps, pet-not-found, item-not-found) plus full HTTP verification against the dev server — 401 signed-out, 404 for a bad pet id and a bad/depleted item id, 400 for a malformed body, a real feed against a 3h-stale seeded pet returning the exact expected `52`/`37`, the item's `quantity` correctly dropping 2→1→0, and the third feed on an empty stack 404ing. Also drove the real `/zoo/[id]` UI: opened the Feed sheet, selected Sunflower seeds, confirmed the sheet closed and both bars updated live to the exact expected values (56%/32%-hungry) with the inventory list re-showing `×1 owned`. `npm test` (166), `tsc` and `eslint` green |
| PET-09 | `POST /api/pets/[id]/customize` — validate user owns the accessory; set `equippedToPetId` on `InventoryItem` | Backend | Done — `src/app/api/pets/[id]/customize/route.ts`, same auth→validate→delegate shape as `feed/route.ts`. Body is `{ inventoryItemId }`, a new `customizePetSchema` (`src/lib/validation/pets.ts`, same one-field shape as `feedPetSchema`). The real work is `recordCustomizeInteraction()` in `src/lib/pets.ts`, delegating the actual write to a new `equipAccessory(tx, userId, petId, itemId)` in `src/lib/inventory.ts` — `pets.ts` never touches `prisma.inventoryItem` directly, same boundary PET-08 draws for `consumeFoodItem()`. Unlike PET-08, no Pet-row changes at all — this ticket's whole scope is the `InventoryItem` write, so the response carries only `item`, not `pet`. **At most one accessory equipped per pet**: `CustomizeSheet`'s radiogroup only ever pre-selects "whichever accessory is already equipped" (singular, PET-05's own wording), so equipping a new one unequips whatever this pet had on first, inside the same transaction — including an item that was "Equipped elsewhere" on a *different* pet, since that label exists precisely to warn a selection there will move it. Ownership is checked with a plain `findFirst` *before* any write, deliberately not the guarded-`updateMany` pattern `consumeFoodItem()` uses — equipping isn't racy/scarce the way a quantity decrement is (worst case on a real race is "last click wins," an acceptable outcome for a reversible cosmetic action), and checking first specifically prevents a bad/unowned accessory id from stripping the pet's *current* accessory before discovering the new one is invalid. **Wires up PET-05's stub for real**: `CustomizeSheet`'s "Equip" button now `POST`s this route, closes on success (nothing left to do) and `router.refresh()`es so every row's equip label updates from the server. Verified live: `src/lib/pets.test.ts` (3 new tests: equips + displaces the pet's prior accessory, pet-not-found, item-not-found-before-any-write) plus full HTTP verification against the dev server — 401/404×2/400 error paths, then a real two-pet scenario: an accessory already equipped on pet B correctly *moved* to pet A when equipped there (pet B's row cleared), and equipping a second, previously-unequipped accessory onto pet A correctly displaced the first, confirmed via the DB after each call. Also drove the real `/zoo/[id]` UI: opened the Customize sheet, confirmed it opened pre-selected on the currently-equipped accessory, selected the other one, watched the sheet close, then reopened it to confirm the equip labels had swapped. `npm test` (169), `tsc` and `eslint` green. **Updated 2026-08-01**: this endpoint and `equipAccessory()`'s at-most-one-equipped behaviour are unchanged; only the caller moved — PET-05's row above covers the sheet's replacement with `PetCustomizer`'s full-page accessory grid, which now `POST`s this same route per tap instead of behind a separate "Equip" button |
| PET-10 | Animal state decay — happiness and hunger are computed values derived from time elapsed since last interaction (calculated on `GET /api/pets`, no background job required) | Backend | Done — built as part of PET-06 (`src/lib/pet-decay.ts`'s `decayedStateFor()`, wired into `GET /api/pets` via `decayedPetsForUser()`); see that row for the rate chosen and how it was verified. This ticket's own text scopes the calculation to `GET /api/pets` specifically, which is the only caller today — PET-01/02's `/zoo` and `/zoo/[id]` pages still read the undecayed `petsForUser()`/`petForUser()` directly and don't show decay yet, since nothing wires them to this route. **Updated 2026-07-31**: that gap was the actual bug PET-07's live testing found — see PET-06/PET-07's rows. Decay now runs on every pet read, everywhere, not just `GET /api/pets`; that route is no longer the only caller |
| PET-11 | Animal acquisition — when a `Transaction` is completed for an animal item, create a corresponding `Pet` record for that user | Backend | Done — `createPetForTransaction(tx, userId, storeItem, now)` in `src/lib/pets.ts`. Returns the new `Pet` (happiness 100, hunger 0 — not specified anywhere in Requirements.md or the design handoff, same "had to choose" situation PET-06's decay rate documents) for an `ANIMALS` `storeItem`, `null` for every other category (food/accessories/decorations become `InventoryItem` rows instead, STOR-16's job). Takes a `Prisma.TransactionClient` and the caller's already-validated `storeItem` rather than re-fetching it — same "caller owns the transaction, hands over what it already has" shape `consumeFoodItem()`/`equipAccessory()` use, so STOR-16 can call this from inside the same transaction that writes the `Transaction` row itself once it exists. **Now has a real caller**: STOR-16's `checkout()` (`src/lib/checkout.ts`) calls this once per unit on an `ANIMALS` cart line, inside the same transaction that writes its `Transaction` row, exactly as anticipated below — see STOR-16's row for the live verification of that integration. Verified independently here: 4 unit tests in `src/lib/pets.test.ts` (creates at 100/0 for `ANIMALS`, no-ops for each of `FOOD`/`ACCESSORIES`/`DECORATIONS`) plus a live run against the real Docker Postgres — a throwaway script created a user, ran the exact sequence STOR-16 now runs for real (`tx.transaction.create()` then `createPetForTransaction()` in the same `$transaction`) for both an animal and a food item, confirmed the animal purchase produced exactly one `Pet` row (100/0, correct `storeItem`) and the food purchase produced none, then deleted the test user (cascade-cleaned everything) and confirmed zero rows remained. `npm test` (173), `tsc` and `eslint` green |

---

## Module 5 — Admin / Researcher Dashboard

| # | Feature | Type | Status |
|---|---------|------|--------|
| ADM-01 | Admin dashboard page — restricted to admin role; accessible at `/admin` | Frontend | Done — `src/app/admin/page.tsx`, gated by the same `requireAdmin()` ADM-09's API routes use (401→`/login`, 403→`/tasks`, no dedicated "forbidden" screen since nothing in the designs draws one and a participant has no reason to be told this route exists). Desktop-first, not `AppShell`: `TaskTails Screens.dc.html`'s "Admin / researcher dashboard" group draws this as plain desktop cards with no phone frame, and INF-22 already carves the admin cards out of the mobile-adaptation pass as "already desktop-first". The page itself is a thin server component — fetch `participantSummaries()`/`studyAggregate()`/`studyInsight()`, hand them to `AdminDashboard` (`src/components/admin/admin-dashboard.tsx`), which owns the one bit of client interactivity: which participant's row is selected. **Found and fixed two real bugs while verifying live, not just checking it compiled**: (1) `StudyOverviewCard` originally called the server-only `studyInsight()` at runtime from inside the `"use client"` tree — since it's rendered directly by a client component rather than passed down as a server-rendered slot, that pulled `@/lib/admin`'s whole Prisma/`pg`/`node:*` chain into the browser bundle and failed to compile (blank black screen, confirmed via console errors naming the exact import trace). Fixed by computing `studyInsight(aggregate)` on the server page and passing the result down as a plain string. (2) `ParticipantDetailPanel`'s client-side `fetch()` of ADM-07's route hit a `RangeError: Invalid time value` in `formatShortDate()` — a plain JSON response has no `Date` type, so `joinedAt` arrived as an ISO string despite the server-side type claiming `Date` (unlike `EngagementTable`'s server-fetched props, which cross the RSC boundary with real `Date` objects intact). Fixed by reviving it (`new Date(...)`) right after the fetch. Verified live end-to-end: seeded 5 participants across both groups plus an admin account, loaded `/admin` as the admin in a fresh browser tab with a clean console, clicked multiple table rows and watched the detail panel update with matching live-fetched data, and confirmed a real signed-in participant hitting `/admin` redirects to `/tasks`. **Updated 2026-08-02**: an admin now never sees the participant app at all, not just on first login — `src/lib/admin.ts`'s new `redirectAdminsAway(userId)` is called by `/tasks`, `/store`, `/zoo`, `/profile`, `/settings` and `/onboarding`, each redirecting a signed-in `ADMIN` session to `/admin` before rendering anything. Since the default post-login `callbackUrl` is `/tasks` (`(auth)/login/page.tsx`), this alone makes "log in → land on `/admin`" true without touching the login form. **Real gap this created and fixed in the same pass**: `/settings` was the only place `LogoutButton` lived, so an admin who can never reach it had no way to sign out. `AdminDashboard` now takes a `logoutSlot: ReactNode` prop — `page.tsx` (a Server Component) renders `<LogoutButton />` server-side and hands it down, since `AdminDashboard` is a Client Component and `LogoutButton` owns a server action that can't be imported into one directly. Verified live: a real admin session hitting any of the 6 participant pages lands on `/admin` instead; a real participant session is unaffected (still lands on `/tasks` normally); clicked the dashboard's "Log out", confirmed the modal, confirmed `/api/auth/session` returned `null` afterward. `npm test` (176), `tsc` and `eslint` still green |
| ADM-02 | Per-user telemetry table — columns: participant, A/B group, session count, total time in app, days returning, tasks completed | Frontend | Done — `src/components/admin/engagement-table.tsx`, reading ADM-06's `participantSummaries()`. Each row is a `<button>`, not a clickable `<tr>`, so the "open this participant's detail" affordance works for keyboard/screen-reader users too, not just a mouse. Row click sets `AdminDashboard`'s selection state, which `ParticipantDetailPanel` (ADM-07's live consumer) reads. Verified live: 14 real seeded rows rendered with correct group pills and numbers, clicking a row highlights it and loads that participant's detail panel |
| ADM-03 | Per-user store telemetry table — columns: participant, A/B group, store visits, items viewed, items purchased, avg time on store page | Frontend | Done — `src/components/admin/store-table.tsx`, a separate table from ADM-02 rather than more columns bolted onto one, matching the two tickets' distinct column sets in the design. Same row-click-to-select behaviour as ADM-02, sharing the same selection state. Verified live alongside ADM-02 |
| ADM-04 | A/B group assignment list — shows each participant's assigned group | Frontend | Done — no separate view; `src/components/admin/group-pill.tsx` renders the "GROUP A"/"GROUP B" pill in every row of both ADM-02 and ADM-03's tables (and the ADM-07 detail panel's header), which is where the design actually draws the group assignment — a third standalone list would just repeat the same column a third time. The only legitimate admin-side use of `--color-urgency` outside a destructive action (AGENTS.md reserves it for Group B's stimuli; a researcher's own view of who is in which arm is that reservation, not a violation) |
| ADM-05 | Aggregate comparison view — side-by-side Group A vs Group B summary for each telemetry metric | Frontend | Done — `src/components/admin/study-overview-card.tsx`: KPI row (participants/telemetry events/tasks done/purchases), then a `CompareBar` (`src/components/admin/compare-bar.tsx`, a bespoke sage-vs-urgency-red labelled bar — deliberately not a `ProgressBar` variant, since that component's tones are a value-driven traffic light or a fixed semantic colour, neither of which fits "sage because Group A, urgency-red because Group B") per metric there's real data for. **Only two comparison metrics are drawn, not the design mock's three** — the mock's third bar is "perceived trust", and ADM-12 documents no instrument for it exists yet; an empty or fabricated bar would be worse than one fewer row. Verified live with real seeded data across both groups |
| ADM-06 | `GET /api/admin/users` — list all participants with aggregated telemetry; admin role required | Backend | Done — `src/app/api/admin/users/route.ts`, gated by ADM-09's `requireAdmin()`. The actual composition is `src/lib/admin.ts`'s `participantSummaries()`: per AGENTS.md's "one module owns one table" rule, this doesn't touch Prisma at all — it calls `users.ts`'s new `listParticipants()` (every `PARTICIPANT` row, the researcher's own `ADMIN` account excluded), then per participant calls `telemetry.ts`'s new `sessionMetricsForUser()`/`storeFunnelForUser()`, `tasks.ts`'s existing `completedTaskCount()`, and `checkout.ts`'s new `purchaseCount()` (real `Transaction` rows, not the `ITEM_PURCHASED` telemetry log of the same fact). Verified live against the Docker Postgres: seeded a real participant with a known session pair (5-minute gap), 2 store visits, 2 item views, 1 add-to-cart, two `STORE_TIME_ON_PAGE` events (4s/8s), one completed task and one purchase, then hit the route as a real signed-in admin — every field matched the seeded values exactly (`sessionCount: 1`, `totalTimeInAppMs: 300000`, `avgTimeOnStorePageMs: 6000`, etc.) |
| ADM-07 | `GET /api/admin/users/[id]/telemetry` — detailed telemetry for a single participant | Backend | Done — `src/app/api/admin/users/[id]/telemetry/route.ts`, same gate. `admin.ts`'s `participantTelemetryDetail()` is ADM-06's per-user summary plus `telemetry.ts`'s new `dailyActivityForUser()` (14-day activity-count sparkline, any event type). Returns null — 404 — for an id that isn't a `PARTICIPANT` row, which also quietly covers "the admin queries their own id" without a special case. **Store funnel's "Added cart" bar needed a new signal**: STOR-18 scoped telemetry to `STORE_VISIT`/`ITEM_VIEWED`/`ITEM_PURCHASED` and never logged an add-to-cart action, so `src/lib/cart.ts`'s `addToCart()` now also logs a new `ADD_TO_CART` event on success (not on the locked/not-found failure branches) — without it the funnel's middle bar had no real data to show. Verified live: 404 for an unknown id and for the admin's own real id (confirmed via `/api/auth/session`), 200 with the exact seeded numbers for a real participant, `dailyActivity` correctly bucketing all 9 same-day seeded events onto today and zero elsewhere |
| ADM-08 | `GET /api/admin/aggregate` — aggregated Group A vs Group B metrics computed from `TelemetryEvent` records | Backend | Done — `src/app/api/admin/aggregate/route.ts` → `admin.ts`'s `studyAggregate()`: study-wide totals (participant count, total telemetry events via `telemetry.ts`'s new `totalTelemetryEventCount()`, tasks completed, purchases — the latter two summed from ADM-06's per-participant rows rather than new global queries) plus one `GroupAggregate` per A/B arm (avg store visits/participant, purchases ÷ views as a conversion rate). **Deliberately carries no "perceived trust" field** — ADM-12 documents that no instrument for it exists yet, and a fabricated number would be worse than the visible gap. Verified live: with 7 Group A and 3 Group B seeded participants (only one, Group A, with any real activity), `avgStoreVisits` for A computed to exactly `2/7` and `avgConversionRate` to exactly `1/2`, Group B correctly all-zero |
| ADM-09 | Admin role middleware — reject non-admin users from all `/api/admin/*` routes with 403 | Backend | Done — `src/lib/admin.ts`'s `requireAdmin()`, the single enforcement point every `/api/admin/*` route calls (same "one place the rule lives" shape `study-group.ts`'s `groupGatedData()` uses for Group A/B isolation): reads `User.role` from the database via a new `findUserById()` on `users.ts` rather than trusting the session/JWT, same reasoning `currentStudyGroup()` documents for `abGroup`. Returns 401 signed-out, 403 for a signed-in non-admin, `{ ok: true, userId }` for an admin. Unit-tested in isolation when first written (`src/lib/admin.test.ts`), but only fully verified once ADM-06/07/08 gave it real routes to protect: logged in as a real `PARTICIPANT` account and confirmed 403 from both `/api/admin/users` and `/api/admin/aggregate`, then logged in as a real `ADMIN` account and confirmed 200 from all three admin routes. `npm test` (176), `tsc` and `eslint` green across all four tickets |
| ADM-10 | Session telemetry logging — log `SESSION_START` and `SESSION_END` events; record time-on-page for store pages | Backend | Done — `src/lib/telemetry.ts`'s `TelemetryEventType` gained `SESSION_START`/`SESSION_END`/`STORE_TIME_ON_PAGE`, each logged through the existing `logTelemetryEvent()` (still the sole writer of `prisma.telemetryEvent`). Two new client components, both render nothing: `src/components/telemetry/session-tracker.tsx` mounts on every authenticated page (`/tasks`, `/store`, `/zoo`, `/profile`, `/settings`, `/onboarding`) and logs one `SESSION_START` per browser tab — gated by a `sessionId` in `sessionStorage` (tab-scoped, survives in-app navigation) so only the first authenticated page a tab loads logs a start, then a `pagehide` listener `sendBeacon`s `SESSION_END` with the same id when the tab actually goes away (closed or refreshed) — matched pairs by `sessionId` are what "session count" and "total time in app" aggregate over later. `src/components/telemetry/store-time-tracker.tsx` mounts only on `/store` and sends `STORE_TIME_ON_PAGE` with the elapsed `durationMs` on whichever comes first: `pagehide`, or the component's own unmount (an in-app navigation away from `/store` never fires `pagehide`, so the effect cleanup is what catches that case). Three new routes receive these: `POST /api/telemetry/session-start`, `/session-end`, `/store-time` — same auth-checked-here-explicitly shape as every other route, 401 signed-out, 400 on a malformed body. **`/store` still being STOR-01's `ComingSoon` placeholder doesn't block this**: the ticket asks for time-on-*route*, not time interacting with real store cards, so it measures correctly today and needs no changes once STOR-01 ships. **Note for future reference**: in dev, React Strict Mode's synthetic double-invoke of `StoreTimeTracker`'s effect logs one spurious ~1ms `STORE_TIME_ON_PAGE` event per real visit (mount → synthetic cleanup → remount) — confirmed live, harmless, and dev-only; Strict Mode doesn't double-invoke effects in a production build, so real telemetry is unaffected. Verified live against the Docker Postgres: registered a real account, logged in, confirmed exactly one `SESSION_START` fired (network tab + DB row) on landing on `/tasks`; navigated to `/store`, confirmed no second `SESSION_START` and one `STORE_TIME_ON_PAGE`; navigated back to `/tasks`, confirmed a second, correctly-larger-duration `STORE_TIME_ON_PAGE` on the real unmount; `location.reload()`'d and confirmed a `SESSION_END` landed with the exact same `sessionId` as the original `SESSION_START`, and no duplicate start. `npm test` (176), `tsc` and `eslint` green. **Updated 2026-08-02 — real bug found on a live account, not just in testing**: `session-tracker.tsx` wrote `sessionId` to `sessionStorage` *before* confirming the `session-start` POST had actually succeeded, so a dropped request (confirmed cause: the `docker compose --profile app` container being rebuilt mid-session — its creation timestamp changed twice in ten minutes while investigating) silently poisoned that browser tab into believing a session had started, with no retry, for the rest of the tab's life — the failure was swallowed by a bare `.catch(() => {})`. Caught via a real account (`saskiasteyn`) that had a fresh `STORE_TIME_ON_PAGE` event from active use but had *never once* logged a `SESSION_START`, despite `StoreTimeTracker` (no such gate) working fine on the same page. Fixed by moving the `sessionStorage.setItem` into the fetch's `.then((res) => res.ok && ...)`, so a failed attempt now correctly retries on the next page load instead of being remembered as done. Verified live by deliberately pointing the tracker at a nonexistent endpoint, confirming `sessionStorage` stayed empty and no `SESSION_START` row appeared, then restoring the real endpoint and confirming the very next reload logged a `SESSION_START` whose id exactly matched the newly-cached `sessionStorage` value. `npm test`, `tsc` and `eslint` still green |
| ADM-11 | AI insight callout — generate and render the short violet summary of the Group A vs Group B divergence on the overview | Backend | To Do — **Removed 2026-08-12 at the user's request**, as a follow-up to issue #178's store-visit fix. Previously done: `src/lib/admin.ts`'s `studyInsight()`, rendered in `StudyOverviewCard`'s violet callout box, a templated sentence (not an LLM call — nothing in this codebase talks to a model API) built from the real store-visit delta between groups. Both `studyInsight()` and the callout markup were deleted outright, along with the `insight` prop threaded through `admin/page.tsx` → `AdminDashboard` → `StudyOverviewCard`; nothing else referenced either |
| ADM-12 | Trust metric capture — per-participant trust rating feeding the "trust 5.3 vs 3.7" comparison bar. **The instrument is undecided** — nothing in the app currently collects this | Backend | To Do |

---

## Module 6 — Profile & Settings

The designs put logout, consent and all preferences on Settings (frame 17), reached
from the Profile banner. See also AUTH-03 (log out) and AUTH-07 (change username).

| # | Feature | Type | Status |
|---|---------|------|--------|
| PRO-01 | Profile page — avatar, name, email, "LEVEL 4" + "STUDY GROUP B" badges, and the entry point to Settings | Frontend | Done — `src/components/profile/profile-header.tsx`, level read live from `currentEconomy()`. The "STUDY GROUP B" chip is deliberately omitted, confirmed with the project owner rather than assumed: AUTH-04 already keeps `abGroup` off the wire so participants can't learn their arm, and showing it on their own profile would break the blinding NFR-TASK-3 requires. Avatar renders `avatarUrl` when set, otherwise the same dashed-border placeholder style used elsewhere (`EmptyTasksState`, `CartPanel`) — no upload wired yet, that's PRO-02/PRO-03. Verified live: registered a fresh account, confirmed the banner renders name/email/LEVEL 1 and the gear reaches `/settings`. `npm test` (176), `tsc` and `eslint` green |
| PRO-02 | Avatar upload — circular user-uploadable avatar; the default is an upload placeholder | Frontend | Done — on Settings, default now a letter avatar, not the upload-placeholder icon the title describes (see the 2026-08-12 update below) — `src/components/profile/avatar-upload.tsx`. Tapping the avatar opens the device file picker (no separate edit mode — the design draws only one state); on success it calls `router.refresh()` rather than keeping local image state, so the server's `User` row stays the one source of truth. Inline error text on a rejected upload (wrong type, too large). **Updated 2026-08-12, at the user's request**: the empty state is now `MonogramAvatar` (LEAD-07's letter-avatar primitive — initials of `displayNameFor()`'s username/email-derived handle) instead of the original dashed-border `ImagePlus` placeholder, the common "temp picture until you set a real one" pattern. Also replaces this file's own plain `<img>` for the has-a-photo case, so both states now share one primitive and one piece of base64-data-URL handling instead of two. `ProfileHeader` gained no new prop — it already threaded `name` through for the heading text, just not to `AvatarUpload` before. Verified live: registered a fresh account (no `avatarUrl`, handle "uxtest") and confirmed the header shows a single-letter "U" monogram in `MonogramAvatar`'s neutral tone, matching the same letter-avatar treatment already shipped on the leaderboard. `tsc` and `eslint` green |
| PRO-03 | `POST /api/user/avatar` — validate and store the uploaded image; set `avatarUrl` on the `User` record | Backend | Done — `src/app/api/user/avatar/route.ts` + `src/lib/users.ts`'s `setAvatar`. **Stores a base64 data URL in the `avatarUrl` column, not a file on disk**: INF-15's container has no volume outside the Postgres one (`docker-compose.yml`), so anything written to the app container's filesystem is gone on the next rebuild — this wasn't in the ticket's wording but is a real constraint of the existing deployment, flagging it rather than assuming. 3 MB cap; type is verified by magic bytes (`src/lib/validation/avatar.ts`) rather than trusting the browser's declared `file.type`. Verified live: uploaded a real PNG through an authenticated session and confirmed it round-tripped and rendered on reload; confirmed 400s for a mislabelled non-image, a 4 MB oversized file, and a missing file field. `npm test` (176), `tsc` and `eslint` green |
| PRO-04 | Lifetime stats grid — tasks done, coins earned, day streak, animals owned (2×2) | Frontend | Done — `src/components/profile/stats-grid.tsx`, read server-side by `/profile` via PRO-05's `lifetimeStatsFor()` directly (no client fetch needed for a screen with nothing to interact with). Value colours are each accent's audited `-text` token (`sage-text`/`amber-text`/`violet-text`), not the mock's raw fill hexes, which measure 2.3–2.9 BPCA on this background — the `-text` variants are what `globals.css`'s audit built for exactly this. Day streak uses `text-terracotta` regardless (2.40 BPCA) — no `terracotta-text` token exists, and the audit already accepts this same trade for the persistent header's streak numeral, so this tile isn't a new gap. Verified live: completed a real task through the full TASK-11 pipeline and watched all four tiles update correctly (3 done, 35 coins, 1 streak, 0 animals) |
| PRO-05 | `GET /api/user/stats` — aggregate lifetime totals for the authenticated user | Backend | Done — `src/app/api/user/stats/route.ts` + `src/lib/stats.ts`'s `lifetimeStatsFor()`, composing existing per-domain reads (`completedTaskCount()`, `economyForUser()`, and pets.ts's new `petCount()`) rather than querying Prisma directly. **"Coins earned" needed a real schema addition**: `UserEconomy.coins` is the spendable balance and falls when the store deducts from it, so it can't answer "coins earned" as a lifetime, only-grows figure the way tasks-done/animals-owned do — confirmed with the project owner rather than assumed. Added `UserEconomy.lifetimeCoinsEarned` (migration `pro04_lifetime_coins_earned`, backfilled to each account's current `coins` since no account predating the column could have a real history to backfill from), incremented in `grantEarnings()` alongside `coins` — it tracks the actual capped/banked amount, not the reward's pre-cap face value, and is untouched by ECO-06's coin→XP conversion, same as `coins` earning history should be. **Caught a real dev-server staleness bug while verifying live, not just in `tsc`**: the already-running dev server had an old Prisma client loaded from before the migration, so completions 500'd on `Unknown argument lifetimeCoinsEarned` — task and streak had already committed in earlier statements by the time the crash hit, leaving two "completed but ungranted" tasks on the test account (harmless, disposable dev data) before restarting the server picked up the regenerated client. Reinforces the `postinstall`/AGENTS.md note: `npx prisma generate` isn't enough on its own if a dev server is already running against the old client. Verified live end-to-end after the restart: a clean task completion granted 35 coins/45 XP correctly, `lifetimeCoinsEarned` incremented exactly once (not by the crashed attempts), all four `/profile` tiles reflected it immediately. `npm test` (176, plus new assertions on `grantEarnings`' lifetime-earned write), `tsc` and `eslint` green |
| PRO-06 | Buy XP card — violet 100 coins → 40 XP card with a "Convert" action calling ECO-06 | Frontend | Done — `src/components/profile/buy-xp-card.tsx`, wired to ECO-06's existing route for real. `costCoins`/`gainXp` come from `BUY_XP_COST_COINS`/`BUY_XP_GAIN_XP` (`rewards.ts`) read server-side by `/profile`, not ECO-06's `GET` — the page is already a server component fetching the account/economy the same way, so a client round trip for a fixed price would be pure overhead; the `GET` stays for whatever non-server-rendered consumer needs it. Convert disables itself (with a "Not enough coins yet" line) when the balance can't cover the cost, rather than only discovering that from a 409 after the tap. A level-up crossing goes straight to ECO-07's `useLevelUp().celebrate()`, same as TASK-05/SUB-05. No border on the card, unlike the mock's `#E1D8F0` — `study-overview-card.tsx`'s violet callout already established a borderless `bg-violet-tint` treatment for this app, and that hex has no home in the token system. Verified live: with a 35-coin balance the button rendered disabled with the correct message; topped up to 185 via a real task completion, tapped **Convert** for real in the browser, watched the level-up screen fire (Level 4→5) and dismissed it; confirmed in the database afterward that exactly 100 coins were deducted (185→85) and 40 XP added, and — the important cross-check with PRO-04/05 — `lifetimeCoinsEarned` stayed at 185, untouched by the conversion, same as `coins` (spendable) should be the only side, not "earned". `npm test` (176), `tsc` and `eslint` green |
| PRO-07 | Achievements grid — earned and locked badge tiles | Frontend | Done — `src/components/profile/achievements-grid.tsx`, read server-side via PRO-08's `achievementsForUser()` directly, same pattern as PRO-04/06. Locked tiles reuse this session's dashed-placeholder treatment (`AvatarUpload`, `EmptyTasksState`) rather than the mock's literal `#F2EEE7`/`#B8AFA4`; earned tiles use each accent's audited `-text` token for the icon (`text-sage-text`/`text-violet-text`/`text-amber-text`, plus `text-terracotta` for the one accent with no `-text` variant — same accepted trade as the day-streak stat tile). Verified live: rendered all-locked on a fresh account, then watched individual tiles flip to earned as PRO-09's criteria were met for real (see PRO-09's row) |
| PRO-08 | `GET /api/user/achievements` — list all achievements with per-user unlocked state | Backend | Done — `src/app/api/user/achievements/route.ts` + `src/lib/achievements.ts`'s `achievementsForUser()`, composing `prisma.achievement`/`prisma.userAchievement` the same "one module owns this table" way as `tasks.ts`/`economy.ts`. Verified live via real HTTP: 401 signed-out, 200 with all 4 catalogue rows and correct per-user `unlockedAt` (null vs. a real timestamp) as PRO-09 unlocked them |
| PRO-09 | Achievement unlock service — evaluate criteria on task completion, purchase, and pet interaction | Backend | Done — `evaluateAchievements()` in `src/lib/achievements.ts`, called from all five relevant routes: task complete, subtask complete, checkout, and all three pet-interaction routes (pet/feed/customize) — the ticket's three trigger points, with task completion covering both TASK-11 and SUB-05. Re-evaluates the whole catalogue against current progress on every call rather than only the criterion type the trigger could plausibly move; cheap at this scale (a handful of achievements, ~20 participants) and simpler than wiring which trigger maps to which criterion type. Idempotent via `UserAchievement`'s `@@unique([userId, achievementId])` (INF-19), not an "already unlocked" check racing itself. **The catalogue itself needed inventing** — INF-19's row already flagged that the design draws badge tiles but names none, so PRO-09 (this ticket) owns what they are: `prisma/seed.ts` now seeds 4 achievements, one per criterion type the schema's own doc comment sketches (`TASKS_COMPLETED`/`LEVEL_REACHED`/`STREAK_DAYS`/`ITEMS_PURCHASED`), with the same example thresholds (10/5/7/1) rather than invented numbers — Task Champion, Rising Star, Week Warrior, First Purchase. Verified live end-to-end against the real Docker Postgres: fresh account showed all 4 locked; completing a task (crossing Level 5 from an earlier session) unlocked "Rising Star" immediately; a real store checkout unlocked "First Purchase"; a pet interaction ran the evaluation cleanly with no new unlock (no criterion type reads pet data yet, as expected) and no error. `npm test` (176), `tsc` and `eslint` green. **Updated 2026-08-02 — added an unlock celebration, at the user's request** after they noticed the grid just silently flipped a tile with no moment marking it. No design frame draws this (achievement unlocking has no addendum the way level-up does), so `AchievementUnlockScreen` (`src/components/economy/`) adapts ECO-07's shipped level-up screen rather than inventing a new visual language: same full-bleed dialog/confetti/medallion, violet instead of terracotta so it reads as a different kind of event from a level crossing. `Confetti` was extracted into its own module (`confetti.tsx`) so both screens share the exact piece layout/timing rather than drifting; the violet confetti piece is swapped for terracotta via a new `tones` prop so it doesn't vanish against the violet ground. Icon-per-badge is `ACHIEVEMENT_STYLE`, pulled out of `AchievementsGrid` into `src/components/profile/achievement-style.ts` so the tile and the celebration can never disagree on a badge's icon/colour. All five `evaluateAchievements()` call sites now return `achievementsUnlocked` in their JSON response, and every corresponding client component (`TaskList`, `SubtaskList`, `CartPanel`, `AnimalCard`, `FeedSheet`) forwards it to a new `AchievementUnlockProvider`/`useAchievementUnlock()` (mounted in `AppShell`, same queue-based pattern as `LevelUpProvider`) — except `PetCustomizer`, which instantiates `AppShell` itself rather than being rendered inside one, so a hook call there could never see the provider; it renders `AchievementUnlockScreen` directly off local state instead, the same reasoning the style guide's `LevelUpDemo` already documents for not using the hook. Verified live: completed a real task crossing the 10-task threshold through the actual `/tasks` UI (not a simulated fetch) and watched "Task Champion!" fire with working confetti, medallion and dismiss, confirmed in the database afterward; confirmed the extracted `Confetti` caused no regression in the existing level-up screen; added a demo trigger for all four badges to `/style-guide#s10`. `npm test` (176), `tsc` and `eslint` still green |
| PRO-10 | Settings page — grouped ACCOUNT / NOTIFICATIONS / PREFERENCES list cards with back navigation | Frontend | Done — `src/app/settings/page.tsx` assembles `AccountCard` (PRO-10's own ACCOUNT group) with `SettingsToggleCard` instantiated twice for NOTIFICATIONS (PRO-13) and PREFERENCES (PRO-14), the existing consent note (PRO-16) and `LogoutButton` (AUTH-03), matching the mock's grouped-list-card layout with `rounded-[13px]` cards (same arbitrary-radius precedent as `StatsGrid`/`BuyXpCard`). **Updated 2026-08-12, UX/UI pass**: header padding standardized to the mock's `8px 18px 14px` — see INF-12's row for the full note |
| PRO-11 | Change password form — current and new password fields with validation and error states | Frontend | Done — `src/app/settings/password/page.tsx` + `src/components/settings/change-password-form.tsx`. No frame in `design_handoff` draws this screen (the Settings mock's row just carries a `›`), so it's built from `RegisterForm`'s existing password-pair pattern (`PasswordField`, `focusFirstInvalid`) rather than invented fresh. Verified live: wrong current password showed the field error inline; correct current password + matching new password succeeded, and signing out and back in with the new password worked. **Updated 2026-08-12, UX/UI pass**: header padding standardized to the mock's `8px 18px 14px` — see INF-12's row for the full note |
| PRO-12 | `POST /api/user/password` — verify the current password and store the new hash | Backend | Done — `changePassword()` in `src/lib/users.ts` (password is a `User` field, so it lives here, not a new module) behind `src/app/api/user/password/route.ts`. Throws the same `IncorrectPasswordError` for a wrong password and for a Google-only account with no password to change — nothing to disambiguate between those for the user. Verified live over real HTTP: 401 with a field error for a wrong current password, 200 for a correct change, confirmed by actually logging in with the new password afterward |
| PRO-13 | Notification preference toggles — "Daily task reminder" and "Streak at risk alert" (both ON by default) | Frontend | Done — instance of `SettingsToggleCard` (see PRO-14) |
| PRO-14 | App preference toggles — "Sound effects" and "Reduce motion" (both OFF by default; reduce-motion pairs with INF-14) | Frontend | Done — `src/components/settings/settings-toggle-card.tsx`, one shared component for both this and PRO-13 (same shape, same PATCH endpoint, differing only in copy/fields). New `src/components/ui/toggle.tsx` primitive — a real `role="switch"` button, knob position animated via `left`, not `translate-x`: **found and fixed a real bug live**, not just in code review — an absolutely-positioned child's "static position" (the position it resolves to when `left`/`right` are both `auto`) is not reliably `0`, so a `translate-x` offset computed assuming a `0` base compounded on top of a non-zero one and pushed the knob outside the pill entirely. Confirmed via `getBoundingClientRect()` before and after the fix. **"Reduce motion" actually does something**, not just a stored flag: the root layout (`src/app/layout.tsx`) reads the signed-in account's `UserSettings.reduceMotion` and sets `data-reduce-motion` on `<html>`; `globals.css` gained a matching (deliberately duplicated, not shared-selector) rule alongside the existing `prefers-reduced-motion` media query, so the two OR together exactly as INF-18's schema comment promises. Verified live: toggled it on, confirmed the DB row and the `<html>` attribute both updated after `router.refresh()`; toggled back off and confirmed both cleared |
| PRO-15 | `GET` and `PATCH /api/user/settings` — read and persist the notification and preference toggles | Backend | Done — `src/lib/settings.ts` + `src/app/api/user/settings/route.ts`. The Settings page itself reads `settingsForUser()` directly server-side (same pattern as PRO-05/08/06); `GET` is for a non-server-render consumer, `PATCH` is what every toggle calls, one field at a time. Verified live: real PATCH calls persisted to Postgres and round-tripped correctly |
| PRO-16 | Research consent note — participant note with the researcher withdrawal contact on Settings | Frontend | Done — already built as part of AUTH-03 (Settings needed *something* to say why usage is logged the moment Log out existed), formally closed out here since this pass rebuilt the rest of the screen around it. Unchanged: the violet callout with the exact copy the mock specifies |
| PRO-17 | Reminder delivery — actually send the daily reminder and streak-at-risk notifications, honouring PRO-13. **Channel undecided** (web push vs email); without this the toggles control nothing | Backend | To Do — asked the project owner rather than guessing given the real infrastructure cost either way (an email provider + credentials, or push subscriptions + a service worker + a scheduler for either), and the call was to skip it for now. PRO-13's toggles already persist correctly (PRO-15) and are inert rather than broken in the meantime — nothing reads them yet, but nothing is wired to silently ignore them either |
| PRO-18 | Achievements expansion — [GitHub #162](https://github.com/SaskiaSteyn/tasktails/issues/162), branch `162-pro-18-achievements`. Five parts: a lucide icon per achievement; seeded XP reward values; an unlock popup reusing PRO-09's `AchievementUnlockScreen`; updated achievement cards on Profile (PRO-07); and the full unlocked/locked Achievements list screen per `design_handoff/ADDENDUM-achievements.md` | Frontend | Done — all five parts built. **Catalogue replaced, not extended**: PRO-09's 4 placeholders (Task Champion/Rising Star/Week Warrior/First Purchase) are gone, per the project owner's call — `prisma/seed.ts` now seeds all 38 from `Achievements.pdf` and `deleteMany`s any stale key. **New criteria engine** (`src/lib/achievements.ts`, fully rewritten): 11 criterion types replace the original 4, none of which the PDF's catalogue used. Category (Streaks/Tasks/Petting Zoo/Items) is derived from criterion type, not a stored column. Evaluation is snapshot-based — one batch of ~5 reads per call regardless of catalogue size, not one query per achievement. `CATEGORY_FULLY_OWNED`/`ALL_ITEMS_OWNED`/`ANIMAL_VARIETY_OWNED` compare against the *live* catalogue count (`store.ts`'s new `catalogueItemCounts()`), so they get harder automatically as more items are seeded rather than needing a data migration. **Real schema/data gaps found and filled**: achievements never paid out any reward before this ticket (badge-only) — `economy.ts` gained `grantAchievementReward()` (bypasses the daily XP cap, same reasoning as `buyXp()`, confirmed with the project owner) and `mergeLevelUps()` (one celebration when a task's own XP and an achievement it triggers both cross a threshold in the same request). "Pet/feed 50 times" had no data to read — `Pet.lastInteractedAt` is overwritten, not counted — so migration `20260810154746_pro18_achievement_xp_and_counters` added `Pet.timesPetted`, `UserEconomy.lifetimePetInteractions`/`lifetimeFeedInteractions`, and `Achievement.xpReward`. `levelUp` is new on the checkout/pet/feed/customize routes (previously only task/subtask completion had it) since achievement XP can now cross a threshold from any of the five trigger points. **A real bug found live while verifying, not just in tests**: the checkout route's `economy` field was captured *before* `evaluateAchievements()`'s XP grant, so a purchase that unlocked an achievement returned a stale XP/level — invisible today since `CartPanel` only reads `economy.coins` (untouched by achievement XP), but a real inconsistency in the response contract; fixed by re-reading the account only when something was actually unlocked. **Profile preview strip stays 4 tiles** per the addendum — `profile/page.tsx` now sorts unlocked-first before slicing, rather than passing all 38 to `AchievementsGrid`. New screen at `/profile/achievements`: back-chevron header with a live "N / 38" pill, 5-tab category filter (`role="radiogroup"`, same pattern `StoreBrowser`'s chips use), and three row states (unlocked/locked/locked-with-progress) — the last needs a new `ProgressBar` tone (`neutral`, grey, since a locked badge's progress isn't good/bad). 17 new unit tests (`src/lib/achievements.test.ts`, one per criterion type plus the multi-achievement XP-summing and idempotency cases) — `npx tsc --noEmit`, `npx eslint`, `npx vitest run` (165, up from 148) all clean; the one pre-existing failure (`pets.test.ts`, a `next-auth`/Next 16 ESM resolution error) predates this ticket and is untouched by it. **Verified live** against a fresh account on the real Docker image (`docker compose --profile app up -d --build`, no longer the stale image every prior ticket had to work around): completed an Epic task (150c/200xp, level 1→4); bought a Common Food item and watched "Common Snack" unlock with the checkout response's `economy.xp` correctly including its +15 XP (confirming the bug fix); bought a Rare Food item through the *real* cart checkout button (`element.click()`, not a coordinate click) and watched the real `AchievementUnlockScreen` render "Rare Snack! · Own a Rare food item. · +40 XP" with the correct Drumstick icon on a violet ground; bought the Koala kit and petted it, correctly unlocking "Nature's Best Friend" (needs every owned pet petted ≥1×) and crossing a level (6→7) via achievement XP on the `/api/pets/[id]/pet` route, which had never returned a `levelUp` before this ticket; confirmed `Pet.timesPetted`/`UserEconomy.lifetimePetInteractions` incremented correctly in Postgres directly; confirmed `/profile`'s 4-tile strip showed the 3 newly-unlocked badges first; confirmed `/profile/achievements`' pill read "5 / 38" and "Menagerie" showed live progress "1/3" after the one animal purchase (superseded below — the catalogue only had 3 animals at that point). Test account deleted after. **Updated 2026-08-12, UX/UI pass**: this screen's own 5-tab category row (line above: "same pattern `StoreBrowser`'s chips use") no longer shows a native scrollbar under it — new shared `.no-scrollbar` utility, see STOR-02's row for the full note. Verified live at 340px: no bar under "All/Streaks/Tasks/Petting Zoo/Items" while it still scrolls to reveal "Items". `tsc` and `eslint` green. **Updated 2026-08-12, UX/UI pass**: header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |

**Corrected same day, at the project owner's request**: Menagerie and Full House are meant to target the real 22-animal catalogue (`Beta/Planning/Gatcha stuffs.pdf`'s Animals table), not the 3 species that happened to have artwork — `1/3` above was a symptom of GACHA-03's own long-documented gap ("the remaining 19 animals need real artwork before they can be seeded"), not a bug in these two achievements' logic. Resolved by removing the constraint rather than working around it: `ItemWell` (`item-visual.tsx`) rendered every `ANIMALS` item as `next/image` off `imageUrl` unconditionally, so an item with no real file 404'd — it now falls back to a lucide icon (new exported `hasAnimalArt()` tells a real path from an icon-name fallback), same "one shared icon, not literal artwork" precedent already used for hats/ties. Three more call sites rendered a pet's own art directly, bypassing `ItemWell` entirely, and needed the identical fix: `AnimalCard` (the Sanctuary stage — its floating-heart burst's ref moved from the `Image` to its wrapping div, so `spawnHearts()` still has something to measure under the icon fallback), `ZooGalleryCard` (gallery tiles), and `PetCustomizer` (the customize screen's pet stage). `prisma/seed.ts` now seeds the other 19 animals from the PDF (level/price/rarity as written there — these aren't part of the study's Level 1/7/10 urgency-gate story the original 3 are priced against, so nothing needed reconciling), all sharing `"paw-print"` as their icon fallback. No schema or criteria change was needed in `achievements.ts` itself — `CATEGORY_FULLY_OWNED`/`ANIMAL_VARIETY_OWNED` already targeted the *live* catalogue count by design, so Menagerie/Full House's target moved from 3 to 22 automatically the moment the seed ran. Verified live on the rebuilt Docker image: registered a fresh account, confirmed all 22 animals render in the Store's Animals filter (19 with the paw-print icon, 3 with real art, none broken); bought a Bunny kit and confirmed it renders correctly — no broken `<img>` — on `/zoo` (gallery tile), its own Sanctuary stage (`AnimalCard`, large centred icon), and would on the customize screen (code-reviewed, same fallback, not separately re-clicked through); confirmed `/profile/achievements` now reads "Menagerie · 1/22" and "Full House · 1/22" after that one purchase. `npx tsc --noEmit`, `npx eslint`, `npx vitest run` (165, unchanged — no new logic needing new tests, the criteria engine was already catalogue-size-agnostic) all clean. Test account deleted after. |

---

## Module 7 — Shared UI & States

Components and screen states specified in the designs that no feature ticket owned.
INF-12 covers the persistent header; SHR-01 covers the other half of the app chrome.

| # | Feature | Type | Status |
|---|---------|------|--------|
| SHR-01 | Bottom nav — four floating 42px tab circles (Tasks, Store, Zoo, Profile) plus a raised 46px terracotta "+" in the middle, on every logged-in screen. The "+" opens the create-task sheet (TASK-02); it replaces the old floating FAB | Frontend | Done — `src/components/layout/bottom-nav.tsx`, wired into all 3 existing authenticated pages (`/tasks`, `/profile`, `/settings`), matching the mock's footer on all three frames. Active tab detected via `usePathname()`. Store (`/store`) and Zoo (`/zoo`) tabs link to routes that don't exist yet (STOR-01, PET-01) — same forward-declared-route pattern as `/tasks` before TASK-01. Owns the create-task sheet's open state, since it's the one thing mounted on every authenticated page. **Updated 2026-07-30**: Store and Zoo are no longer dead links — see SHR-08's row for the coming-soon placeholders each now lands on |
| SHR-02 | Inline date-picker popover — **not a modal**; attaches beneath the due-date field with quick chips (Today / Tomorrow / Weekend), month nav, and a square-cell month grid. Consumed by TASK-02 and TASK-03 | Frontend | Done — `src/components/tasks/date-picker.tsx`. Genuinely inline (attaches below the trigger field, pushes content down, no overlay), quick chips, month nav, Monday-first grid with adjacent-month padding days, today ring, selected fill. Verified live: correct grid for July 2026 with June/August padding, today (Jul 29) ringed correctly, "Tomorrow" chip selects and closes to the right date (30 Jul) |
| SHR-03 | Confirm modal component — one shared component with destructive and non-destructive variants, stacked full-width actions, scrim dismiss. Consumed by TASK-04 and AUTH-03 (see `design_handoff/ADDENDUM-username-modals.md`) | Frontend | Done — `src/components/ui/modal.tsx` already existed (built for the onboarding username step's "skip" confirmation) and already matched the addendum spec exactly; noticed and flipped here rather than left stale, same as SHR-04. Its two *named* consumers are wired up as part of this pass: TASK-03's delete button (new) and AUTH-03's logout (`src/components/auth/logout-submit.tsx`, retrofitted — logout no longer fires on the visible button's click, only after confirming; the visible button is `type="button"`, and a hidden `type="submit"` sibling is what the modal's confirm actually clicks, so the confirm step can't be bypassed). Verified live: logout requires confirming and then genuinely signs out (redirects to `/login`) |
| SHR-04 | Empty state — tasks ("All clear!" + "New task" action) | Frontend | Done — built as part of TASK-01 (`src/components/tasks/empty-tasks-state.tsx`), noticed and flipped here rather than left stale. The "New task" action opens TASK-02's create-task sheet. **Updated 2026-08-12, UX/UI pass**: "+ New task" dropped its literal "+" prefix, now just "New task" — see AUTH-07's row for the full icon-consistency note |
| SHR-05 | Empty state — cart ("Cart's empty" + secondary "Go to store") | Frontend | Done — built as part of STOR-06/07 (`src/components/store/cart-panel.tsx`'s `cart.length === 0` branch), noticed and flipped here rather than left stale, same as SHR-04. Verified live: fresh account's `/store/cart` renders the icon tile, "Cart's empty", "Browse the store to treat your pets.", and an outlined "Go to store" link to `/store`, at the mobile 375px viewport |
| SHR-06 | Locked-by-level state — violet lock, "Locked — Level 7", level progress bar and levels-to-go label. Distinct from the locked *card* variant in STOR-04 | Frontend | Done — `src/components/store/locked-by-level-state.tsx`, per the mock's own "State · locked by level" frame. **Real design gap**: the mock never shows how a user reaches this full-screen state — resolved by explicit user direction (2026-08-05): STOR-04's locked card (`store-item-card.tsx`) is now a real `<button>`, and tapping it (`store-browser.tsx`) swaps the grid for this state, same "replace the content, keep the header/nav chrome" pattern `CartPanel`'s own empty/confirmation states use, rather than a modal. The mock draws no action at all on this frame; a "Go to store" button was added on the same instruction, styled like SHR-05's own secondary button, and dismisses back to the grid (there's nowhere else for it to go — this state only ever appears on `/store` itself). Progress bar/levels-to-go derive from `levelOf()` (`StorePage`, parallel to `storeItemsForUser()`'s own gate check) against the tapped item's `levelRequired` — `percent = level / levelRequired`, `levelsToGo = levelRequired - level` — checked against the mock's own worked example (level 4 vs a level-7 item: 57%, 3 to go) before wiring live. Verified live: a fresh level-1 account's `/store` shows Treat box/Cosy den/Fox kit/Penguin kit as locked buttons; tapping "Fox kit" shows "Locked — Level 7", "The fox kit unlocks at level 7. You're level 1 — keep completing tasks!", a progress bar at `aria-valuenow="14"` (1/7, matches the derived formula), "Level 1 · 6 to go", and "Go to store" — header and bottom nav stayed visible throughout; "Go to store" returned to the full grid with search/filter state intact, no reload. `npm test` (115), `tsc` and `eslint` green |
| SHR-07 | Error / offline state — "Can't reach TaskTails" with a Retry action | Frontend | Done — `src/components/layout/offline-state.tsx` (the mock's own visual, `WifiOff` in a `lucide-react` circular tint rather than the hand-drawn CSS shape, same call `StoreItemCard`'s lock glyph already made) plus `src/components/layout/connectivity-gate.tsx`, wired into `AppShell` so every authenticated screen gets it for free without wiring it up per-page. **Real design gap**, same situation SHR-06 was in: the mock never says what triggers this state — resolved by explicit user direction (2026-08-05): a client-side gate on the browser's own `online`/`offline` events (not a `Next.js` `error.tsx` render boundary, the other option offered — a different failure mode, an uncaught render/data-fetch error rather than literal connectivity), built on `useSyncExternalStore` rather than `useState`+`useEffect` to read `navigator.onLine` without either a hydration mismatch or the `eslint react-hooks/set-state-in-effect` violation the naive version hit. Wraps `AppShell`'s `children` only, keeping SHR-01's header/nav chrome visible while offline — same "replace the content, keep the chrome" pattern `CartPanel`/`StoreBrowser`'s own states use. `(auth)` login/register don't render `AppShell`, so they're outside its reach — deliberate, since the mock's "we'll sync your progress" copy only makes sense once there's progress to sync. "Retry" calls `router.refresh()` to actually re-fetch the current route's data, not just dismiss the overlay. Verified live: signed into `/tasks`, forced `navigator.onLine` to `false` and dispatched a real `offline` event — the tasks content was replaced by "Can't reach TaskTails" / "You're offline. We'll sync your progress once you're back." / "Retry", header and bottom nav still visible and functional; clicking "Retry" while still offline correctly stayed on the state; dispatching `online` (restoring `navigator.onLine`) recovered automatically back to the real tasks content with no click needed, matching the copy's own promise. Zero console errors. `npm test` (115), `tsc` and `eslint` green |
| SHR-08 | Not-found state — `design_handoff/ADDENDUM-404.md`'s full-bleed "This page wandered off" screen with a single "Back to tasks" action, plus coming-soon placeholders for Store (`/store`, STOR-01) and Zoo (`/zoo`, PET-01) so neither dead-ends before their real screens ship | Frontend | Done — `src/app/not-found.tsx` is Next's global 404, rendered automatically for any unmatched route; no route of its own needed. Matches the addendum: 96px terracotta "404", "This page wandered off", the body copy, and a `Button`-styled CTA that hugs its label (`fullWidth={false}` + explicit padding) rather than the app's usual full-width primary, since the addendum draws it that way. **The button's destination adapts to session** even though its label always reads "Back to tasks", per the addendum's own "routes to the dashboard (or marketing home if logged out)" — signed in goes to `/tasks`, signed out goes to `/`, which redirected to `/register` at the time and, as predicted, picked up the real marketing home for free when MKT-03 shipped — `src/app/page.tsx` is now that page and this CTA needed no change. **Discovered while verifying**: a signed-out visitor essentially never sees this page today — `src/proxy.ts`'s allowlist (AUTH-06) redirects any path outside its known-public set straight to `/login` before Next's router can resolve it as a 404, since the proxy can't tell in advance whether an unlisted path would 404 or succeed. Left alone rather than loosened: AUTH-06's "allowlist, not denylist" is a deliberate call for a study that must never expose participant-shaped screens without a session, and weakening it wasn't asked for. The signed-out branch is still correct, tested code — it just currently only fires via a `notFound()` call from within an already-public page, not from an arbitrary bad URL. Store and Zoo now use `src/components/layout/coming-soon.tsx` (mirrors `EmptyTasksState`'s dashed-tile look, no action button — there's nothing to *do* yet, just somewhere to land) inside the normal `AppShell`/`PersistentHeader` (title density)/`BottomNav` chrome, so both tabs work exactly like every other authenticated screen. Verified live at 375px and the 640px desktop card: `/store` and `/zoo` render, their nav tabs highlight active, and both tab away and back correctly; the 404 page's CTA resolves to `/tasks` for a signed-in session (confirmed) and to `/` for a signed-out one (confirmed by reading `src/app/not-found.tsx`'s logic directly — the proxy interaction above means it can't be reached live via a bad URL while signed out, only via `authenticate()`'s own session check going missing). `npm test` (74), `tsc`, `eslint` green |

---

## Module 8 — Marketing & Public Site

Unauthenticated entry points. Desktop-first for MKT-01/02 (the only part of the
product that is), mobile for MKT-03.

| # | Feature | Type | Status |
|---|---------|------|--------|
| MKT-01 | Marketing site — desktop nav (logo, Features / How it works / The zoo / Log in, "Get started free"), hero with CTAs, avatar-stack social proof, and the hero phone | Frontend | Done — `src/components/marketing/marketing-nav.tsx` + `marketing-hero.tsx`, served at `/` to every browser at every width (see MKT-03 for the app-vs-website split). **Responsive from 320px up**, not desktop-only: the hero stacks below `lg`, the headline steps 58 → 44 → 34px and drops the frame's hard line breaks below `sm` (they are chosen for a much wider measure), and the nav's three in-page anchors drop below `md` rather than folding into a hamburger — a disclosure widget with focus management and an Escape handler is a lot of machinery for a page that is one short scroll on a phone, where what the bar actually needs to carry is the two things you cannot scroll to: sign in and sign up. **The nav is sticky**, which the frame can't show either way since it draws a screenshot of the top of the page — three of its five items are in-page anchors, and a nav that scrolls out of reach on the way to the thing it links to is the one version that is definitely wrong; the drawn appearance is unchanged. **"Watch demo" goes to `#how-it-works`**: there is no demo video, and on a deception-study instrument a button that promises a recording and delivers a dead `#` is exactly the small lie the study is about, so it goes to the section that does show how the product works. The label is the frame's. **The frame's avatar stack and "2,400+ students building better habits" are not built.** They were, briefly, on the argument that fabricated social proof shown identically to both arms before signup is a constant rather than a stimulus. **Removed on 2026-08-06 at the user's request**: the claim is untrue, and a study about manufactured pressure should not open by manufacturing any. Deliberate absence — do not reinstate from the frame. Real numbers, if they ever exist, can go here. The frame paints body copy and nav labels #5C5470, which is `--color-violet-text` in the token file — a collision, not an intent (violet means XP and level everywhere else), so all of it uses `ink-soft`, which is what it means and measures better. **The hero phone mirrors `AnimalCard` as it ships, not the hero phone the frame draws** — that drawing predates the card, showing one stat bar with an "85%" numeral and a two-button row, where the real card has two bars (hunger inverted to a *fullness* fill after a participant called the raw one backwards), no numerals, and a third customize button. A marketing screenshot of a version of the product that no longer exists is a worse kind of wrong than one a few pixels off its mock. Its stats run through the app's own `moodFor()`/`stateTone()` rather than hard-coded colours, so the picture cannot drift from the real thresholds, and its gradient stops are `AnimalCard`'s exact two. `aria-hidden` and built from spans: "Pet", "Feed" and the customize square are pictures of buttons and must never enter the tab order, though they borrow `buttonClasses` so they cannot drift either. Verified live at 1280px: nav, hero and both CTAs render as drawn, all three anchors land with their heading 88px down (clear of the 77px nav), "Start for free" → `/register`. `npm test` (176), `tsc`, `eslint src`, `next build` green. **Updated 2026-08-10**: "Watch demo" removed at the user's request while PWA-07's install button was added to this row; see `marketing-hero.tsx`'s own doc comment for the full record and rationale kept for posterity |
| MKT-02 | Marketing site — three-column feature grid, "Three steps" how-it-works, closing CTA, dark footer | Frontend | Done — `src/components/marketing/marketing-sections.tsx`, assembled with MKT-01 in `marketing-site.tsx`. Icon wells carry Lucide marks where the frame draws bare geometry (amber square, violet diamond, sage circle) — shorthand for the icon set AGENTS.md pins to lucide-react, same call as MKT-03's chips. **`#the-zoo` anchors the "Your own sanctuary" card**: it is the nav's third link and the frame draws no zoo section, and reusing the one card that is about the zoo is a smaller departure than inventing a whole section. **The footer's Privacy / Terms / Contact are replaced** by the public destinations the site actually has — they have no pages, no tickets and nothing truthful to put behind them, and three links to nowhere in the footer of a research instrument is worse than three links fewer. Bar, wordmark, muted type and copyright are as drawn; give them real pages and they go back. The frame's #A89E92 footer type is only 3.09 on ink, so it became `--color-on-dark-soft` (#CDC7C1, 4.59 BPCA) — that colour lightened along its own hue until it clears AA, the same fix and method the `ink-soft` entry in the globals.css audit records. **No `scroll-smooth`**: it was tried and removed — the anchors land correctly either way, but a smooth scroll that fails to finish leaves the page a few pixels from where it started and reads as a dead nav, which is how it behaved in one renderer during checking; an instant jump has no such failure mode and the frame asks for no animation. The page is its own bounded scroll container, not the window's, because the root layout pins `<body>` to the viewport for `AppShell`'s nav. Verified live at 1280px: all three feature cards, the three steps, the closing CTA and the footer render as drawn, every link resolves to a real target |
| MKT-03 | Landing / welcome screen — full terracotta screen, light fox badge, three translucent feature chips, white "Get started" + "Log in" link | Frontend | Done — `src/components/marketing/welcome-screen.tsx`. **MKT-01/02/03 are one route and two designs, not three pages, and the split is app vs website — not phone vs desktop.** This screen is the installed PWA's splash: you get it when TaskTails is launched from the home screen, at any width (edge to edge on a phone, the centred 400×640 card from `frame:` up). Every browser visitor gets the marketing site and logs in from there, however narrow the screen. **This corrected a width-based split** — welcome below `lg`, marketing above — which read Module 8's "desktop-first for MKT-01/02, mobile for MKT-03" as a breakpoint. It isn't one: it describes which frame each design was drawn in, and treating it as a breakpoint meant a phone on the public site never saw the marketing page at all, only a splash for an app it had no way to install. NFR-GEN-2 asks for both viewports, not one design each. **Required building the PWA, which did not exist** — no manifest, no service worker, no install path: `src/app/manifest.ts` (`display: "standalone"`, `start_url: "/"`, the badge as an SVG icon at `sizes: "any"`), `appleWebApp` metadata in the root layout for older iOS, and a `standalone:` Tailwind variant in globals.css wrapping `@media (display-mode: standalone)`. **`/manifest.webmanifest` had to join `PUBLIC_PATHS`** in `src/proxy.ts`: the matcher exempts static assets by extension and has no `.webmanifest`, so the proxy was answering it with a redirect to `/login` — and a manifest the browser cannot read is a PWA that cannot be installed, which fails silently, with the install option simply never appearing. The switch is CSS, so both trees are server-rendered and one is `display:none` — no hydration flash, no JS to choose, no user-agent sniffing. Signed in, `/` redirects to `/tasks`, which bounces admins to `/admin` itself. This also retires `page.tsx`'s redirect to `/register` and completes the signed-out half of SHR-08's 404 CTA, which was written expecting exactly this. **`icon-light.svg` could not be used** even though the handoff names it here: it is byte-identical to `icon.svg`, and correctly so — "light" only ever recolours the ink wordmark across this asset set and the icon has none — so its terracotta forehead band merges with this screen's ground and the mark collapses into two floating cream blobs (swapping the two fills fails from the other side, as a cream "T"). Added `public/brand/icon-on-brand.svg`: same geometry one shade apart, plate #FBF6EF and band `terracotta-hover`, no new colours. The footer keeps `icon-light.svg`, which is right on ink. Terracotta is the one ground where nothing reaches 3:1, so every ratio here is an accepted AA exception already in the globals.css list, and the screen sets `--focus-ring` to white with a dark halo exactly as the level-up celebration does. Verified live at 390px and 768px: badge, chips and actions render as drawn, the two-tone focus ring is clearly visible on terracotta, "Get started" → `/register`; and with a signed-in session `GET /` answers 307 → `/tasks`. Manifest verified served at 200 with the right JSON, `<link rel="manifest">` and the apple meta tags present in `<head>`. The standalone branch was verified by applying the two declarations the built stylesheet's `@media (display-mode: standalone)` block actually contains — a real install could not be driven from the automation browser, so this proves the screen renders under that configuration and that the query drives exactly those two rules, rather than proving an install end to end. **Worth an install test on a real handset before the study opens** |

---

## Module 9 — Leaderboard

Social ranking, reached from Profile: `Profile → rank button → Leaderboard →
back → Profile`.

Specified by [`design_handoff/ADDENDUM-leaderboard.md`](design_handoff/ADDENDUM-leaderboard.md)
— **and only there.** The addendum says the frame lives in the Profile & settings
group of `TaskTails Screens.dc.html`; that file currently has no leaderboard
frame, so the addendum's measurements are the entire design.

The score is coins earned in the selected period, and **nothing in the schema
records when coins were earned**, which is why this module opens with a ledger
rather than a screen. LEAD-01/02 block every other ticket here.

Two questions these tickets deliberately leave open, because they are the project
owner's to answer: whether the leaderboard is shown to both study arms (social
ranking is itself a pressure mechanic, and the addendum does not gate it), and
whether participants seeing each other's usernames is covered by PRO-16's consent
note.

| # | Feature | Type | Status |
|---|---------|------|--------|
| LEAD-01 | Prisma schema — coin-earning ledger (`id`, `userId`, `coins`, `xp`, `source`, `createdAt`), indexed on `(userId, createdAt)` | Database | To Do — **the module is blocked on this.** `UserEconomy` carries `lifetimeCoinsEarned` (only grows), `coins` (spendable, falls on purchases) and `dailyCoinsEarned` (reset daily); `Transaction` records coins *spent*. None of them can answer "earned between two dates". Summing tier rewards over `Task.completedAt` is not equivalent either — it ignores the daily cap (NFR-TASK-2), ECO-02's anti-spam reduction and subtask bonuses. Record `xp` beside `coins` so the metric can switch without a migration |
| LEAD-02 | Write a ledger row from `grantEarnings()`, inside the transaction it already opens | Backend | To Do — one insert beside the `lifetimeCoinsEarned` increment in `src/lib/economy.ts`, so it commits or rolls back with the grant it describes. That single call site covers both earning paths (task and subtask completion). **Sell refunds stay out:** `src/lib/sell.ts` increments `coins` without touching `lifetimeCoinsEarned`, and matching that keeps the all-time board reconcilable with PRO-04's "coins earned" tile. Existing accounts start from an empty ledger — no backfill is possible, the data was never kept |
| LEAD-03 | `src/lib/leaderboard.ts` — ranked scores over a period window (this week / this month / all time) | Backend | Done — **all-time only**, which needs no ledger: `allTimeLeaderboard()` composes `users.ts`'s `listParticipants()` (admins already excluded) and `economy.ts`'s new `lifetimeEarningsFor()`, rather than touching either table itself — the `stats.ts` pattern. LEAD-01/02 stay To Do and block only the two period tabs. **Competition ranking** (1, 2, 2, 4), not row position: every deployment starts with everyone tied on 0, and splitting identical scores would report row order as if it were performance. Ties then display in account-creation order, which `listParticipants()` supplies and a stable sort keeps. **Peers never see an email**: `displayNameFor()` falls back to the email local part, which is right on your own screens and wrong on the one screen that shows accounts to each other, so `nameFor()` gives handle-less accounts a positional "Participant 7" instead. A participant with no economy row scores 0 rather than vanishing from the board. 14 unit tests (`npm test` 235 green), `tsc` and `eslint` clean. Verified against the live dev database: 12 economy rows → `participantCount` 11, the `ADMIN` row absent; scores ordered 408 / 266 / 215 / 55; the seven accounts tied on 0 all returned rank 5, not 5-11; and `mkt-check@example.com`, which has no handle, rendered as "Participant 11" rather than "mkt". **Updated 2026-08-12, at the user's request**: every row now shows the participant's real uploaded avatar (PRO-02/03) when they have one, initials otherwise — `LEAD-07`'s `MonogramAvatar` primitive already supported this (it's used the same way on other screens), it just never received `avatarUrl` on this one. `LeaderboardEntry` gained an `avatarUrl` field (string or null), `rankParticipants()`'s row shape grew the same field, and `users.ts`'s `ParticipantSummary`/`listParticipants()` now select the column (it feeds ADM-02/03/04/06's admin tables too, unaffected by the extra field). Deliberately **not** the same privacy move as `nameFor()`'s email guard: unlike an email, nobody has an avatar unless they chose to upload one, so there's no default-identifying value being newly exposed — an account with no photo still shows only initials, same as before. `LEAD-11`/`LEAD-12` (podium/ranked list) just pass the new field through. Verified live: seeded one account with a real base64 `avatarUrl` and `lifetimeCoinsEarned: 250` directly in Postgres — its row rendered the actual image, not initials, both in the ranked list and (confirmed separately) the podium; every other row still fell back to initials as before, including one pre-existing account in the dev DB whose own already-uploaded photo also rendered correctly, unprompted. `npm test` (185, `leaderboard.test.ts`'s `row()` helper updated for the new required field), `tsc` and `eslint` green |
| LEAD-04 | Current-user rank, percentile, and movement against the previous period | Backend | To Do — feeds the rank button's "Top 8% · climbing 3 spots" subline. Movement is LEAD-03 re-run over the preceding window, so no snapshot table is needed. The addendum calls movement optional, so the copy has to degrade when there is no prior period — which is every account's first week |
| LEAD-05 | `GET /api/leaderboard?period=` — leaders, the caller's own rank, and the movement figures | Backend | To Do — switching tabs re-queries rather than re-renders, so this is what LEAD-10 calls |
| LEAD-06 | Podium tokens — amber text ramp, plus the silver and bronze pedestal families | Frontend | Done — **no gold family was added**: the addendum's gold *is* the amber family (`#F2C879` = `amber-ring`, `#FCEBD3` = `amber-tint`, `#E5A93C` = `amber`) and its `#8A6416` text measures 3.00 BPCA where `--color-amber-text` measures 3.02, so first place reuses those rather than adding a fifth near-identical amber. Silver and bronze had no equivalent and got the four-part accent shape (fill / ring / tint / text). The pinned row is deliberately **not** in this block — it is built from `terracotta-tint` + `terracotta` + `terracotta-press`, because the addendum's `#C23B2E` sits a hair off `--color-urgency-text` and urgency red is reserved for Group B. Contrast measured with the repo's own `bpca()`: silver 2.57, bronze 2.48 on their pedestals; AA-passing variants exist (`#434A54` / `#473123`) but take the metal out of both, so these ship as drawn — **accepted on the project owner's call, 2026-08-10** — and are recorded in the globals.css audit block. **No ellipsis token**: one was added, then removed with the ellipsis itself when LEAD-12 moved to a full scrolling list. Verified live at `/style-guide`: all eight podium tokens, `list-well` and the three `rank-*` button tokens resolve to their intended hex |
| LEAD-07 | Monogram avatar primitive in `src/components/ui/` — initials fallback at 48 / 40 / 30px | Frontend | Done — `src/components/ui/monogram-avatar.tsx`. Renders `avatarUrl` (PRO-02/03) when there is one and initials otherwise, which on this screen is nearly every row. Five tones: the three podium medals, `neutral` for ranked rows, and `you` (terracotta, never urgency). Border width and font size scale off the diameter so a 30px circle isn't wearing 48px type. Always `aria-hidden` — every caller draws the name beside it, so announcing initials too would read the row twice. 6 unit tests on `initialsOf`, including the surrogate-pair case (`[...word][0]`, not `word[0]`). Seen rendering live at all three sizes |
| LEAD-08 | Rank button on Profile — amber card between the lifetime stats grid and the Buy XP card | Frontend | Done — `src/components/profile/rank-button.tsx`, between `StatsGrid` and the Buy XP card; the whole card is the tap target. **Two copy departures from the drawn subline, both confirmed with the project owner on 2026-08-10.** "climbing 3 spots" is not built: movement compares against the previous period's rank, nothing records when coins were earned, and fabricating one on an instrument about manufactured pressure is the wrong shortcut. "Top 8%" is not built either: a ~11-person cohort puts nine percentage points on a single place, so the true figure renders as "Top 46%" — large-population precision the number doesn't have. The subline carries period and cohort size ("All time · 11 participants") and the numeral carries the rank, with no overlap. Title is "Your rank", not "...this week", for the same reason. Verified live: `--color-rank-border` / `-sub` / `-numeral` all resolve once this consumed them (they were tree-shaken while unused) |
| LEAD-09 | Leaderboard screen — back-chevron header, and the shared bottom nav with Profile lit | Frontend | Done — `src/app/profile/leaderboard/page.tsx`, at `/profile/leaderboard` so `BottomNav`'s existing prefix match lights the Profile tab with no change to SHR-01. Header follows the Settings `ChevronLeft` pattern. **No period tabs**: only all time is answerable until LEAD-01/02's ledger exists, and three segments with two dead is exactly the control-that-promises-nothing this study is about — the period is a static "All time" label until LEAD-10 can make it real. Seen rendering live with Profile lit in the nav. **Updated 2026-08-12, at the user's request** (this screen's header vs. GATC-17's "Sell items" is the exact comparison that surfaced the inconsistency): header padding standardized to the mock's `8px 18px 14px` (was flat `p-3`) — see INF-12's row for the full note |
| LEAD-10 | Period tabs — This week / This month / All time, re-querying and re-ranking | Frontend | To Do — three equal segments, active terracotta on white. No Friends tab; friend-adding is not in scope |
| LEAD-11 | Podium — top three in 2 / 1 / 3 order, gold star, graded avatars and pedestals | Frontend | Done — `src/components/leaderboard/podium.tsx`. **DOM order is 1/2/3 and only the visual order is 2/1/3**, via flex `order`: these three appear nowhere else on the screen (the list starts at 4), so `aria-hidden` would drop the actual winners and reordered markup would announce second place first. Renders only the places that exist, so one or two participants is not a broken podium. **Scaled 1.55x from the drawn sizes at the owner's request** — the handoff's figures are for a 300x640 frame and looked squished on a real handset; proportions are unchanged (pedestal ramp still 1 : 0.72 : 0.55) and sizes derive from one `SCALE` constant. 1.55 is a ceiling, not a taste call: places are `flex-1` capped at the drawn proportion so three plus gaps still fit a 320px handset, where a first attempt at 1.65 with fixed widths overflowed at 342px. **Updated 2026-08-12, UX/UI pass**: now passes `avatarUrl` through to `MonogramAvatar`, so a real uploaded photo shows on the podium instead of always initials. See LEAD-03's row for the full note |
| LEAD-12 | Ranked list — rows 4–5 as white cards, plus the ellipsis that collapses the gap | Frontend | Done — `src/components/leaderboard/ranked-list.tsx`. **Departs from the addendum on the owner's call, 2026-08-10.** The handoff draws ranks 4-5, a centered "···", then a pinned copy of your row; built that way first and it was wrong on a real handset — two rows in a screen with room for eight, most of the board unreachable, and an ellipsis standing in for ranks nobody could ever see. The list now holds **every** rank below the podium and scrolls, so a 640px frame and an 844px handset each use their whole screen with nothing measuring anything. The ellipsis is gone because the gap it explained is gone; its token went with it. Verified against the live board: 18 of 18 ranks below the podium rendered. **Updated 2026-08-12, UX/UI pass**: now passes `avatarUrl` through to `MonogramAvatar`, same as the podium — see LEAD-03's row for the full note |
| LEAD-13 | Pinned current-user row — always visible however far down the user sits | Frontend | Done — same component. Your row is **`position: sticky` against the bottom of the scroll container**, not a pinned duplicate: it holds the bottom edge while you are below the fold and settles into its natural place in the order when you scroll to it. One row, never drawn twice, and no expand/collapse state — which is what keeps this a server component. Terracotta tokens throughout, never urgency; an opaque fill and `shadow-nav-idle` so rows passing underneath don't show through. Verified against the live board at rank 11 of 21: exactly one sticky row, exactly one "(you)", and zero sticky rows when the signed-in account is on the podium |
| LEAD-14 | Sparse and empty states — fewer than three ranked participants, and a user who earned nothing in the period | Frontend | Done — a board where nobody has earned anything is the state every deployment starts in, and a podium of three zeroes reads as a bug, so that case gets a trophy and "Nobody's on the board yet" instead. Sparse podiums (1-2 participants) fall out of LEAD-11 rendering only the places that exist. **Not exercised live**: the dev database has four scored accounts, so neither the empty board nor a 1-2 person podium is reachable without LEAD-16's seed — verified by construction and through the render harness only |
| LEAD-15 | Leaderboard telemetry — screen view and period-switch events | Backend | To Do — add the event types to `src/lib/telemetry.ts` and log through `logTelemetryEvent()`, which stays the only writer of `prisma.telemetryEvent` |
| LEAD-16 | Dev seed — demo participants with backdated ledger rows | Database | Done — `prisma/seed-leaderboard.ts`, five commands (`status` / `empty` / `restore` / `populate` / `clean`), **deliberately not wired into `prisma db seed`**, which is the real catalogue seed; this one mutates participant accounts and is run by hand. Exists because two branches are unreachable on any populated database: the empty board needs nobody to have scored, and a below-the-fold rank needs a cohort bigger than the dev data. `empty` writes every score to a gitignored backup *before* the first update and refuses to run twice, so a second run cannot replace the real values with a snapshot of zeroes. Demo accounts carry `passwordHash: null` and an `@seed.invalid` address (RFC 2606 reserved, so it can never route), and `clean` deletes exactly that domain — **anything analysing participants must exclude it or run `clean` first**, since seeded rows would otherwise sit in ADM's tables as study data. Exercised end to end on 2026-08-10: `empty` showed the empty board and the rank button's "no coins earned yet" branch, `restore` returned all 12 scores, `populate` put a real account at rank 11 of 21, `clean` returned the database to exactly its original 11 participants and scores |

---

## Module 10 — Gatcha & Lucky Box

The Lucky Box: a paid random-pull box in the Store, its reveal and odds screens,
and the sell-back path that lets a participant turn pulled items into coins
again. A Store feature (Module 3) built after that module shipped, on branch
`155-gatc-01-gatcha-element` and merged in [#157](https://github.com/SaskiaSteyn/tasktails/pull/157).

**Backfilled on 2026-08-10.** These 17 tickets were built and merged without
ever reaching this file or `Features.csv` — the gap is why "is selling
implemented?" was not answerable from the tracker. The rows below are
reconstructed from the merged code, its doc comments and the commit series, and
every one of them is `Done` on that evidence; they were **not** re-run and
re-verified ticket by ticket the way a freshly built row is.

Two things to know before working here:

- **The design source is not in this repo.** These screens follow
  `Beta/Planning/TaskTails Screens - Gacha.html` (§1, §2, rev.3), which
  `design_handoff/` has no copy of — the handoff frames predate the gacha
  feature entirely. `prisma/schema.prisma` also cites a
  `research_gacha_mechanics.md` that isn't in the repo either.
- **The code says `GACHA-NN`, everything else says `GATC-NN`.** Commits, the
  GitHub issue and these rows use `GATC`; the doc comments throughout
  `src/lib/gacha.ts` and the components use `GACHA`. The numbers agree —
  `GATC-07` is `GACHA-07` — only the prefix differs.

| # | Feature | Type | Status |
|---|---------|------|--------|
| GATC-01 | Prisma schema — `StoreItemRarity` enum (COMMON / RARE / EPIC / LEGENDARY) and `StoreItem.rarity` | Database | Done — migration `20260807055024_gacha01_store_item_rarity`. INF-20's 7 placeholder items didn't map onto a rarity ramp, so this landed with GATC-03's real catalogue |
| GATC-02 | Prisma schema — `UserEconomy.pullsSinceLegendary`, the hidden hard-pity counter | Database | Done — migration `20260807055523_gacha02_pulls_since_legendary`. **Deliberately never reaches the client**: the Lucky Box home and Drop Rates screens show no pity meter, by the approved design |
| GATC-03 | StoreItem catalogue seed — the full *Gatcha stuffs.pdf* catalogue with per-item rarity | Database | Done — `prisma/seed.ts`, expanded from 7 items to the real ~69-item catalogue across Food / Accessories / Decor / Animals. Idempotent, and matches on `name` **plus** `category` because the catalogue has two "Fish" (Food 220, Decoration 150) that a name-only lookup would collapse onto one row |
| GATC-04 | `POST /api/gacha/pull` — spend 150 coins and open one Lucky Box, as a single transaction | Backend | Done — `src/lib/gacha.ts`. Weighted roll, coin deduction, pity update and the resulting `InventoryItem`/`Pet` all commit or roll back together. Animals become a `Pet`, everything else an `InventoryItem` — the same split `checkout.ts` uses |
| GATC-05 | Hard pity — force a Legendary once 30 pulls pass without one, then reset | Backend | Done — `HARD_PITY_THRESHOLD = 30`, confirmed 2026-08-07. The counter resets on any Legendary, natural or forced. Silent by design: nothing in the response or the UI reveals it |
| GATC-06 | `RARITY_ODDS` plus `GET /api/gacha/odds` — the published drop rates | Backend | Done — COMMON 55% / RARE 30% / EPIC 12% / LEGENDARY 3%. One constant, read by both the route and GATC-15's screen, so the published odds cannot drift from the ones the roll actually uses |
| GATC-07 | `POST /api/inventory/[id]/sell` — sell an owned item or pet back for 70% of its price | Backend | Done — `src/lib/sell.ts`, `SELL_RATE = 0.7` (fixed by the owner's brief, not a draft). **The route says "inventory" but also sells pets**: an owned animal is a `Pet` row, not an `InventoryItem`, so it tries inventory first and falls back to pets — one endpoint, resolved against whichever table owns the id. Selling a pet deletes it and any equipped accessory unequips for free via `onDelete: SetNull` |
| GATC-08 | `GET /api/inventory/sellable` — everything owned, annotated with its sell value | Backend | Done — composes `inventory.ts` and `pets.ts`'s own reads rather than querying their tables, since a read has no atomicity requirement to justify the exception GATC-07's write takes |
| GATC-09 | `luckyBoxUrgencyForUser()` — fabricated Group B urgency values for the Lucky Box card | Backend | Done — `src/lib/gacha.ts`, gated through `groupGatedData()` so Group A receives nothing at all, not a hidden value. Same seeded-fabrication pattern as URG-02/03/04 |
| GATC-10 | Lucky Box card on the Store screen — full width above the item grid | Frontend | Done — `src/components/store/lucky-box-card.tsx`, the Group A baseline. Uses the identical card recipe as every other `StoreItemCard` rather than a bespoke treatment, so Group B's additions are the only difference between arms |
| GATC-11 | Group B urgency on the Lucky Box card — odds-boost banner and "N opened in the last hour" | Frontend | Done — `lucky-box-odds-boost-banner.tsx` + `lucky-box-recent-pulls-note.tsx`. The banner is worded as a probability boost rather than a count of items already taken, mirroring URG-07's currency-urgency pattern; the note mirrors URG-04's recent-purchases pattern |
| GATC-12 | Lucky Box home screen — box, cost and the Open action | Frontend | Done — `src/components/store/lucky-box-home.tsx` at `/store/lucky-box`. **No odds and no pity anywhere on this screen**, per the ticket's own wording — those live only on GATC-15, one tap away behind the info icon. **Updated 2026-08-12, UX/UI pass**: header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |
| GATC-13 | Opening moment — the ~1.5s beat between tapping Open and the reveal | Frontend | Done — `lucky-box-opening.tsx` is purely the visual; `LuckyBoxHome` enforces the minimum duration, so the beat is a fixed dramatic pause rather than however long the request happens to take |
| GATC-14 | Reveal state — the pulled item, including the locked "unlocks at Lvl N" case | Frontend | Done — a box can yield an item one level above the puller (`UNLOCK_LEVEL_BUFFER = 1`), which reveals in a locked state rather than being suppressed. GATC-07 makes it sellable even while locked |
| GATC-15 | Drop Rates screen at `/store/lucky-box/odds` | Frontend | Done — reachable only from GATC-12's info icon. Reads straight off GATC-06's `RARITY_ODDS` and never imports `HARD_PITY_THRESHOLD` or `pullsSinceLegendary`, so the pity system cannot leak onto a published-odds screen. **Updated 2026-08-12, UX/UI pass**: header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |
| GATC-16 | "Sell items" card on Profile, under the Buy XP card | Frontend | Done — `src/components/profile/sell-items-card.tsx`, same card recipe as `BuyXpCard` (PRO-06) and directly beneath it |
| GATC-17 | Sell Items screen — a Sell action per row, removing the row on success | Frontend | Done — `src/app/profile/sell/page.tsx` + `sell-items-list.tsx`. Page does the initial GATC-08 read; the list sells one row at a time against GATC-07. **Updated 2026-08-12, at the user's request**: "Sell" no longer fires GATC-07 straight away — it opens the shared `Modal` (the same destructive-confirm dialog `EditTaskForm`'s "Delete task" and `LogoutSubmit`'s "Log out" already use, `src/components/ui/modal.tsx`), naming the item and its exact payout ("Sell Fish? You'll get 154 coins…"), and only `onConfirm` calls the real endpoint; "Keep it" or a scrim tap cancels with no request sent. `pendingSell` holds the whole `SellableItem` (not a bare boolean, unlike the other two callers) since this list's confirm target changes per row. Verified live: seeded an owned Fish (220 coin price) directly in Postgres, confirmed "Keep it" closes the dialog with the row still present and no coins moved, then confirmed "Sell" removes the row and credits exactly 154 coins (`floor(220 × 0.7)`, `SELL_RATE`) in `UserEconomy` directly. `tsc` and `eslint` green. **Updated 2026-08-12, at the user's request** (this screen's header vs. LEAD-09's "Leaderboard" is the exact comparison that surfaced the inconsistency): header top padding corrected from `6px` to the mock's `8px` base offset (still safe-area-aware) — see INF-12's row for the full note |

---

## Module 9 — PWA & Offline

GitHub issue #153 ("PWA-01: Create a PWA") is this module's umbrella ticket. MKT-03
already built the installable core — manifest, standalone display mode, the iOS
meta tags, and the proxy fix that lets the manifest actually be fetched — because
MKT-03 needed a real app/website split to work at all. PWA-01 below records that
as done; PWA-02 onward is what #153 still leaves open: there is no service worker
anywhere in the codebase, so nothing here works offline yet, install still depends
entirely on each browser's own menu, and MKT-03 flagged real-device verification
as outstanding. Same open-question pattern as PRO-17: several of these need a
product call (what "offline" means for a task list, whether push is in scope) before
they can be estimated, not just built.

| # | Feature | Type | Status |
|---|---------|------|--------|
| PWA-01 | Web app manifest + install path — `display: "standalone"`, `start_url`, theme/background colour, the SVG icon, `appleWebApp` meta tags for iOS, and `/manifest.webmanifest` reachable through the proxy allowlist | Frontend | Done — built as part of MKT-03 (`src/app/manifest.ts`, `src/proxy.ts`'s `PUBLIC_PATHS`). Recorded here as this module's own umbrella ticket (#153); the rows below are what it left open |
| PWA-02 | Service worker — registration, install/activate lifecycle, and a cache-first strategy for the static app shell (JS/CSS/fonts/icons) | Frontend | Done — `public/sw.js` (the worker itself, plain JS — no build step touches `public/`) and `src/components/pwa/service-worker-registration.tsx`, mounted once in the root layout so every page registers it, signed in or out. **Deliberately narrow**: the fetch handler cache-firsts only `/_next/static/` (safe — fingerprinted, filename changes whenever content does), the hand-known static files (`/brand/`, `/animals/`, the manifest), and web fonts; it does not touch pages or `/api/` responses. TaskTails is a signed-in, per-user app, and caching HTML or API responses here would risk one participant seeing another's cached data or their own gone stale — a correctness bug this ticket has no business creating. That's PWA-03/04's decision to make, not this one's. No update-prompt logic either (PWA-08) — this worker just takes the browser's own default lifecycle. Versioned cache name (`tasktails-shell-v1`); `activate` deletes any other `tasktails-shell-*` cache, so bumping the version is the whole invalidation story. **Hit the exact same bug MKT-03's manifest did, for the same reason**: `src/proxy.ts`'s matcher exempts static assets by extension but has no `.js` in that list, so `/sw.js` was reaching the auth gate and a signed-out registration attempt got a `/login` redirect body back — `navigator.serviceWorker.register()` rejects that as a `SecurityError` ("script resource is behind a redirect"), silently, and the worker never installs. Fixed the same way: `/sw.js` added to `PUBLIC_PATHS`. Also added a `Cache-Control: no-cache` header for `/sw.js` in `next.config.ts` (Next's own PWA guide's own recommendation) — a cached worker script can never deliver its own update, which would otherwise undermine PWA-08 before it's even built. Verified live: fresh incognito-equivalent tab (no prior registration) loads with zero console errors, registers successfully (`state: "activated"`), and the cache (`tasktails-shell-v1`) is populated with exactly the static shell (JS chunks, fonts, brand icons, the manifest) and nothing else — no page or API entries. Confirmed the proxy fix specifically with a cookie-less `curl` against `/sw.js`: 200, not a redirect. `npm test` (221), `tsc`, `eslint` (including `public/sw.js` itself) green. **Amendment, discovered during PWA-09**: the cache-first `/_next/static/` strategy is fine in production, where chunks are content-hashed and therefore genuinely immutable, but is a real trap in local dev — Turbopack's dev chunks aren't hashed the same way, so once a chunk URL is cached, the worker can keep serving that exact stale JS indefinitely across edits *and* dev-server restarts (the cache is browser-side, not server-side), with no error of any kind. Cost real time on PWA-09 before being traced to this. Anyone debugging "my edits aren't taking effect" locally with the worker registered should unregister it and clear `caches` first, in a genuinely fresh tab — a plain reload isn't enough, since the same worker keeps controlling the reloaded page |
| PWA-03 | Offline app shell — a cached shell (chrome + last-known screen) renders when launched with no network, instead of the browser's own offline interstitial | Frontend | Done — confirmed with the user (2026-08-10) as a **plain static fallback**, not a cached "last-known screen": caching real per-user page HTML was already ruled out for PWA-02 (stale/wrong-participant-data risk) and the same reasoning applies here. `src/app/offline/page.tsx` (new, generic — deliberately not `SHR-07`'s `ConnectivityGate`/`OfflineState`, whose "we'll sync your progress" copy assumes an existing session this can be reached without) plus two additions to `public/sw.js`: `/offline` joins `PRECACHE_URLS`, and the `fetch` handler grew a `navigate`-mode branch — network-first (a page's real content must still always be fresh when the network is there), falling back to the cached `/offline` only on an outright failure, never on a real 4xx/5xx from the server. `/offline` also had to join `proxy.ts`'s `PUBLIC_PATHS`, the identical bug MKT-03/PWA-02/PWA-08 already hit three times now for the same reason: unauthenticated precache fetches would otherwise cache the `/login` redirect under the `/offline` key. Bumped `CACHE_VERSION` to `"v2"` — adding a precache entry under an unchanged cache name is exactly the kind of change PWA-02's own "bumping the version is the whole invalidation story" note anticipates. **The page uses inline styles, not Tailwind classes — the one deliberate exception to "tokens, not hex" in this codebase**, and earned rather than assumed: live-tested this page with the dev server stopped (a genuine network failure, not simulated) and *nothing else cached* beyond the three precache URLs, and the first version — built with ordinary Tailwind classes — rendered with zero styling (unstyled dark background, default blue underlined link), because Tailwind's compiled CSS lives in Next's own external, content-hashed chunk, which isn't guaranteed cached the very first time anyone ever goes offline. Rewritten with inline styles copied from `globals.css`'s own token values (`--color-board`/`ink`/`ink-soft`/`terracotta`/`terracotta-tint`, `--font-display`) so the raw cached HTML alone is correct — no CSS chunk, no JS hydration required (the page has no interactivity to hydrate: even "Try again" is a plain `<a>`, not `next/link`'s `Link`, on purpose — `Link` would intercept the click as a client-side transition once hydrated, a different, non-`"navigate"` fetch that would never reach `sw.js`'s fallback logic at all). Re-tested after the rewrite, same true-offline conditions: renders fully styled. **Found and left honest**: the install-time precache did not reliably populate through the normal browser lifecycle in this specific automation environment across many clean attempts, despite the underlying `cache.open(...).then(cache => cache.addAll(...))` call being independently verified correct — proven twice by invoking that exact call directly from page context (succeeds immediately, all 3 URLs cached) and once via a real install that did complete correctly with a freshly-bumped cache name. Given the mechanism is proven sound in isolation and this pattern is the standard, well-established "offline fallback" recipe, this reads as an environment-specific quirk of automated/headless service-worker lifecycle timing rather than a code defect — flagged here rather than silently assumed away. `npm test` (221), `tsc`, `eslint` green |
| PWA-04 | Offline task data — cache the signed-in user's task list for read access with no network, and queue completions/edits made offline to replay once back online | Frontend | To Do — **confirmed with the user (2026-08-10): skip entirely**, same "asked rather than guessed, and the call was to skip it for now" shape as PRO-17's channel decision. SHR-07's existing offline banner already covers the honest "you're offline" case for someone already in the app, and PWA-03's own fallback now covers a cold start with no network; participants are expected to have normal connectivity, and building real offline reads/writes (a sync queue, conflict resolution, retry logic) for a feature they may rarely need was judged not worth the scope for a research instrument. Revisit only if a real participant need shows up |
| PWA-05 | Full launcher icon set — PNG rasters (192×192, 512×512) with a `maskable` purpose variant for Android adaptive icons, plus `<link rel="apple-touch-icon">` for iOS, which does not read manifest icons at all | Frontend | Done — four PNGs generated by the new `scripts/generate-icons.ts` (`npx tsx scripts/generate-icons.ts`, same one-shot-script relationship `verify-bpca.ts` already has to `contrast.ts`, re-run by hand whenever the source SVGs change): `icon-192.png`/`icon-512.png` (`purpose: "any"`, added to `manifest.ts` alongside the existing SVG rather than replacing it), `icon-180.png` (Apple's own recommended size, wired as `metadata.icons.apple` in `layout.tsx` — `apple-touch-icon` doesn't accept SVG and is the one path every iOS version has always honoured for the home-screen icon specifically, manifest support or not), and `icon-maskable-512.png` (`purpose: "maskable"`). **The maskable icon needed new artwork, not just a resize**: `icon.svg`'s fox already fills its tile corner to corner (documented in `manifest.ts`'s own comment as the reason `purpose: "maskable"` was deliberately left off it) — a circular crop would clip the ears. `public/brand/icon-maskable.svg` is the same paths scaled to 70% and centred on a full-bleed 120×120 plate, chosen so the artwork's own furthest points (radius ≈39.6 at that scale) sit inside the spec's 80%-diameter safe-zone circle (radius 48 on a 120 canvas) with an ≈8px margin. Verified by literally compositing `icon-maskable-512.png` against a 512px circle mask (`dest-in` blend, the worst-case launcher crop) and inspecting the result: full bleed background, fox comfortably inside the circle, no clipping. Added `sharp` as an explicit `devDependency` (`^0.35.3`) rather than relying on it staying resolvable as Next's own undeclared transitive dependency, which is all it was before this ticket — and pinned above `0.35.0` deliberately: `npm audit` flagged the transitively-resolved `0.34.5` for a high-severity libvips advisory (CVE-2026-33327 et al.); `0.34.5` is still present, but only as Next's own separate nested copy for its image-optimisation pipeline (`node_modules/next/node_modules/sharp`), unrelated to and unused by this script, and bumping that would mean bumping Next itself — out of scope here. Verified live: `/manifest.webmanifest` serves all four icon entries with the right `sizes`/`type`/`purpose`, `<head>` carries `<link rel="apple-touch-icon" href="/brand/icon-180.png">`, and all four PNGs serve 200 `image/png` from `/brand/`. `npm test` (221), `tsc`, `eslint` green |
| PWA-06 | iOS splash screens — `apple-touch-startup-image` link tags per device size | Frontend | Done — `src/lib/apple-splash-screens.ts` (a plain, dependency-free device table + the `media`/URL string helpers), `scripts/generate-splash-screens.ts` (renders the actual PNGs via `sharp`, `npx tsx scripts/generate-splash-screens.ts`, re-run whenever the device list or `icon.svg` changes — same one-shot relationship PWA-05's `generate-icons.ts` has), and `layout.tsx`'s `appleWebApp.startupImage`, built from that same device table so the two can't drift out of sync. **Split the device data out of the script deliberately**: `layout.tsx` needs the same list `generate-splash-screens.ts` renders from to emit matching `<link>` tags, but it can't import the script directly — that would pull `sharp` (a devDependency, native bindings) into the server bundle just to read some numbers back out of it. Every splash is the same background/icon Android's own generated splash already draws from the manifest (`background_color` + `icon.svg`, centred at 28% of the shorter physical side) — manifest.ts's own doc comment already describes this exact picture for Android; iOS just needs it as literal pixels per device since it never computes one itself, and shows a blank white screen if no `media` query matches at all — there is no wildcard/default entry. **`DEVICES` is iPhone-portrait only, not Apple's full device matrix** — a deliberate scope cut, not an oversight: `design_handoff` only ever draws the 300×640 phone frame, INF-22 already deprioritises the desktop/tablet adaptation pass, and participants use the study on a phone. 11 devices, SE through the 16 series, deduped to physical pixel size (two devices can share one screen). Verified live: `<head>` carries all 11 `apple-touch-startup-image` links with the exact device-width/device-height/-webkit-device-pixel-ratio/orientation media queries and matching hrefs, and all 11 PNGs serve 200 `image/png` from `/brand/splash/`. Spot-checked two renders (smallest and largest) directly — centred, correct board colour, right proportions, no artefacts. `npm test` (221), `tsc`, `eslint` green |
| PWA-07 | In-app install affordance — capture `beforeinstallprompt` on Android/desktop Chrome and surface an explicit "Install app" action; add on-screen "Add to Home Screen" instructions for iOS Safari, which never fires `beforeinstallprompt` at all | Frontend | Done — `src/components/marketing/install-button.tsx`, placed as the hero's third CTA (`marketing-hero.tsx`, placement and copy confirmed with the user; no `design_handoff` mock exists for this since it postdates the handoff). Renders nothing until the client knows it's actionable — already-installed, a captured `beforeinstallprompt`, or iOS — read via `useSyncExternalStore` rather than `useState`+`useEffect`, the same call `ConnectivityGate` (SHR-07) made, to avoid both a hydration mismatch and the `eslint react-hooks/set-state-in-effect` rule. Clicking either replays the captured native prompt (Chrome/Edge/Android) or opens a small instructions dialog ("tap Share, then Add to Home Screen") for iOS Safari, which has no programmatic install API at all. The iOS dialog is a new single-action component, not the shared `Modal` — `Modal` is built for the addendum's confirm/cancel pattern, always two opposed actions, and forcing a second button that would do the same thing as the first is noise; it reuses the same tokens (`rounded-modal`, `shadow-modal`, `bg-scrim`) so it still looks like the rest of the app's overlays. **Also removed MKT-01's "Watch demo" button** from this same CTA row, 2026-08-10 at the user's request — see that row's own updated note and `marketing-hero.tsx`'s doc comment for the rationale kept on file. Verified live: hidden by default with no captured prompt (matches this automation browser, which never fires `beforeinstallprompt`); a synthetic `beforeinstallprompt` event makes the button appear, clicking it calls `prompt()`, awaits `userChoice`, and correctly hides the button again afterward; forcing the iOS branch (temporary one-line override, reverted before commit) renders the instructions dialog with the Share icon, correct copy, scrim, and a working "Got it" dismiss (`dialog.open` false afterward). `npm test` (221), `tsc`, `eslint` green. **Updated 2026-08-12, UX/UI pass**: dropped the `Download` icon from "Install app" for icon/text consistency with the rest of the app's buttons — see AUTH-07's row for the full note. The iOS dialog's own `Share` icon (its illustrative icon well, not a button) is untouched. `tsc` and `eslint` green |
| PWA-08 | Update handling — detect a new service-worker version on deploy and either refresh automatically or prompt the signed-in user ("New version available") | Frontend | Done — `src/components/pwa/service-worker-registration.tsx` grew the update-detection logic alongside PWA-02's own registration (same file, since both need the same `ServiceWorkerRegistration` object); `public/sw.js` grew a `message` listener for the other half of the handshake. **Chose "prompt", not silent auto-refresh**: TaskTails is a task-entry app, and silently reloading out from under someone mid-edit (a task title, the create-task sheet) is a worse experience than a dismissible banner. Also **doesn't gate the banner on being signed in**, despite the ticket's own wording — a stale cached build is exactly as wrong for a signed-out marketing visitor as a signed-in participant, and there was no reason to build the auth-awareness just to withhold it from one group. A new worker is detected two ways, both landing in the same `waitingWorker` state: already `waiting` when this tab attaches (another tab updated first), or `updatefound` → its `statechange` reaching `"installed"` while this tab is already controlled (the `navigator.serviceWorker.controller` check is what tells an update apart from this tab's own first, uncontrolled install, which has nothing to prompt about). No polling (`registration.update()`) — the browser already checks on every navigation and periodically in the background, which is enough for "detect it on deploy". Clicking "Refresh" posts `SKIP_WAITING` to the waiting worker, which calls `self.skipWaiting()`; the reload itself waits for `controllerchange` rather than firing immediately, since activation is asynchronous and reloading before it completes would just reload onto the *old* worker again. Banner styling has no `design_handoff` mock (postdates it) — reuses the dark-`ink` treatment MKT-02's footer already established, plus the same `--focus-ring: white` + dark-halo two-tone pattern the welcome screen (MKT-03) uses for focus visibility on a saturated background. Verified live end-to-end: registered a real "v1" worker (fresh registration, confirmed `controller: true`), edited `public/sw.js`'s `CACHE_VERSION` to a new value (a real byte-diff, the same as a real deploy), called `registration.update()`, confirmed a worker actually landed in `waiting`, confirmed the banner rendered with the right copy/icon/actions, clicked "Refresh", and confirmed the full chain completed — `skipWaiting` → `controllerchange` → automatic reload → new worker `active` → old versioned cache (`tasktails-shell-v1`) deleted by `activate`, only the new one left. Reverted the version bump after. **Found and fixed an unrelated environment problem while verifying**: a `docker compose --profile app up` container (`tasktails-app`, a baked image per INF-15, not a live dev server) had been holding port 3000 for the last ~35 minutes of this session, silently serving a stale build underneath what looked like a working `npm run dev` — explains why earlier edits appeared to work (the image happened to be built after they landed) but this ticket's edits didn't. Stopped the container (left `tasktails-db` running) and restarted the real dev server; worth knowing this can happen again if that profile gets started alongside `npm run dev`. `npm test` (221), `tsc`, `eslint` (including `public/sw.js`) green |
| PWA-09 | Manifest extras — `screenshots` for the richer Chrome/Edge install dialog, and `shortcuts` (e.g. straight to "Add task") | Frontend | Done — `shortcuts` built and verified; `screenshots` deliberately skipped, see below. `manifest.ts` now declares one shortcut, "Add task" → `/tasks?new=task`; `BottomNav` (`src/components/layout/bottom-nav.tsx`) reads that query param in an effect and opens the same create-task sheet TASK-02 already built, then `router.replace("/tasks")` strips it so a refresh doesn't reopen it. Deliberately reads `window.location.search` directly rather than `useSearchParams()` — that hook needs a `Suspense` boundary for no benefit here, since nothing in this component needs to stay reactive to the query string after mount. `openedFromShortcutRef` guards the one-time open against React Strict Mode's dev-only double-invoke. Verified live end-to-end (after tracing an unrelated false lead first — see PWA-02's amended note: a stale service-worker cache from PWA-02 was silently serving old JS throughout most of this verification, cleared by unregistering + clearing `caches` in a fresh tab): `/tasks?new=task` opens the sheet and cleans the URL to plain `/tasks`; a normal `/tasks` visit afterward stays closed; Cancel still dismisses normally. `npm test` (221), `tsc`, `eslint` green. **`screenshots` skipped, not built**: the manifest's `screenshots` field needs real captures of the running app (per spec — not mockups), and while the browser tool can capture and *show* a screenshot, there is no way from here to get the raw image bytes onto disk as an actual `public/brand/screenshots/*.png` file — no accessible temp file, no reachable CDP port, nothing on the clipboard. Raised with the user rather than worked around; the call (2026-08-10) was to skip it — it was already the ticket's own "not required for installability" nice-to-have, not worth blocking on. No `screenshots` field exists in `manifest.ts`. Revisit if the tooling gap closes, or if someone supplies real device screenshots directly |
| PWA-10 | Push notification groundwork — service-worker push handler + subscription storage | Backend | To Do — the infrastructure PRO-17 is blocked on ("a service worker + push subscriptions + a scheduler"). Depends on PWA-02; revisit alongside PRO-17 once a delivery channel is chosen, not before. **Reconfirmed with the user 2026-08-10** while wrapping up the rest of this module — still skipping, no change from PRO-17's original call |
| PWA-11 | Lighthouse PWA audit — run against the deployed build and clear the installability checklist (HTTPS, viewport meta, themed status bar, etc.) | Frontend | Done — **Lighthouse (13.4.1, installed fresh for this ticket) no longer has a PWA category at all**: `--only-categories` lists `accessibility, best-practices, performance, seo, agentic-browsing` — Google deprecated the scored PWA category (and its `installable-manifest`/`service-worker`/`splash-screen`/`themed-omnibox` audits) some versions back, and this codebase's own copy of the tool reflects that. Ran it anyway for the categories that remain (0.93 performance, 0.96 accessibility, 1.00 best-practices, 0.92 SEO, against `npm run build && npm run start` — the deployed build, not dev) and it's clean; general scores are outside this ticket's scope, so left alone rather than chased. The actual installability checklist was walked by hand against that same production build instead, since that's what the tool would have checked: web app manifest present and complete (`name`, `short_name`, `icons` with 192+512 PNGs, `start_url`, `display: "standalone"`, `background_color`, `theme_color` — PWA-01/05/09, re-verified here), `<meta name="viewport">` with `width=device-width, initial-scale=1` (also independently confirmed passing by Lighthouse's surviving `viewport-insight`/`meta-viewport` audits), `<meta name="theme-color">` present, `apple-touch-icon` present (PWA-05), iOS splash screens present (PWA-06), service worker registers and reaches `active` on the production build specifically — re-verified live rather than assumed from the dev-mode PWA-02 pass, since minified/hashed prod chunks are a genuinely different code path. **HTTPS is out of this app's hands by design, not a gap**: `.env.example` already documents TLS termination happening at "an ALB, or anything else... in front of the app," and `proxy.ts`'s `externalOrigin()` already reads `X-Forwarded-Proto` on that assumption — correctly deferred to the EC2 deployment's own config (INF-15/16), not app code. Flagging it explicitly here since a service worker is a hard secure-context requirement: without real HTTPS at the ALB, none of PWA-02 through PWA-09 install for anyone, silently. **One real, minor gap found and left honestly unfixed**: `layout.tsx`'s own comment claimed `appleWebApp.capable` still emits the legacy `apple-mobile-web-app-capable` meta tag; reading `next/dist/lib/metadata/metadata.js` shows this Next version only emits the modern unprefixed `mobile-web-app-capable` now. Tried the documented fix (`metadata.other`) — `resolve-metadata.js` merges it cleanly on paper, but it verifiably does not render in the built HTML either. Only pre-11.3 iOS (2018 and older) ever required the prefixed tag specifically, so left as a documented, accepted gap in `layout.tsx` rather than spending further time reverse-engineering Next's metadata pipeline for a device era this old. `npm test` (221), `tsc`, `eslint` green throughout |
| PWA-12 | Real-device install verification — install on an Android phone and an iPhone; confirm icon, splash, standalone chrome, and (once PWA-02 ships) offline launch | Frontend | To Do — MKT-03 already flagged this exact gap ("worth an install test on a real handset before the study opens") and could not be verified from the automation browser |
