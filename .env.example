# Copy to .env.local and fill in. Never commit real secrets.

# ---------------------------------------------------------------------------
# NextAuth (INF-11, NFR-GEN-4)
# ---------------------------------------------------------------------------
# Signs the JWT session cookie. Generate one with:  npx auth secret
AUTH_SECRET=

# Google OAuth — create credentials at https://console.cloud.google.com
#   Authorised redirect URI (local): http://localhost:3000/api/auth/callback/google
# The "Continue with Google" button stays disabled until both are set.
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# ---------------------------------------------------------------------------
# Database (INF-01 onwards) — not used yet; auth runs against an in-memory
# mock store in src/lib/mock/users.ts until Prisma lands.
# ---------------------------------------------------------------------------
# DATABASE_URL="postgresql://user:password@host:5432/tasktails?schema=public"
